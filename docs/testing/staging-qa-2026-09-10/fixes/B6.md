# B6 — Commerce (fix results)

Branch `fix/staging-qa-2026-09-10`. Findings in the order worked:
F57, F34, F100, F101, F59, F58, F55, F56, F102, F37, F35.

---

## F57 — fixed

- **Files:** `app/app/Modules/Commerce/Services/ShippingService.php`,
  `app/app/Modules/Commerce/Http/Rules/ServiceablePincode.php` (new),
  `app/app/Modules/Commerce/Http/Controllers/Storefront/AddressController.php`,
  `app/app/Modules/Commerce/Http/Controllers/Storefront/CheckoutController.php`,
  `app/tests/Modules/Commerce/ShippingServiceAreaTest.php` (new)
- **Tests:** `ShippingServiceAreaTest` F57-01 … F57-07 (7 passed)
- **Commit:** `e5b3f463`
- **What changed:** `ShippingService` now owns `mainlandOnly()` (defaults ON when the
  settings row is missing) and `servesPincode()` with the two non-mainland ranges —
  Andaman & Nicobar 744001–744999 and Lakshadweep 682551–682559. A single
  `ServiceablePincode` validation rule enforces it on the address book (`pincode`) and
  the checkout delivery address (`ship_pincode`). Billing pincodes and ADC collection
  orders are deliberately untouched.
- **Follow-up:** staging order ORD-260910-KVTQW1 (Port Blair) was accepted before this
  fix and is still live data.

---

## F34 — fixed

- **Files:** `app/app/Modules/Catalog/Models/Product.php`,
  `app/app/Modules/Commerce/Http/Controllers/Storefront/CartController.php`,
  `app/resources/views/shop/cart.blade.php`,
  `app/tests/Modules/Commerce/CartThumbnailTest.php` (new)
- **Tests:** `CartThumbnailTest` F34-01 … F34-04 (4 passed)
- **Commit:** `efd5a038`
- **What changed:** `Product::primaryImageUrl()` is the single source for "the one
  picture of this product" — first gallery image, falling back to the legacy
  `image_url` column. The cart line reads it (and now carries alt text); the cart
  query eager-loads `galleryImages` so it stays one query.
- **Follow-ups (not done, deliberately):**
  - `database/seeders/ProductCatalogSeeder.php` still sets three `image_url`s to a
    random-image placeholder service. Removing them changes the inferred array shapes
    and invalidates two `phpstan-baseline.neon` entries; the baseline is being edited by
    another batch right now, so I left it. It is seed data only — staging rows already
    exist and are covered by the catalogue-data finding (F01). One-line change when the
    baseline is quiet.
  - Pre-existing, unrelated: `CartQuantityTest`'s "highlights the just-added line" test
    uses `withUnencryptedCookie()` for the anon-cart cookie, which this app's
    `EncryptCookies` discards — the request gets a *fresh empty cart*, and the test only
    passes because `cart-line-added` also appears in the page's stylesheet. Use
    `withCookie()` (as `CartThumbnailTest` does) when someone touches that file.

---

## F100 — fixed

- **Files:** `app/app/Modules/Returns/Http/Controllers/Storefront/ReturnController.php`,
  `app/resources/views/shop/returns/create.blade.php`,
  `app/tests/Modules/Returns/ReturnFormDisclosureTest.php` (new)
- **Tests:** `ReturnFormDisclosureTest` F100-01 … F100-03 (3 passed); whole
  `tests/Modules/Returns` suite green (58 passed)
- **Commit:** `7ace4e9c`
- **What changed:** the controller derives a per-reason disclosure from
  `BuybackMatrix` — window, whether GST is refunded, whether shipping is refunded,
  the amount payable on *this* order, and the different figure if the goods turn out
  not to be saleable. The form renders it as a table above the notes field, with a
  footnote that a promo discount is never refunded as cash and that redeemed points
  and repurchase-wallet credit return in the same form they were used. The reason
  `<select>` now loops the same list, so the labels cannot drift from the matrix.

---

## F101 — fixed

- **Files:** `app/app/Modules/Commerce/Http/Controllers/Admin/AdminOrderController.php`,
  `app/resources/views/admin/commerce/orders-show.blade.php`,
  `app/resources/help/payments.md`,
  `app/tests/Feature/Commerce/AdminOrderInvoicePanelTest.php` (new)
- **Tests:** `AdminOrderInvoicePanelTest` F101-01, F101-02 (2 passed); `tests/Feature/Commerce` green
- **Commit:** `1fc7eecc`
- **What changed:** an Invoice panel on the admin order page — number, issue date and
  amount when one exists, "No invoice issued yet" otherwise — with Generate / Re-issue
  posting to `admin.payments.invoices.generate`, gated on `finance.record` and behind
  the standard confirm modal, plus a note that an invoice is issued only once the order
  is paid. The Payment panel now names the gateway and status and links
  `admin.payments.show`.
- **Follow-up:** `AdminPaymentController::generateInvoice()` still redirects to
  Payments → index after issuing, so issuing from the order page lands the admin on a
  different screen. One-line `back()` change in the Payments module — left alone as it
  is outside this batch's files.

---

## F59 — fixed (4 of 5 sub-items; the 5th was already correct)

- **Files:** `app/app/Modules/Commerce/Models/OrderCoolingOff.php`,
  `app/app/Modules/Commerce/Notifications/OrderNotificationChannels.php`,
  `app/app/Modules/Commerce/Notifications/OrderStatusChangedNotification.php`,
  `app/resources/views/shop/cart.blade.php`, `app/resources/views/shop/checkout.blade.php`,
  `app/tests/Modules/Commerce/OrderStatusNotificationChannelsTest.php` (new),
  `app/tests/Modules/Commerce/CoolingOffDaysRemainingTest.php` (new)
- **Tests:** F59-01 … F59-06 (6 passed); `tests/Modules/Commerce` green (189 passed)
- **Commit:** `1c5aa8d6`
- **What changed:**
  - Cooling-off counter now rounds up, so a window opened today reads 30 days, the
    final day reads 1 and an elapsed window reads 0. One method, so order detail,
    admin and the return form all move together.
  - `OrderNotificationChannels::withDatabase()` added; the order-status notification
    delivers mail + an in-app record (`kind: order.status_changed`). Guest buyers are
    unaffected — Laravel skips the database channel for anonymous recipients.
  - Clear-cart confirmation counts units, not lines ("All 3 items" for one line of 3).
  - Checkout Billing card shows "We will bill this order to your delivery address
    above." while "same as shipping" is ticked, instead of an empty box.
- **Already correct, no change:** the distributor price does go through `IndianNumber`
  — `ProductVariant` imports `App\Modules\Shared\Support\IndianNumber as Number`, so
  the `Number::format` the QA saw at line 74 *is* the Indian formatter. Verified by
  reading the import; the file has not changed since 2026-08-06.
- **Follow-up:** the in-app record is written but the bell does not read the
  `notifications` table yet (it counts messages + announcements) — that is F54's
  territory in another batch. Until that lands the trace is visible only to code, not
  to the buyer.
