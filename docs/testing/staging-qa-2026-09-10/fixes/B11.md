# B11 — MSB deduction, company Arete Centre, recompute-gate banner

## F33 — fixed

The client confirmed (2026-09-10) that the Mentorship Bonus is the **fifth**
repurchase-deduction source, alongside GSB, Rank, Growth Booster and Fortune —
as the 2026-06-26 clarifications always had it ("repurchase wallet = 10% of last
month's GSB + MB + GBB + Rank + Fortune").

**Engine.** `MentorshipBonusService::creditAccrual()` now credits through
`WalletService::creditWithRepurchaseDeduction()` instead of `credit()`, so an
MSB credit writes the same three ledger entries as every other bonus
(`mb_credit` gross, `repurchase_transfer` debit, `repurchase_deduction` credit),
all stamped with the cut-off day and the earned month. `WalletService` itself was
not touched. The rate, the ₹10,000 monthly ceiling and the earned-month
windowing come from that one method, so MSB automatically honours the cap
already spent by the month's other bonuses.

**Frozen figures.** `mentorship_bonus_results` gained
`repurchase_deduction_paise` and `mb_net_paise` (migration backfills
`mb_net_paise = mb_gross_paise − deduction`, i.e. gross for every pre-decision
row), matching the four other result tables. Every MSB surface now reads those
stored facts rather than re-deriving anything from the ledger.

**Surfaces updated** (the "No repurchase deduction applies to MSB" note added by
B4a is gone from both reports):

- MSB Input & Output: `Income · Repurchase deduction · Credited to wallet`
  columns and day totals, CSV gains `Repurchase Deduction (Rs)` and
  `Credited to Wallet (Rs)`, note inverted.
- MSB Calculation report: "Received Income" → "Income (credited)" showing net
  with a `gross … · repurchase …` subline (the RB report's pattern), grand total
  and CSV likewise.
- Admin distributor **Mentorship Bonus** tab: the shared
  `<x-bonus-credit-head>` / `<x-bonus-credit-cells>` three-column pair.
- Distributor **My Income → Mentorship Bonus**: same three columns; the summary
  tiles now read "MB Credited This Month / Lifetime" and sum `mb_net_paise`.

**Docs.** `app/resources/help/compensation.md` (MSB section) states the
deduction and that pre-decision rows show gross as credited;
`docs/compensation/repurchase-client-examples-2026-09-07.md` §2.4 now separates
the two ideas — a **failed cycle** still never withholds Mentorship, but the
credit-time **deduction** does apply to it. ADC and Awards & Rewards remain
outside both.

Files:
- `app/app/Modules/Compensation/Database/Migrations/2026_09_11_100000_add_repurchase_deduction_to_mentorship_bonus_results.php` (new)
- `app/app/Modules/Compensation/Models/MentorshipBonusResult.php`
- `app/app/Modules/Compensation/Services/MentorshipBonusService.php`
- `app/app/Modules/Compensation/Http/Controllers/IncomeController.php`
- `app/app/Modules/Compensation/Http/Controllers/Admin/AdminMsbCalculationController.php`
- `app/app/Modules/Compensation/Http/Controllers/Admin/AdminMsbInputOutputController.php`
- `app/resources/views/income/mentorship.blade.php`
- `app/resources/views/admin/compensation/msb-calculation/index.blade.php`
- `app/resources/views/admin/compensation/msb-input-output/index.blade.php`
- `app/resources/views/admin/compensation/distributors/_tab-mb.blade.php`
- `app/resources/help/compensation.md`
- `docs/compensation/repurchase-client-examples-2026-09-07.md`
- tests: `MentorshipBonusServiceTest`, `AdminMsbCalculationTest`, `AdminMsbInputOutputTest`

Tests (new):
- `it takes the repurchase deduction from the MB credit — the fifth deduction source (client 2026-09-10)` — three ledger entries, all carrying the earning day.
- `it honours the monthly repurchase cap: a sponsor already at the ceiling is credited gross`
- `it shows the credited amount with its gross and repurchase deduction beneath it` (calculation report)
- CSV test extended with the two new columns; the I&O note assertion inverted.

29 tests green on SQLite and on MySQL (`arovolife_test` in the container).
Pint clean, phpstan clean.

Follow-ups:
- The new migration is **not** applied to the local dev DB: several other
  batches have pending migrations on this branch and `php artisan migrate`
  would run theirs too. Run it once the branch settles.
- `IncomeControllerTest > it dates every mentorship bonus row on the distributor
  page (F62)` fails on this tree — another batch's raw insert omits the NOT NULL
  `sponsee_gsb_paise`. Not caused by, and not related to, this change.

Commit: `48cc6dbd` — another implementer's concurrent `git commit` swept my
staged index into their commit (its message names F18). The F33 content is on
the branch in full; only the commit message is theirs.

---

## F120 — fixed

**Engine.** `AreteDevelopmentCenterBonusService::runForMonth()` now skips any
centre whose `centre_type` is `company` **before** it looks at
`assigned_distributor_id`. A company centre is ownerless by definition; gating
on the id alone let staging's company-default centre (with
`assigned_distributor_id = 33`) pay a distributor ₹10,500 for 3,50,000 BV of the
company's own sales. The existing null-owner skip stays, now correctly described
as "a distributor centre awaiting its owner".

**Admin.** `assigned_adn` gains `prohibited_if:centre_type,company` with the
message "A company centre is ownerless and earns nothing. Leave the owner ADN
blank, or change the type to Distributor centre." — a validation error on both
create and edit, never a 500. The form copy already said the centre is ownerless
and earns nothing; it is unchanged.

**Recompute coverage (verified, not edited).** Both rows the staging defect
wrote are inside `DerivedTables`: `adc_bonus_results` is in `TABLES` and windowed
by `month_start` at month granularity (line 149), and `wallet_ledger_entries` is
in `TABLES` and windowed by `created_at` at day granularity (line 130); only
`repurchase_wallet_used` survives a wipe. So a windowed recompute covering
September 2026 rebuilds both, and with this fix the company centre produces
neither. One caveat for whoever runs it: the wallet window is on the **write**
date, not the earned month — the staging entry was written 2026-09-05 for
September, so a September window catches it, but a credit written in a different
month from the one it was earned for would need the window to include its write
date. `DerivedTables.php` not touched (B2 owns it).

Files:
- `app/app/Modules/Compensation/Services/AreteDevelopmentCenterBonusService.php`
- `app/app/Modules/Compensation/Http/Controllers/Admin/AdminAreteCenterController.php`
- tests: `AdcBonusServiceTest`, `AdminAdcReportsTest`

Tests (new):
- `it never pays a company centre, even when one carries an assigned distributor (F120)`
- `it refuses to assign a distributor to a company centre (F120)` (create)
- `it refuses to give an existing company centre an owner on edit (F120)`

`makeActiveCenter()` in `AdcBonusServiceTest` now sets
`centre_type = distributor` — the column defaults to `company`, so every fixture
built through it was silently a company centre.

74 tests green (`--filter='Adc|Arete'`). Pint clean, phpstan clean.
