# Staging QA — 2026-09-10 — bonus engines, payouts and the last week's changes

Orchestrator: Fable 5.1 (plans, monitors, consolidates). Verifiers: Opus (browser + money math), Sonnet (data setup, log sweeps).
Each task is one subagent with a small brief. Each task writes `results/Txx-<slug>.md` with a
PASS / FAIL / BLOCKED verdict per check and the evidence (query output, screenshot description, URL).
Status legend: `[ ]` not started · `[~]` running · `[x]` done · `[!]` done with defects · `[-]` blocked.

## Baseline (verified by the orchestrator before any task ran)

- Staging = `https://phplaravel-1611779-6390605.cloudwaysapps.com`, deployed commit `6f114500` (= origin/main HEAD), 0 pending migrations, `earned_on` backfill count = 0 (OK), config not cached.
- Every feature flag is ON (FAQ + Announcements toggled on 2026-09-10 10:44 by the developer account).
- Queue: `QUEUE_CONNECTION=database`; the three supervisor workers run `queue:work redis` — that name is aliased to the database driver in `config/queue.php` (ADR-0011). Probe job was consumed in < 12 s. Workers restarted 2026-09-10 10:40 IST (after the 10:32 deploy). Queues otp / default / compensation all empty.
- Scheduler is live (GSB + MSB pools froze at 00:10 IST every day up to 2026-09-10). The engine_runs rows before today were produced by the PRE-deploy schedule (repurchase 00:30, Rank Bonus on the 8th 08:00, Fortune on the 9th 09:00, monthly payout on the 9th 10:30). The new orchestrated schedule (monthly-close 1st 00:20, payout-close 8th 04:00, weekly Tue 03:00, repurchase 00:05) has never fired on staging yet.
- ⚠ Suspect: `rank.bonus` / `rank.check` / `gbb.monthly` / `adc.bonus` / `offers.monthly` runs carry `period_start = 2026-09-01` — an in-flight month — and wallet holds 4 `rank_credit` rows (₹37,189) + 1 `adc_credit` (₹10,500). `fortune.enroll/payout` carry 2026-08-01. See T14.
- Data: 317 distributors (35 users active, 284 pending), 31 company reserved accounts (`reserved-NN@arovolife.local`, ADNs 444555666 …). 16 paid `online` orders (stub gateway, 2026-09-04..06) all by 7 reserved accounts; 20.39 lakh BV accrued in Sep. Only 2 distributors have bank details. 3 payout batches, all `pending`, `distributor_count 0`, `total_net 0`.
- Gateways: `payments.gateway.stub.enabled=true`, razorpay disabled, COD enabled; `payout.gateway=manual_neft`, min payout ₹100.
- Admin login: admin@arovolife.test / admin12345 (verified hash). Developer account: user 242.
- Full test suite passed on MySQL this morning (2046 passed, 1 skipped).

## Rules every agent follows

1. Staging only. Never touch other apps on the Cloudways box. Never deploy, never `git pull`, never edit `.env`, never run `migrate:fresh`/`recompute-all`/`purchase_reset`/`queue:clear`.
2. Never run a pool-freezing engine for an in-flight period (today, or the current month). Closed periods only.
3. Record every mutation you make (row ids, before/after) in your result file under "Mutations".
4. Verdicts need evidence: paste the query + result, or describe the screenshot/URL. No "looks fine".
5. A mismatch between UI and DB, UI and plan parameters, or code and docs is a FAIL. Do not rationalise it away.
6. Browser tasks: one Chrome session at a time (shared cookies). Log out when done.

## Tasks

### Phase 0 — setup (SSH/DB only)
- [x] T01 QA logins + bank details + KYC for the 7 earning reserved accounts; remove the probe failed job 239. (opus)
- [x] T02 Product BV sanity on staging: variant BV vs price (a ₹1,560 order accrues 2.5 lakh BV) — test data or bug? (sonnet)

### Phase 1 — engines and schedule, verified at the database (SSH/CLI)
- [!] T10 `repurchase:evaluate` + repurchase cycles + forfeit model vs `docs/compensation/repurchase-client-examples-2026-09-07.md`. (opus)
- [!] T11 `gsb:daily-cutoff` for a closed day: pools, slabs, carry-forward, MSB point value, per-row deductions, premature-freeze self-heal. (opus)
- [!] T12 `gsb:weekly-payout` — Tuesday-only rule, earnings_through, hold reasons, ₹100 threshold, 3 % admin (4 groups ≤ ₹25k) + 5 % TDS order, repurchase sweep. (opus)
- [!] T13 `compensation:monthly-close --month=2026-08` — resume semantics, step order, rank check gate for Rank Bonus, GBB prior-month waiver, Fortune enrol/payout, ADC, offers. (opus)
- [!] T14 In-flight-month results audit (Sep rows from the pre-deploy schedule): what the new code will do on 1 Oct; is a windowed recompute needed; wallet impact. (opus)
- [!] T15 `compensation:monthly-payout-close` gate + `payout:monthly-run` + monthly batch on the 8th. (opus)
- [x] T16 `compensation:engine-health-digest` — failed/missed/stuck detection, recipient setting, email actually leaves. (sonnet)
- [x] T17 Scheduler + cron + worker audit: schedule:list vs EngineRegistry vs docs, IST timezone, withoutOverlapping, worker restart after deploy, R-71. (sonnet)
- [x] T18 Payout auto-retry, NEFT reconciliation CLI paths, `payments:reconcile` / `orders:expire-unpaid` sweeps. (sonnet)

### Phase 2 — distributor journeys in Chrome (as a customer)
- [!] T20 Login → dashboard → income snapshot → repurchase-wallet traffic light → My Business. (opus)
- [!] T21 Shop → cart → checkout (stub pay + COD) → confirmation → My Orders → invoice; BV accrual; then admin ships/delivers; notifications. (opus)
- [!] T22 Income pages (GSB history/export, Genos BV, Genos ledger, Mentorship, Rank, GBB, Fortune, ADC, Wallet/export) foot to DB; Indian number format; forfeit + payout-week copy; no projections. (opus)
- [!] T23 Genos tree, sponsorship tree, team roster + download, ID-card stats, downline_stats_visible rule. (opus)
- [x] T24 Profile, password, bank details form, KYC resubmit/re-upload, documents, tax statements, membership card. (opus) — PASS-with-notes, results/T24-profile-kyc.md (F70–F73)
- [x] T25 Announcements, FAQ, messages (rate limit / block / report / PII guard), my requests, grievances + tracking. (opus) — PASS-with-notes, results/T25-communications.md (F74–F80)
- [!] T26 Public site: policy pages, public FAQ, grievance form, registration wizard first steps (joining free, orientation gate), no income claims. (opus)

### Phase 3 — admin in Chrome
- [x] T30 Engine Runs page: closed-period manual trigger through the queue, events page, recompute progress, in-flight refusal, flag-off hidden. (opus) — PASS-with-notes, results/T30-engine-runs-page.md (F81–F85)
- [x] T31 Compensation reports + I&O + exports foot to DB. (opus) — PASS-with-notes, 77 checks, all 17 reports foot to the paise; results/T31-compensation-reports.md (F86–F92)
- [x] T32 Weekly + monthly payout pages, batch detail, NEFT screen, approval, holds, Payout Settings developer-only. (opus) — PASS-with-notes, results/T32-payouts.md (F93–F99); batch 3 approved (audit 3218)
- [x] T33 Admin orders, BV ledger + export, payments, refunds, returns/buyback. (opus) — PASS-with-notes, results/T33-admin-commerce.md (F100–F105); order 18 delivered, order 17 returned+refunded end to end
- [x] T34 Distributors, KYC review, distributor requests, tree, audit log, feature flags, settings, help docs in sync. (opus) — PASS-with-notes, 56 checks, results/T34-admin-identity-settings.md (F106–F112); KYC approved user 34, doc 16 flagged
- [x] T35 Announcements / content pages / FAQ CRUD + publish-one-at-a-time; messaging reports. (sonnet → opus after two stalls) — FAIL (2 High), results/T35-content-announcements.md (F113–F118)
- [x] T36 Analytics, dormancy, grievances admin + report + SLA sweep, Arete centres. (sonnet) — FAIL (2 Critical), results/T36-analytics-grievances-adc.md (F119–F121); ticket 2 closed, ticket 1 escalated

### Phase 4 — cross-cutting
- [!] T40 Compliance sweep of last week's user-facing copy (hard rules, R-75 payout-week copy, forfeit wording). (compliance-officer)
- [!] T41 Security spot-check of new endpoints (messaging, announcements, payments webhooks, payout settings). (security-auditor)
- [x] T42 Log + failed-jobs sweep across the whole QA window. (sonnet) — PASS-with-notes, results/T42-log-sweep.md (F122–F123)
- [x] T43 Reusable staging QA playbook written: `docs/testing/staging-qa-playbook.md` (fable, 2026-09-11)

## STATUS (2026-09-11 ~09:00 IST) — first full run COMPLETE

All 30 tasks done (T01–T02, T10–T18, T20–T26, T30–T36, T40–T43). Findings F01–F123 in the register below; consolidated report in `99-final-report.md`; reusable playbook at `docs/testing/staging-qa-playbook.md`. The original RESUME POINT (13:30 IST on 2026-09-10) is retired — it sat inside the findings table and has been removed.

## Findings register (filled by the orchestrator from result files)

| # | Sev | Task | Finding | Status |
|---|-----|------|---------|--------|
| F01 | Medium (data) | T02 | 9 of 13 staging product variants carry BV wildly out of proportion to price (e.g. `AV-AG-009-V1` ≈ 4,050× MRP). Accrual math is exact; catalogue values are test data. Real BV values needed before UAT numbers mean anything. | Open — client action |
| F02 | Medium (data) | T16 | `gbb.monthly` and `offers.monthly` have no Aug-2026 engine run on staging (only a Sept-dated one) — digest flagged both correctly. Cross-ref T14. | Closed by T13 (Aug runs now exist) |
| F03 | Low (infra) | T16 | ElasticEmail returned "421 Daily limit exceeded" 454 times on 2026-09-05 — a digest email could silently fail on a bad day. | Open |
| F04 | Info | T16 | User to confirm inbox: "Compensation engines need attention — 2 item(s)" sent ~12:22 IST 2026-09-10 from noreply-staging@karonix.com. | Awaiting user |
| F05 | High (code) | T14 | `EngineStatusService::hasSucceededRun` matches by period date only, so a run that started BEFORE the period ended satisfies the monthly-close resume check and the payout-close gate. On staging the pre-deploy Sept runs mean `monthly-close --month=2026-09` on 1 Oct would skip all 7 steps (Sept frozen on 17.89 L BV vs 20.39 L actual) and 8 Oct would sweep premature Sept credits. In prod the CLI OpenMonthGuard blocks in-flight runs unless `--in-flight`, so exposure is via override/regression. Fix: resume + gate must require `started_at` after the period end (same idea as `hasSucceededRunAfterDay`). Verified by orchestrator. | Open — fix before launch |
| F06 | High (code) | T14 | `create_rank_monthly_pools_table` migration has no backfill: months with credited `rank_bonus_results` but no pool row make `refuseUnfrozenPaidMonth()` throw, so `monthly-close --restart` aborts at the Rank step. | Open |
| F07 | High (code) | T14 | `purchase_offer_grants` is not in the recompute `DerivedTables` list — grants survive every recompute and can never be re-evaluated. | Open |
| F08 | Medium (code) | T14 | A premature `fortune_monthly_pools` row permanently closes Fortune enrolment for that month (0 participants). | Open |
| F09 | Low (code) | T14 | `adc_credit` wallet rows written with `bonus_month = NULL`. | Open |
| F10 | Action | T14 | Staging cleanup needed before Oct: windowed recompute from 2026-09-01 (Engine Runs page). Deletes Sept derived rows incl. payout batches 1–3 and 23 wallet rows. **Needs user go-ahead (destructive on staging data).** | Awaiting user |
| F11 | Medium (infra) | T17 | Compensation-queue supervisor conf on staging uses `--timeout=999`, not the 7200 ADR-0011 mandates. Chain/recompute jobs override it with their own `$timeout`; `PropagateGroupBvJob` / `ReverseGroupBvJob` do not. Fix in the Cloudways supervisor entry (and prod's). | Open |
| F12 | Medium (code) | T17 | `PropagateGroupBvJob` / `ReverseGroupBvJob` declare `$tries = 3`, overriding the compensation worker's `--tries=1` policy. Idempotent upserts limit the risk; still contradicts ADR-0011 and is untracked in the risk register. | Open |
| F13 | Low | T17 | Cron entry for the app is not readable as master user; scheduler firing inferred from daily 00:10 pool freezes and 5-min sweeps. | Info |
| F14 | Medium (code) | T18 | Manual-NEFT reconciliation import (`PayoutReconciliationService::import`) validates no amounts — a bank file with a wrong amount imports silently despite the commit claiming mismatch detection. | Open |
| F15 | Medium (code) | T18 | No duplicate-UTR guard on NEFT import and no unique constraint on `payout_line_items.utr_number` — two lines can be settled against one bank reference. | Open |
| F16 | Low (code) | T18 | `orders:expire-unpaid` final gateway check only looks at Razorpay intents; stub-only environments rely solely on the stub confirmation transaction. | Info |
| F17 | High (compliance) | T40 | `ContentPageSeeder::run()` publishes all five policy pages, including `compensation` (payout-week + 8th cadence) that R-75 says must stay unpublished until the DSA §6.2 notice. `platform:reset` / `db:seed` both take that path. | Open — fix before launch |
| F18 | High (compliance) | T40 | Messaging `reporting_enabled` defaults ON while the DPDP purpose/retention rows live only in the unpublished `privacy.md`. Publish privacy first (`content:publish privacy`) or ship reporting OFF. | Open |
| F19 | Medium (copy) | T40 | `income/wallet.blade.php:159,169` describe the old payout-time deduction ("after your first payout is processed", "withheld from payout"); the shipped model deducts at credit time. | Open |
| F20 | Medium (copy) | T40 | `shop/pay-unavailable.blade.php:16` promises a refund without the 7-working-day timeline. | Open |
| F21 | High (code) | T10 | Migration `2026_09_06_100003_backfill_verdicts…` has no `due_date < today` guard (its docblock claims one) — it settled all 7 in-flight staging cycles as `completed` with `fulfilled_on = due_date` and NULL wallet columns; the wallet=0 condition will never be applied to that window and nothing self-heals (the undo path only covers premature *failed* verdicts). Prod exposure only if cycles exist at deploy time. | Open |
| F22 | Medium (code) | T10 | Existing open cycles keep the old `start+29` due date (2026-10-03) while new code uses `start+30`; no migration re-dated open windows. | Open |
| F23 | Medium (design) | T10 | One throwing distributor makes `repurchase:evaluate` exit non-zero, which makes `gsb:daily-cutoff` refuse platform-wide every night until someone intervenes. Consider per-distributor isolation + a FAILED-with-partial summary. | Open |
| F24 | Low | T10 | `repurchase:evaluate` writes no run summary; an orphan `repurchase.snapshot` engine_runs row remains for a retired engine. | Info |
| F25 | High (security) | T41 | `MessageController::show()` (`GET /messages/{id}`) only checks "not yourself" — any logged-in distributor can open a thread view against any sequential user id and see that user's full name/email; audience rules gate sending only. Needs `abort_unless(canMessage OR thread exists, 404)` + a policy test. | Open — fix before launch |
| F26 | Medium (security) | T41 | Announcement publish/email and content-page archive lack a dedicated permission (admin-finance can do them, R-17); recompute/reset endpoints gated on `finance.record` rather than the developer role. | Open |
| F27 | Info | T41 | Staging HTTP probes were not run (local `curl` denied by the permission system); source-derived findings only. Re-test list in the report. | Info |
| F28 | Low (schema) | T01 | No bank-account-holder / beneficiary-name column exists anywhere — the NEFT export cannot emit a beneficiary name distinct from the user's full name. T12/T32 to confirm what the export sends. | Answered by T12 → see F44 |
| F29 | Info | T01 | Harness flagged the setup agent's actions for review; audited: only the 3 QA passwords, dummy encrypted bank details on distributors 1–7, and the probe failed-job deletion — all as briefed. | Closed |
| F30 | High (data) / Medium (design) | T11 | 2026-09-05 GSB/MSB pool on staging was frozen mid-day (pre-deploy recompute) at 5.59 cr BV vs 8.09 cr true; MSB point value ₹430 vs ₹622 → distributor 1 underpaid ₹7,488. Self-heal detected it but kept the funded premature row with only a `premature_freeze_kept` log line — no audit row, no failed run, no digest signal. Data fix = F10 windowed recompute; design fix = surface "kept premature freeze" as a health-digest item. | Open |
| F31 | Medium (code) | T11 | `gsb:daily-cutoff` has no closed-day guard on the CLI (`--date` defaults to today); it is blocked only as a side effect of the repurchase gate, which `--force` / flag-off removes. Add an explicit refusal like OpenMonthGuard. | Open |
| F32 | Low | T11 | `admin_charge_paise` / `tds_paise` on `gsb_cutoff_results` are always 0 (deductions are payout-time) — misleading columns for reports. | Info |
| F33 | Question (business rule) | T11 | Older note (payout-time model, 2026-09-05) lists 5 deduction sources incl. `mb_credit` "per spec"; the shipped credit-time system deducts from 4 (GSB/RB/GBB/FB) and excludes MSB. Staging data confirms MSB credits carry no deduction. **User to confirm whether MSB should be a deduction source.** | Awaiting user |
| F34 | Medium (UI) | T26 | Cart thumbnails hot-link `picsum.photos` (third-party image, differs from the product page's S3 gallery). Cart renders `image_url`, PDP renders the gallery — they can always diverge. | Open |
| F35 | Medium (compliance/UX) | T26 | No public refund / cooling-off / shipping policy page and no footer link, while product pages show "30-day returns" badges. | Open |
| F36 | Medium (product decision) | T26 | Guests can see full pricing and build a cart; sign-in is only demanded at checkout. Matches the `guest_checkout` setting's documented meaning, contradicts the "members-only buying" expectation. **User to decide.** | Awaiting user |
| F37 | Low (copy/UI) | T26 | `/join` doesn't say joining is free; policy pages lack the print contact footer; one person is both Grievance Officer and DPO; banner says "arovolife Shopping Mall"; shop shows a 7th category; mobile carousel arrows overlap hero text; Immunity Booster listing shows VitaPlus pack art. | Open |
| F38 | Info | T26 | Test grievance GRV-260910-2PF6K created on staging — delete after QA. Every known staging ADN is placement-full, so registration wizard steps 2+ could not be reached (source-verified only). | Info |
| F39 | Medium (design) | T13 | `compensation:monthly-close` preflight waits (10 min) for the month's last-day GSB cut-off, then fails with `summary/error NULL`. On staging Aug-31 has no cut-off (history starts 4 Sep) so the Aug close can only run with `--force`. In prod the same shape appears whenever the last-day cut-off is missing (F23). Failed run shows no cause on the Engine Runs page / digest. | Open |
| F40 | Medium (code) | T13 | `MonthlyEngineCompletionGate::blockingFailure()` ignores a FAILED `compensation.monthly-close` run — it only reads the 7 crediting engines. A month whose close aborted before step 1 (but whose engines have older runs) reads "ready to pay" on the 8th. Same family as F05. | Open |
| F41 | Low (data) | T13 | `rank_qualifications` id 5 exists for a zero-BV August (pre-deploy artefact). Fold into F10 cleanup. | Info |

| F42 | Low (data — corrected by T32) | T12 | **T32 correction:** the NULL `earnings_through` is stale data, not a live bug — the stamping code deployed 2026-09-10 03:40 after both batches ran, and the batch-1/batch-3 income overlap is by design; batch 1's Saturday date remains an unexplained seed artefact. Original: Payout batches 1 (dated Saturday 2026-09-05) and 3 carry held lines for 4–6 Sept income that under the deployed Wed→Tue rule belongs to the 2026-09-15 batch; `earnings_through NULL`, never corrected by code or migration. Same cleanup as F10. Weekly maths otherwise reconcile to the paise: 2026-09-15 batch should pay ₹71,124.31 net to 3 distributors. | Open → F10 |
| F43 | Medium (observability) | T12 | A non-Tuesday refusal of `gsb:weekly-payout` records `engine_runs` status `failed` with `error NULL`, keyed to a date the engine can never succeed on, so the health digest reports it for 30 days with no explanation. Staging row: engine_runs id 43 (QA mutation). Refusals should not be failures, or must carry their message. | Open |
| F44 | Medium (ops) | T12 | Manual-NEFT export emits `Bank Last 4` only (no account number / IFSC) — it is a reconciliation sheet, not a bank upload, while `payout.gateway=manual_neft` is live and admin copy says "upload it to the bank". Payee name = `users.full_name` with no ADN fallback. | Open — decide before launch |
| F45 | Low (design) | T12 | Weekly batch (Tue 03:00) and monthly payout close (8th 04:00) share the ₹50L income cap with no lock; separation is by clock only. | Open |
| F46 | Info | T12 | `payout_batches.total_gross_paise` is not additive across batches (held lines re-appear every week). Finance reports must not SUM it. | Info |
| F47 | High (code) | T15 | `payout:monthly-run` has no gate: not in `OpenMonthGuard::FREEZING_COMMANDS`, no `MonthlyEngineCompletionGate`, and `--month` defaults to the CURRENT month. One hand-typed CLI run sweeps and marks-paid every unswept monthly credit, defeating the 1st→8th buffer. Verified by orchestrator (MonthlyPayoutCommand.php:19-39, OpenMonthGuard.php:33-41). | Open — fix before launch |
| F48 | High (design) | T15 | The monthly batch has no earning window — selection is `swept_by_payout_batch_id IS NULL` only and `earnings_through` is never stamped for monthly batches. The gate certifies month M's engines while the batch pays the whole wallet (batch 2, August's, holds September ADC income). On 8 Oct the September close passes and sweeps everything. | Open — fix before launch |
| F49 | Medium/High (data) | T15 | Batch 2 (pre-deploy) owns `batch_date 2026-09-01` with `processed_at` set, so `--month=2026-08` returns "Batch #2 pending — 0 distributors" forever; a permanently unpayable month is indistinguishable from a ₹0 month. Cleanup = F10. | Open → F10 |
| F50 | Medium (observability) | T15 | Deliberate refusals of the payout close record `engine_runs.status=failed, error NULL` (reason only in audit_log). Same class as F43/F39. | Open |
| F51 | Info | T15 | October acceptance target from current wallet rows: ₹30,736.56 net to 4 distributors; d33 held `web_only`. Engine_runs 45–48 are QA mutations. | Info |
| F52 | Medium (code) | T20 | Dashboard income snapshot "THIS MONTH" (₹80,495.82) exceeds LIFETIME (₹78,053.84): `WalletService::creditTotalsByMonth()` sums every `amount_paise > 0` row with no type filter, so `repurchase_deduction` rows (₹2,441.98 = the gap) count as income. Affects the tile, the TOTAL row and the 6-month chart. | Open — fix before launch |
| F53 | High (compliance) | T20 | Widens F17: the Wed→Tue payout week and 8th-of-month cadence are already visible to logged-in distributors in dashboard + My Business tooltips, so keeping the `compensation` policy page unpublished does not keep the un-notified cadence unseen (R-75). | Open |
| F54 | Low (UX) | T20 | Notification bell sums messages + announcements into one badge but always links to /messages; announcement-only unreads cannot be cleared there. My Business shows carry-forward / carry-over as identical pairs with no hint; hero-card gradient differs between dashboard and My Business. | Open |
| F55 | High (commerce) | T21 | Product page shows "Distributor price ₹450" to logged-in members (`shop/product.blade.php:136-141`) but the cart charges `sale_price_paise` ₹549 (`CartService.php:99`); `distributor_price_paise` is display-only. Price shown ≠ price taken. Verified by orchestrator. **Decide which price members pay, then make PDP and cart agree.** | Open — fix before launch |
| F56 | Medium (commerce) | T21 | `payments.cod.enabled=true` is an orphan setting — checkout hardcodes online payment only; COD cannot be chosen. | Open |
| F57 | Medium (commerce) | T21 | `commerce.shipping.india_mainland_only=true` is enforced nowhere — an order to Port Blair 744101 was accepted (staging order ORD-260910-KVTQW1). | Open |
| F58 | Medium (copy) | T21 | "GST invoice — issued for every order" badge contradicts the invoice's own "not a GST tax invoice" line (R-28 deferred). | Open |
| F59 | Low (UI) | T21 | Distributor price rendered with `Number::format` not `IndianNumber`; empty Billing card on order detail; "All 1 items" plural; "29 days remaining" off-by-one on the cooling-off counter; no bell notification on status change (email only). | Open |
| F60 | Info | T21 | QA orders on staging: ORD-260910-YOAJJK (id 17, delivered) and ORD-260910-KVTQW1 (id 18, shipped) by distributor 5; audit rows 3185–3193. BV accrual, group BV, stub intents and audit all exact. BV-ledger CSV download unverified (needs a human click). | Info |
| F61 | High (UI/data) | T22 | **Corrected by T31 (F90):** `gsb-calculation` renders the STORED `weaker_bv_paise` (verified = min(side + carry-forward) on every row); the remaining defect is the missing Left/Right label, not a recomputation in that report. The blade recompute in `income/genos-bv.blade.php:243` (distributor side) still stands. `/income/genos-bv` "Weaker side" is recomputed in the view from raw day legs (`genos-bv.blade.php:243` `left <= right ? 'Left' : 'Right'`), ignoring carry-forward; the engine's stored `weaker_bv_paise` / `power_side` differ (06 Sep shows "Left" weaker with Left = 0 and Slab 3). Show the stored side. Verified by orchestrator. | Open — fix before launch |
| F62 | Medium (UI) | T22 | "Power CF after" has no Left/Right label while the power side flips R→L in view; Genos Ledger says "No Genos BV added this day" beside "GSB earned · Slab 3" (personal-BV top-up has no ledger line); Mentorship table lacks a Date column despite a date filter. | Open |
| F63 | Medium (UI) | T22 | Wallet ledger is dated by `created_at` (₹256 credit shows 07 Sep) while GSB History says 06 Sep; no bonus_month / earned_on / batch shown anywhere on the wallet page. | Open |
| F64 | Low | T22 | Raw enum "No_bank_account" hold status; `/income` title breaks the "My Income —" pattern; wallet CSV uses internal enums. | Open |
| F65 | Question (compliance) | T22 | Genos Ledger lists full downline ADNs regardless of `genealogy.downline_stats_visible`. ADNs are not money, but confirm this is intended under R-65. | Awaiting user |
| F66 | Info | T22 | F19 confirmed live on the wallet page; F30 and F53 reproduced in the UI. Forfeit-window copy unverifiable on staging (no forfeited window exists after the F21 backfill). | Info |
| F67 | Medium (UI) | T23 | Re-rooted Genos view (`/tree/{adn}`) puts the "YOU" ribbon on the downline member's card and keeps "Showing your placement…" (`TreeController::binary()` sets `$self = $candidate`; `_binary-node.blade.php:5`). Authorization is correct. | Open |
| F68 | Low (copy) | T23 | `/tree` banner exposes "the binary placement tree"; membership-card back uses employee wording ("while in office", "HR's notice") for an independent contractor; dashboard PERSONAL BV tile truncates. Roster modal renders all 316 rows with no pagination/sort/filter. | Open |
| F69 | Info | T23 | All tree/team counts foot to the DB; `downline_stats_visible` is OFF (no settings row) and downline rank/BV read "—". `/tree/suggest` returns downline email + phone (subtree-scoped) — fold into F65. Mobile width unverified (resize had no effect). | Info |
| F70 | High (launch) | T24 | No self-service bank-details path. Bank is optional at registration (blank → NULL); the only distributor-side write is the wizard route `/register/kyc/bank` (`wizard.progress:8`), and `ProfileController::update()` explicitly rejects PAN/Aadhaar/bank input. Confirmed by grep: `bank_account_enc` is written only by `RegistrationService` and `AdminDistributorController`. A distributor who skips bank at sign-up is held `no_bank_account` forever unless admin keys it in. Also confirms F28 (no confirmation/beneficiary-name field on the one form that exists). | Open — user decision |
| F71 | Medium (compliance copy) | T24 | `/profile/withdraw-consent` tells a fully active ADN with zero `consents` rows "Your consent has already been withdrawn. There is nothing further to do here." `hasLiveConsent()` cannot distinguish withdrawn from never-recorded, so the DPDP §6 withdrawal flow (consequences → typed WITHDRAW → "Keep my account" → DPO contact) is hidden from accounts whose consent was never persisted (the 271 `1.0.0` + 15 `2026-08-30` rows do not cover every account). | Open — user decision |
| F72 | Medium (compliance record) | T24 | `agreements` registry table has 0 rows on staging while `consents` has 1,144 rows across two version schemes (`1.0.0` ×271 distributors, `2026-08-30` ×15). No distributor-facing consent/agreement record page exists (`/profile` links only the Privacy Policy; `/dashboard/documents` is the KYC-upload screen). DPDP §5/§6 record-of-consent is not surfaced to the data principal. | Open — user decision |
| F73 | Low (UI/copy) | T24 | Seven low items: (a) profile bank mask is a literal `••••` — real last-4 never shown, PAN `XXXXXX8994` / Aadhaar `…0000` masks are correct; (b) password validation copy leaks the field name `new_password` (StrongPassword + NotPwned messages); (c) doubled page title on profile; (d) membership-card back copy "while in office"/"HR's notice" (= F68, now confirmed verbatim); (e) DSA PAN mask is 11 chars `XXXXXX8994X` vs 10 on profile; (f) 31 reserved accounts print "30-day window" while `cooling_off_end_at` equals join time (0-day cooling-off); (g) bank field clips at 422 px. Everything else passed: read-only fields 1–5 ignored server-side, no raw PII in page source, confirm modals + audit rows 3199–3208, OTP-gated mobile change (reverted), HIBP refusal, password round-trip (reverted, `Hash::check`), KYC resubmit/reupload guards (403 unsigned), tax statements honest (TDS 0), no console errors, slowest page 577 ms. | Open |
| F74 | High (environment, UAT blocker) | T25 | No malware scanner on staging: every file upload is refused with "Set CLAMAV_HOST and run clamd" (laravel.log 19:13:06). Fail-closed is correct, but it blocks the whole Distributor Requests feature, grievance evidence attachments and KYC re-upload on staging. Install/point ClamAV before UAT. Refusal copy also leaks the raw field path `documents.id_proof.0` (`ScannedForMalware.php:51`). | Open |
| F75 | Medium (security, confirms F25) | T25 | `MessageController::show()` has no audience/participation check: `GET /messages/{id}` returned 200 with the target's display name for ids 75, 73, 242, 1 (999999 → 404), so the user base can be enumerated by name. Bodies do not leak and the send path is guarded. | Open |
| F76 | Medium (coverage) | T25 | ADN 444555666 is the tree root (316 of 317 distributors are its descendants), so the `downline_upline` messaging audience restriction can never fire from it; `announcements` and FAQ `content_pages` are both empty on staging, so draft/archive filtering, read-marking, the announcement badge and the FAQ accordion were verifiable only by code. Re-test messaging from a leaf ADN and seed one announcement + one FAQ before UAT. | Open |
| F77 | Medium (compliance copy) | T25 | `/help` says the helpline is 10:00–18:00 Mon–Sat while the site footer says 9:30–17:30 daily except Sundays. One of them is wrong; the published Grievance Redressal Policy must match. | Open — user decision |
| F78 | Medium (UX) | T25 | Failed distributor-request submit wipes the typed name and reason (no `withInput()` on that path). | Open |
| F79 | Low (UI) | T25 | Flash banners render twice (layout + view) on block, grievance creation and message report; grievance-reply confirm modal falls back to a generic "Please confirm". | Open |
| F80 | Info | T25 | Verified: PAN and Verhoeff-valid Aadhaar guards refuse message bodies; block/unblock round-trip (UI + 422 on direct POST) with a non-disclosing refusal to the blocked sender; message report creates `message_reports` id 1; grievance `GRV-260910-39CCK` created with SLA timestamps matching the published 48 h / 5 working days / 30 days; distributor reply; 404 on another person's ticket; public tracking accepts the right email and refuses a wrong one. Console clean; slowest page 217 ms. | Info |
| F81 | High (observability) | T30 | Failed engine runs record no reason anywhere: `RecordEngineRun::finished()` never writes `error`, so all 49/49 `engine_runs` rows have `error IS NULL` and the Events page shows DETAILS "—" for both failures. Run 37's reason exists only in laravel.log; run 43 has no log line at all. Generalises F39/F40/F43/F50: it is the column, not the rows. | Open |
| F82 | Medium (engine safety, staging — BY DESIGN) | T30 | Premature-freeze guard is deliberately lifted on staging while the recompute testing gate is open (code comment in `AdminEngineRunsController::parsePeriodOrFail`: "an operator may run any period to preview it — but only inside the replay's own horizon"; capped at end of next month). Verified by me: `.env` has `COMP_RECOMPUTE_ENABLED=true` with `ahdhesuhty` in the allowed DB list, so `parsePeriodOrFail()` returns before the `requiresClosedPeriod` check. Every period input's `max` is end-of-NEXT-month (`gbb.monthly` max `2026-10`, `gsb.daily-cutoff` max `2026-10-31`) and the page offers the current in-flight month — the 24-Aug bug class. Not pressed. Risk: staging is where the 24-Aug premature freeze happened and where the client reviews numbers; an admin can still freeze the live month by mistake. Decide whether staging keeps the gate open; production must have `COMP_RECOMPUTE_ENABLED` unset (the tool is slated for deletion after plan sign-off). | Open — user decision |
| F83 | Medium (UI) | T30 | Run summary renders raw JSON `{"output":"","exit_code":0}`; nothing tells the operator a re-run was "already processed / nothing to do". Confirm modal never names the period. | Open |
| F84 | Medium (RBAC) | T30 | Recompute-all / reset-purchase-data cards render on `RecomputeGuard::isPermitted()` alone with no permission check: `admin-compliance` / `admin-operations` would see both destructive buttons plus DB name and row counts. Not developer-only (`admin` and `admin-finance` hold `finance.record`). | Open |
| F85 | Low / Info | T30 | Events page rounds duration to seconds (557 ms → "1s"); 2 wallet entries with NULL `engine_run_id` (−₹2,169) are unattributed; `repurchase.snapshot` leaks a raw engine key (no registry entry). F47 context: `payout.monthly` is NOT web-triggerable ("Scheduler-only"), so the ungated default-to-current-month risk is console-only. Idempotent manual trigger verified: engine_runs id 49 (manual, actor 1, 557 ms), audit_log 3210, one `RunEngineChainJob` drained by the worker; `gsb_cutoff_results` 317→317, wallet ledger 40 rows / 12,540,984 paise unchanged. | Info |
| F86 | High (confirms F30, quantified) | T31 | 5 Sept pools frozen at 14:03: true day BV 8,09,400 vs frozen 5,59,400. Correct MSB payout ₹24,258 (39 pts × ₹622) vs actual ₹16,770 = ₹7,488 short. The same freeze cost September's GBB/Fortune/Rank monthly pools ~2,53,000 BV. Pages show "Computed 05 Sep 14:03" faithfully but never flag the premature freeze; only the GBB I&O report names the 3 ADNs who earned after the freeze and says nothing will pay them. Same fix as F10. | Open → F10 |
| F87 | Medium (UI honesty) | T31 | `/admin/compensation/gbb` and `/fortune-bonus` index pages say "engine has not yet run" while 2 frozen pool rows each exist; the index reads only `*MonthlyResult` rows with status=credited. | Open |
| F88 | Medium (units) | T31 | `gbb/{month}` and `fortune-bonus/{month}` print "Company BV ₹17,88,800.00" — BV rendered as rupees; GSB/MSB I&O CSV headers say "Day Total BV (Rs)". BV must never carry a currency sign. | Open |
| F89 | Medium (confirms F52, quantified) | T31 | "Pending payouts" tile ₹1,25,409.84 is the whole ledger; cash-payable is ₹1,21,234.36 and the other ₹4,175.48 is repurchase wallet. | Open → F52 |
| F90 | Medium (Left/Right labelling) | T31 | No GSB report names the weaker side Left or Right (house rule: every weaker/power/carry-forward figure is anchored to an explicit Left/Right label). See corrected F61. | Open |
| F91 | Low | T31 | MSB reports correctly carry no repurchase deduction (confirms F33) but never say so; `rank-bonus/2026-09` shows "Qualifiers 2" and "3 qualifiers" on one screen; tiles truncate ₹22,522.50 → ₹22,522; `carry-forwards` is the only report without a CSV export; daily-cutoffs help text says "leg" (house term "group"). | Open |
| F92 | Info | T31 | 77 checks; every money and BV figure on all 17 reports foots to the paise; all exports match page + SQL, ungrouped, PII limited to ADN + name (+ rank title). No mutations. Slowest page 182 ms. | Info |
| F93 | High (payout correctness / audit) | T32 | Hold reasons are frozen at batch creation and never re-evaluated: d1/d2/d3 received bank details + active KYC on 10 Sep (T01), yet batches 1 and 3 still show "No bank account", and the approved batch 3 records that falsehood in its audit trail. `approve()` does not re-gate; retry handles only `STATUS_FAILED` in Razorpay mode. Not money loss (held income rolls forward unswept) but the audit record is wrong and released distributors stay held until the next batch. | Open |
| F94 | High (separation of duties) | T32 | No maker-checker on payouts: `approve`, `reconcile` and `retry` all share `can:finance.record`; no `finance.approve` permission exists; `payout_batches` has no `created_by`, so self-approval is unrepresentable rather than blocked. (`admin-compliance` IS correctly excluded.) CLAUDE.md "separation of duties" principle not met for approve-vs-record. | Open — user decision |
| F95 | Medium (confirms F44, extended) | T32 | NEFT CSV columns: `Line#, ADN, Full Name, Bank Last 4, Net Amount (₹), UTR, Status` — no account number, no IFSC, so a bank cannot execute it. It also downloads 200 on an UNapproved batch (header-only, no warning) and carries no `can:` gate (all five admin roles can pull it). | Open — user decision |
| F96 | Medium (confirms F14/F15/F48) | T32 | Reconcile (NEFT import) validates only mime/size; the service reads ADN/UTR/Status/Reason — no amount check, no duplicate-UTR check (nothing submitted). Monthly batch creation never sets `earnings_through`; no earning-window column exists. | Open |
| F97 | Low (UI) | T32 | "Wallet balance" column stores `effectiveGross` (gross − repurchase), contradicting its tooltip; confirm modal said "₹0.00 to 0 distributor(s)" beside ₹79,890 of held income; monthly pages post to `weekly-payouts/*` routes. | Open |
| F98 | Info (RBAC) | T32 | Payout Settings is correctly 403 + zero-trace for `admin`, but the Feature flags and Plan settings nav links ARE visible to plain `admin` (developer-owned per the stealth-role rule). Verify in T34. **Resolved by T34 — not a defect:** only `registration.killswitch` is `owner: incident`; the other 16 flags are filtered server-side and 404 on toggle for admin; Plan settings GET is deliberately admin-readable with all writes `role:developer` and fields disabled via `$canEdit`; payout-settings is `role:developer` on GET too. | Closed |
| F99 | Info | T32 | Batch 3 approved once through the UI (all three preconditions verified: 3/3 lines held, gateway `manual_neft`, Razorpay disabled, `jobs` empty afterwards): status pending→approved, `approved_by 1`, `approved_at 2026-09-10 22:57:32`, audit_log 3218; second approve refused with "Batch cannot be approved in its current state." Admin-charge/TDS arithmetic could not be observed on a real line (no payable line on staging; held lines are 0/0, page shows "—" honestly); formula and order confirmed in `PayoutService.php:268-277`. Harness note: coordinate/ref clicks were inert this session; the agent drove element handlers directly. | Info |
| F100 | Medium (compliance copy) | T33 | Buyer's return form shows only the window per reason and never the buy-back deduction: no "less GST" for damage-non-saleable / general / termination buyback, no "shipping refunded only on cooling-off" (₹186.49 + ₹60.00 on order 17). Admin side renders the matrix correctly; only the buyer side omits it (DSR buy-back disclosure). | Open |
| F101 | Medium (admin UX) | T33 | No invoice link or "generate invoice" control anywhere in admin (order detail or payment detail); the `payments/orders/{order}/invoice` route is unreachable from any page. Order detail's PAYMENT panel does not link to its payment intent. | Open |
| F102 | Medium (returns) | T33 | Per-line returns unsupported: `OpenReturn` hardcodes `order_item_id`/`qty` to null, so order 17's 2 SKUs / 3 units could only be returned whole. | Open — user decision |
| F103 | Low (RBAC / labels) | T33 | BV ledger admin routes carry no `can:` gate while `/admin/analytics` is gated on `audit.read` for the same league-table reason (no practical exposure today — all five roles hold `audit.read`). BV Ledger headings say "NET BV" / "LIFETIME NET BV", never "personal BV". Separation of duties elsewhere correct. | Open |
| F104 | Info (staging data) | T33 | `invoices` rows 17/18 are stale orphans from `reset-purchase-data` (invoice ₹604.00 vs order ₹1,557.00, HSN 3401 vs order's 0902/3004). R-28 confirmed deferred (GSTINs NULL, intra-state supply booked as IGST). Stub gateway auto-settles refunds (`processed` 1 s later, `gateway_refund_id` NULL), so the unsettled-refund worklist and settle/retry cannot be exercised on staging. Non-cooling-off refunds release at Approve; receive/not-returned only for cooling-off returns. Fold the orphan invoices into F10 cleanup. | Info → F10 |
| F105 | Info (verified) | T33 | Return flow open → inspect → approve → auto-settle completed: return_requests id 1 `RMA-1SKZGBQL9R`, return_inspections id 1, refund_intents id 1 (₹1,497.00 processed), order 17 delivered→refunded, bv_ledger_entries id 20 = −179,900 p (d5 60,239,800→60,059,900), group_bv_reversals 2 rows on exactly the originally-credited upline (ancestors 1/L, 2/R, fully absorbed, debt 0), wallet_ledger_entries UNCHANGED (no cash entry — R-60 holds), ledger tx 19/20 balanced. Order 18 shipped→delivered (+cooling-off row, audit 3219). audit_log 3220–3228. | Info |
| F106 | High (PII / audit) | T34 | KYC document previews bypass the audited route: `show.blade.php` embeds a 30-minute presigned S3 URL in `<img src>`, so views that actually happen produce no `admin.kyc.document_viewed` row (1 audit row for 12 scans rendered) and the URL works with no session if copied. Hard rule 8 / DPDP access-logging gap. **Confirmed by me:** `resources/views/admin/kyc/show.blade.php:99` calls `$diskKyc->temporaryUrl($doc->object_storage_key, now()->addMinutes(30))`. | Open |
| F107 | Medium (PII retention) | T34 | KYC approval purge covers only `pan` + `aadhaar` document types; `aadhaar_back` (secure QR) and `cheque` (full bank account number) survive approval. R-31 claims images are purged. **Confirmed by me:** `ApproveKycSubmission::purgeIdNumbersAndFiles()` line 152 filters `whereIn('type', ['pan', 'aadhaar'])`; the migration enum also has `cheque`, `address_proof_*`, `photo`. R-31 wording ("purges the uploaded images") overstates the scope. | Open — user decision |
| F108 | Medium (audit integrity) | T34 | Audit chain mostly empty: 19 of 3,235 rows have `before_hash`, 1,126 have `row_hash`; `admin.kyc.approved` (3234) has NULL before/after digests. CLAUDE.md requires before/after hashes for every admin/KYC/settings action. | Open |
| F109 | Medium (help docs / stale content) | T34 | `/admin/help` "Franchise Programme" card → 404 (file deleted by 59ef9bf3) and the card still advertises the "3% fulfilment commission"; orphan `FranchiseFeature` flag row remains. All other help docs checked are in sync (compensation, payout-operations, admin-actions, distributor-requests, kyc-review-guide, payments). | Open |
| F110 | Medium (KYC workflow) | T34 | A flagged document is invisible in the KYC queue (the "awaiting re-upload" tile counts `status='rejected'`, not `flagged_at`), and Approve stays enabled while a flag is unresolved. | Open |
| F111 | Low (assorted) | T34 | Approval notification is `['mail']` only while the flag path is `['mail','database']`; `/admin/distributors` search ignores phone while `/admin/tree/search` matches it; distributor detail has no KYC-state row and no orders/BV summary; audit log has no actor filter; root shown as its own sponsor; `/admin/tree` is 3.6 MB / 926 ms for 316 nodes; staging SMTP rejecting (250 empty) while the flash still says "has been emailed". | Open |
| F112 | Low (security hygiene) | T34 | Stale per-user Pennant rows: `HibpPasswordCheck User 1 = false`, so admin@arovolife.test is exempt from the HIBP check while the console shows the flag ON. Mutations: approved users.id 34 / distributor 33 (ADN 678721891); flagged kyc_documents.id 16 (distributor 34); one impersonation of user 2; audit 3231–3235 (+2 `admin.register.exported`). `distributor_requests` empty (0 rows) — re-run after F74. | Open |
| F113 | High (hard rule 3 guard) | T35 | `NoIncomeProjection` is a lowercase `str_contains` over a fixed phrase list (`app/Modules/Shared/Rules/NoIncomeProjection.php:86-89`, confirmed by me). "You can earn ₹50,000 per month guaranteed with arovolife." was accepted and persisted as content_pages 9; positive control "guaranteed income every month" was refused, so the wiring works and the phrase list is the gap. Needs pattern-based matching (e.g. `earn … per (day or month)`, currency amount + period). | Open |
| F114 | High (RBAC / seeding) | T35 | `messaging.moderate` does not exist in staging's `permissions` table (12 rows; the only seeder permission missing). `admin-operations` and `admin-compliance` are locked out of `/admin/messaging/reports`; it opens only via the super-staff `Gate::before` bypass, so R-17 holds by accident. Fix: re-run the additive `RolesAndPermissionsSeeder` on staging (audited additive; still confirm before running). **Confirmed by me:** staging `permissions` has 12 rows, the seeder defines 13; `messaging.moderate` (seeder line 86 → admin-operations, admin-compliance) is absent. Also observed: the `admin` role holds finance.record + grievance.handle + compliance.discipline together, so separation of duties is only real for the admin-finance / admin-compliance / admin-operations roles. | Open — user decision |
| F115 | Medium (product rule) | T35 | Publish-one-at-a-time is NOT implemented for announcements: id 2 published while id 1 was live, no refusal, no auto-archive, both listed "New" on `/announcements`. `AdminAnnouncementController::transition()` has only the `NoIncomeProjection` re-scan and `canPinAnother()` (pin limit `announcements.pin_limit`, default 3). Product decision needed. | Open — user decision |
| F116 | Medium (moderation UX) | T35 | A message report shows only `full_name` for reporter and sender; both read "Arovolife Private Limited" on staging, so the moderator cannot tell the parties apart. No ADN, no link to either profile. | Open |
| F117 | Medium (robustness) | T35 | Blank `sort_order` on content create → HTTP 500 (`Column 'sort_order' cannot be null`) instead of a validation error. Trix JS+CSS load from unpkg at render with no local fallback; when unreachable the Body field silently degrades to a dead box (no console error), risking a blanked body on save. | Open |
| F118 | Low / Info | T35 | Publishing a content page audit-logs as `content_page.updated`, indistinguishable from a typo fix. Mutations: content_pages 9 published → archived; announcements 1 and 2 created → published → archived; announcement_reads 1; message_reports 1 → reviewed (reviewer 1, 08:00:26 IST 2026-09-11); audit_log 3240–3250. All three content/messaging flags read ON. | Info |
| F119 | Critical (wrong numbers) | T36 | `/admin/analytics` BV figures are under-reported 100×: `resources/views/admin/analytics/index.blade.php` lines 50 and 202 pass `bv_paise / 100` to `@bv(...)`, and `Bv::format()` divides by 100 again (`Bv.php:18-26`, confirmed by me). "BV Generated" shows 20,399 BV; true value 2,039,999 BV; the "Highest volume" table is wrong the same way for all 7 rows. Page-specific — Arete Centres page uses the helper correctly. Side note: `Bv::format` uses `Number::format`, not `IndianNumber` (lakh grouping rule). | Open |
| F120 | Critical (compensation / compliance) | T36 | The company-default Arete Centre (id 1, `centre_type='company'`, documented as ownerless in UI copy and code comments) has `assigned_distributor_id = 33` on staging, and the ADC Bonus engine gates only on `assigned_distributor_id !== null`, not `centre_type`. Result: `adc_bonus_results` id 1 credited distributor 33 (ADN 678721891) ₹10,500 net for September 2026 from the company centre's 3,50,000 BV (credited 2026-09-05 14:03:29; wallet_ledger_entries id 23). Confirmed by me in DB. Needs a compliance-officer decision: either the engine must exclude `company` centres, or the assignment is intentional and the copy is wrong. | Open — user decision |
| F121 | Medium (observability) | T36 | `grievance:sla-sweep` is not in `EngineRegistry`, so it never writes `engine_runs`, and it logs nothing on no-op runs — its hourly execution cannot be verified from standard evidence. Neither open ticket is near breach. Mutations: ticket 2 assign → respond → third-party → status-update → resolve → close (all confirm modals, "QA test — please ignore"); ticket 1 escalated 1→2 and third-party toggled, left open; `ticket_events` and `audit_log` rows verified. Everything else on analytics/dormancy/grievance report/Arete centres footed to SQL exactly. | Open |
| F122 | High (compliance report) | T42 | `/admin/grievances/report/export` throws HTTP 500: `Csv::safe()` is typed int, string or null (`app/Modules/Shared/Support/Csv.php:20`) and `AdminGrievanceReportController.php:78` passes a float (the median/average time column once a ticket is resolved with a fractional value). Two 500s at 2026-09-11 08:30:02 and 08:30:24 IST after ticket 2 was closed by T36; the same export passed earlier in T36 when no fractional value existed. Confirmed by me in code and laravel.log. This is the Compliance Committee / regulator export. | Open — fix before launch |
| F123 | Info (verified) | T42 | QA window 2026-09-10 12:00 → 2026-09-11 08:30 IST: 8 ERRORs, all explained (T01 probe ×3, T13 abort, T25 ClamAV, SMTP 250-empty ×2, T35 sort_order); 3,517 WARNINGs are one pre-existing message (`LOG_SLACK_WEBHOOK_URL is empty` — silence it); 0 PII hits in logs; failed_jobs unchanged (11 pre-existing, ids 228–238); jobs 0; 5 workers alive. Overnight scheduler ran unattended: engine_runs 50 (`repurchase.evaluate` 00:05:04) and 51 (`gsb.daily-cutoff` 00:10:03) succeeded; the 2026-09-10 GSB pool `company_bv_paise = 59,900` = 239,800 accrual − 179,900 T33 return reversal, so the reversal was priced correctly; the 08:00 engine-health digest ran with `failures=2` (runs 37 and 43) as designed (answers F04). Crontab of the SSH user points at a different Cloudways app — ahdhesuhty's cron entry not readable; indirect proof only. | Info |
