# Compliance Risk Register

Standing risks re-assessed at every phase exit gate. Critical / High
risks must be mitigated or formally accepted (dated, signed) before a
phase can close.

| ID | Risk | Nature | Source | Mitigation | Phase owner | Status |
|---|---|---|---|---|---|---|
| R-01 | Pyramid / money-circulation interpretation | Statutory — Critical | DSR 2021 §2(1)(j); T&C Definition "Pyramid Scheme" | Commissions only on product sales; every commission row has `product_sale_id NOT NULL` | 4 | Pending |
| R-02 | Mis-selling & income guarantees | Statutory — High | DSR 5(1)(d); T&C §6 | No earnings calculator on public site; orientation required; all marketing copy reviewed | All | In-design |
| R-03 | Forced purchase at joining | Statutory — High | T&C §4 | Registration cannot add SKUs; unit test asserts | 1 | In-progress |
| R-04 | Failure of cooling-off (30 days) | Statutory — High | T&C §4, Code V | `cooling_off_end_at` stamp; one-click cancel; reminders at D-7 / D-1 | 1 + 8 | In-progress |
| R-05 | Buyback / return not honoured | Statutory — Medium | T&C §8 | Workflow enforces saleable/non-saleable split and GST deduction rules | 8 | Scheduled |
| R-06 | Duplicate PAN / duplicate ADN | Statutory — High | T&C §1.4, §7 | Unique index on PAN hash; couple rule engine; duplicate detection cron | 1 | In-progress |
| R-07 | KYC data breach (PAN, Aadhaar) | Data protection — Critical | DPDP 2023; T&C §15 | AES-256 at rest; last-4 display; audit log; purpose-limited access | 1 | In-progress |
| R-08 | Third-party resale / e-commerce listing | Contractual — High | T&C §9 | Terms acceptance; marketplace crawler; takedown workflow | 2 + 8 | Scheduled |
| R-09 | Minor as distributor | Statutory — Critical | T&C §1.1 | DOB validation with state threshold; PAN cross-verification | 1 | In-progress |
| R-10 | Auto-termination of inactive DS (>1 y) | Contractual — Medium | T&C §21 | **Partially mitigated (2026-08-16)** — `distributors:inactivity-sweep` runs daily at 09:30 IST in three passes: clear notices on anyone who has sold since the notice was issued (first, so a distributor who sold yesterday is never closed today by a sweep that had not caught up), terminate accounts whose seven-day notice expired, then notice anyone newly past twelve months. The clock runs from the effective date or the last sale, whichever is later; cancelled and refunded orders do not count as sales. Notice and termination are **two stages, never one** — §21 promises notice before termination, so a direct closure would breach the agreement even reaching the same end state. Windows are settings-driven (`termination.*`) so a change is an audited edit reconcilable with a §16.2 amendment notice. **The master switch `termination.inactivity_sweep_enabled` ships OFF**: the sweep reports and writes nothing until a human has reviewed the first cohort at `/admin/dormancy`, because there is no path back from `terminated` and the first automated run is the only chance to catch a sales-attribution bug before it closes distributors who were in fact selling. Staff can withdraw a notice with an audit-logged reason for exactly that case. Residual: see R-43 (re-registration path unbuilt) and R-44 (rank-name mapping is an interpretation) | 1 | Partially mitigated (2026-08-16) |
| R-11 | Grievance redressal SLA breach | Statutory — Medium | T&C §11; DSR 2021 Rule 12 | **Partially mitigated (2026-08-16)** — the Grievance module shipped: `/p/grievance/form` public intake, in-app intake at Dashboard → Help, staff intake for the phone/post/email/walk-in channels recorded against their **receipt** date, unique complaint number (`GRV-YYMMDD-XXXXX`), the three published clocks (48h acknowledgement / 5 working days first response / 30 days resolution, 60 with a one-shot third-party extension and a 15-day progress-update duty), a four-step escalation ladder with auto-escalation, and an hourly `grievance:sla-sweep` that **stamps a breach the moment a clock lapses** so the monthly compliance report counts recorded facts rather than recomputing history against today's settings. Confidentiality controls: attachments on a private S3 bucket with randomised object keys, every read audit-logged, officer alerts carry no complainant text at all, `grievance.handle` excludes `admin-finance`, Ethics/Privacy tickets are visible only to the compliance side, and nobody can open a grievance filed by their own distributor account. Residual: see R-39 (email intake is manual), R-40 (anti-retaliation unbuilt), R-41 (retention purge unscheduled), R-42 (order-level SLA rows not modelled) | 1 | Partially mitigated (2026-08-16) |
| R-12 | Placement Strategy flipped mid-flow | Operational — High | ADR-0002 (superseded) | n/a — ADR-0003 removes the strategy concept entirely; no `placement_strategy_snapshot` column, no resolver; placement is a single-level invariant rule | 1 | Mitigated (2026-05-01) |
| R-13 | Placement_id outside sponsor's downline | Operational — High | ADR-0003 | `PlacementEngine::isSelfOrDescendant()` (`app/Modules/Genealogy/Services/PlacementEngine.php:147`); cross-line attempts raise `CrossLinePlacementError`, dispatch `ForbiddenPlacementAttempted`, write `genealogy.placement.rejected` audit row; `RegistrationWizardController::start()` re-validates at link-open time and surfaces failures as a generic Contact Us redirect | 1 | Mitigated (2026-05-01) |
| R-14 | Contact-form PII on a plaintext queue payload | Data protection — Medium | DPDP 2023 §6, §8(3) | `NewContactInquiryNotification` is constructed with the inquiry id only; PII is re-fetched at delivery time so the `jobs` table never carries name/email/phone/address/message | 1 | Mitigated (2026-05-01) |
| R-15 | Contact-inquiry retention not bounded | Data protection — Medium | DPDP 2023 §8(3) | `contact-inquiries:purge` artisan command (`app/Modules/Public/Console/PurgeStaleContactInquiriesCommand.php`) deletes unhandled inquiries >90d and handled inquiries >365d; scheduled daily at 03:00 IST via `PublicServiceProvider::boot()`; each run records counts (no PII) in `audit_log`; covered by 6 Pest tests (PURGE-01..06) | 1 | Mitigated (2026-05-01) |
| R-16 | Line-change approval residual TOCTOU on requester leaf status | Operational — Low | ADR-0003 | Line-change approval re-checks the requester's leaf status under a row lock, but a concurrent placement landing under the requester is not fully serialized by the target-parent advisory lock (`app/app/Modules/Genealogy/Services/ApproveLineChange.php`). Accepted for Phase 1 (low placement volume); revisit if placement volume rises. **Resolved (2026-06-17):** `ApproveLineChange` now holds the advisory lock on BOTH the requester and the target parent (ascending-id order, deadlock-safe), fully serialising any concurrent placement landing under the requester against the leaf re-check | 1 | Resolved (2026-06-17) |
| R-17 | Any admin may approve/reject a line-change (binary-placement move) | Operational — Low | T&C §3; separation-of-duties principle | Any user with the `admin` role may approve/reject line-change requests; the admin route group in `app/routes/web.php` is not scoped to a dedicated `admin-operations` role. A dedicated `admin-operations` role is deferred to a later phase. Accepted for Phase 1 (small admin team). **Resolved (2026-06-18):** separation-of-duties roles shipped — `admin` super-admin (Gate::before bypass) + scoped `admin-operations` / `admin-finance` / `admin-compliance`; line-change decisions gated `can:placement.decide`, COD/finance `can:finance.record` (closes R-20 too), block/terminate `can:compliance.discipline`. Tests: AdminRoleSeparationTest | 1 | Resolved (2026-06-18) |
| R-18 | `coupon_redemptions.customer_id` retention window not bounded | Data protection — Medium | DPDP 2023 §8(3) | The coupon-redemption ↔ customer FK has no defined retention/purge. It is required during the order's life for per-customer usage caps + tax audit; schedule a `coupon-redemptions:purge` command (or null `customer_id` after the order's return window closes) before Phase 2 closes | 2 | Open (2026-05-31) |
| R-19 | `customer_addresses` retention window not bounded | Data protection — Medium | DPDP 2023 §8(3) | Shipping + billing addresses are saved "on file" for the customer at checkout (Epic 4) with no defined retention/purge. The "My Addresses" self-service book (saved-address feature, 2026-06-07) now lets a data principal view/edit/**delete** their own saved shipping addresses — a partial DPDP correction/erasure mitigation — but the gap remains Open: no scheduled purge, and the `customer_addresses.customer_id` `cascadeOnDelete` FK is never fired because there is no account-closure/Customer-deletion flow. Schedule a purge or anonymise-on-account-closure policy alongside R-18 before Phase 2 closes | 2 | Open (2026-05-31) |
| R-20 | COD cash-collection (`markCodPaid`) postable by any admin | Operational — Low | T&C §3; separation-of-duties (see R-17) | The Epic-4 `AdminOrderController::markCodPaid` posts a cash-in ledger entry but is gated only by the `admin` role. Like R-17, it should move to a dedicated `admin-finance` role before order volume grows. **Resolved (2026-06-18):** `markCodPaid` is now gated `can:finance.record` (held by `admin-finance` + super-admin) — see R-17 | 2 | Resolved (2026-06-18) |
| R-21 | Franchise modelled as an offline retail outlet | Statutory — Critical | DSR 2021 Rule 4; T&C §5.2(2); Code of Ethics §2.9; Hard Rule 7 | **Withdrawn 2026-09-17 (client decision): the franchise programme is dropped in full and the Arete Development Centre replaces it.** The risk was that an independent franchisee-owned "billing centre" taking walk-in product sales is offline retail, which our own T&C forbids. An ADC is not that: the owner must accept a versioned declaration that the centre is **for training and support only, never a retail store** (`arete_center_application_declarations`), the centre holds no stock and bills nothing, and the only sale it touches is an online order whose buyer chose it as a collection point. No franchise ADR, ERD or migration was ever written and no franchise was ever created. **Closed conditionally, and the condition is not yet met.** That declaration is the evidence for this closure, and the shipped checkout flow currently falsifies it: the buyer is told the order will be held at the centre for collection, and the centre's postal address is written into the order's `ship_*` fields — the centre acting as an e-commerce fulfilment point, which the declaration forbids in terms. This row reopens unless R-47 is remedied: wire the fulfilment, withdraw the collection option, or reword the declaration under a new version. What else survived the franchise design is tracked as R-22, R-23 and R-24. **REOPENED 2026-09-18 — the closure rested on a permission the declaration does not contain.** Compliance review of the actual text settled the contradiction between this row and R-47 in R-47's favour. `AreteCenterDeclarations.php:23` (VERSION `v2`) reads: *“I will use the centre **only** for training, product demonstration and distributor support — **not as a retail store, outlet or e-commerce fulfilment point**”*. Two limbs, and a collection point fails both: the positive list is exhaustive and receiving a parcel for a buyer — who may be a guest consumer with no ADN — is none of the three things on it; and “e-commerce fulfilment point” is the express wording, not a paraphrase. The sentence above describing what the system does was written as though it were what the declaration permits; there is no collection carve-out, no “except”, no “save that”. **Hard rule 7 itself is not breached** — the operative text is DSA §5.1/§5.2, not §9, and §5.2(2) bites on “stock … for over-the-counter resale”, which a sealed consignment for one identified, already-paid buyer is not. What is breached is the declaration, which is this company's stated evidence that hard rule 7 is observed. Breaching the evidence does not breach the rule; it destroys the defence. **Closing this now needs three things, not one:** a v3 declaration permitting supervised collection (retaining no-stock / no-price / no-bill / no-payment / no-other-channel); every centre that receives a consignment having accepted v3, recorded **per centre**; and that re-acceptance enforced as a hard gate on the first consignment, not a scheduled task. A version bump alone re-papers nobody — see R-95. Plan: `docs/plans/2026-09-18-order-fulfilment.md`. **Two of the three conditions are now mechanised** (095eee97): declarations are recorded per centre, and no centre can receive a consignment without having accepted every declaration at the version in force. The third — the v3 wording itself — is deliberately NOT done in code: rewording a contract existing centres have signed is a client decision, and `AreteCenterDeclarations::VERSION` therefore still reads `v2`. Drafted v3 text is in the plan for approval. Because the gate reads the constant, bumping it to v3 will immediately block every existing centre until each re-accepts — that is the intended behaviour and must not be mistaken for a regression. **Closed 2026-09-18 — all three conditions this row named are now met.** The client approved the v3 wording, so `AreteCenterDeclarations::VERSION` is `v3` in code. (i) `training_use_only` now permits *supervised collection of orders a buyer has already placed and paid for on arovolife's own platform*, while prohibiting the things that would actually make a centre a shop — holding, displaying, stocking or offering product for sale; quoting a price, raising a bill or accepting payment; and accepting or fulfilling an order from any other channel — plus release only to the named buyer against the collection code, same-day recording of every handover, and return of anything uncollected beyond the published period. A sixth declaration `buyer_data_duty` binds the operator on the buyer's personal data. The citation is corrected from DSA §9 (PII Handling) to §5.1 and §5.2, which is where the prohibition actually lives. **v3 does not contradict the DSA it cites:** §5.2(2) bites on stocking *"for over-the-counter resale"*, which a sealed consignment for one identified, already-paid buyer is not, and §5.2(1) bites on platforms that accept orders *"without the customer being attributed to a specific ADN at the moment of purchase"*, which arovolife's own platform is not. No DSA amendment is required. (ii) Acceptance is recorded per centre in `arete_center_declarations`, and (iii) `DispatchService` refuses to consign to any centre that has not accepted every declaration at the version in force. The superseded v2 text is retained in `AreteCenterDeclarations::SUPERSEDED` — an acceptance row recording `version = 'v2'` is evidence of nothing if the v2 wording has been deleted, and this is the first time the version has ever moved. **Consequence, intended:** every centre that accepted v2 is now blocked from receiving a consignment until it accepts v3. On dev all three centres went to *Declarations pending* the moment the constant changed. That is the gate working, not a regression. The text has not been seen by counsel; it is a contract change and is bundled into the pre-launch legal review with the rest of `database/seeders/content/`. **Correction — that closure was written before review and was premature.** Compliance review the same day returned FAIL on two forgery paths, both of which meant the gate was reading evidence the operator need not have produced. Neither went *through* the two-entry-point split in `AreteCenterDeclarationService`; both went around it. **(a) Impersonation.** `AdminImpersonationController::start()` is a full session swap (`Auth::login($target)`) with no read-only mode and nothing blocking state-changing POSTs, so a super-staff admin could wear a centre owner's session and accept. The row would read `signed_by = owner` with the distributor's own user id on it — indistinguishable from a genuine acceptance except by correlating timestamps against `admin.impersonate.start`. **(b) Reassignment.** Declarations key on `center_id` alone, and `arete-centres.update` carried no permission beyond the outer admin role group, so a centre transferred from A to B carried A's signature forward — and staff could accept for a company centre, then have an admin assign a distributor to it, which is admin-on-behalf acceptance in two steps. **Both fixed the same day.** Acceptance is refused while `impersonator_id` is in session; a centre's acceptances are **superseded** (never deleted — "A signed this on this date" stays true, it is simply not B's signature) whenever `assigned_distributor_id` changes, via a new `superseded_at` column the gate filters on; and reassignment now requires `compliance.discipline`, checked on the change rather than on the route so operations staff can still correct an address. Three regression tests cover the impersonation refusal, the supersede-on-transfer, and the two-step route specifically. **The lesson worth keeping:** the first closure was verified in the browser by exercising the *accept* route and finding no admin path. That tested the door and not the wall — the edit route was the wall | 7 | **Closed (2026-09-18, after remediation of two forgery paths found in review)** |
| R-22 | ADC bonus is computed from an aggregate BV figure with no per-sale `product_sale_id` chain | Statutory — High | DSR 2021 Rule 5(1)(c); Hard Rule 2 | **Re-scoped from the dropped franchise programme 2026-09-17 — the risk moved to the ADC rather than going away, and the replacement is weaker than the design it replaced.** `adc_bonus_results` stores `order_count` and `total_attributed_bv_paise`: a per-centre, per-month **aggregate** (`2026_06_25_400003` + `2026_09_01_200001`). The franchise design at least carried a `franchise_commission_result_orders` join table naming the individual sales a commission was paid against; the ADC engine has no equivalent table, so at audit the monthly bonus is a percentage of a number with no stored evidence chain back to the sales that produced it — the engine re-derives it by querying `orders.arete_center_id` at run time, which is not the same as having frozen what it paid against. Required: an `adc_bonus_result_orders` row per contributing order (FK to the order, its BV and its `product_sale_id`), written by the run that freezes the result and surfaced on the ADC Monthly Calculation report. Worse than a bare aggregate: the two frozen figures do not even describe the same set of orders — the BV sum filters on `bv_ledger_entries.effective_at` while `order_count` filters on `orders.placed_at` (`AreteDevelopmentCenterBonusService` ~:84-92). Recruitment-originated BV must be excluded by construction | 4 | Open (2026-06-01, re-scoped 2026-09-17) |
| R-23 | ADC cooling-off clawback gap — a sale refunded in a later month is never clawed back | Statutory — High | T&C §4.2(4); ADR-0005; Hard Rule 5 | **Re-scoped from the dropped franchise programme 2026-09-17.** **Partially mitigated (2026-08-07, `feature/adc-center-address`):** the ADC engine's monthly base is the signed net attributed BV — same-month refunds are deducted before the rate is applied, and a month netting ≤ 0 is skipped (`AreteDevelopmentCenterBonusService::runForMonth()`). Residual gap, unchanged and live: BV reversals carry `effective_at` = the refund date, so a sale refunded in a **later** month does not claw back the month it was already paid on, and a net-negative month is discarded rather than carried forward. A buyer who orders on the 28th and cancels on the 5th — squarely inside the statutory 30-day window — leaves the centre paid on a sale that no longer exists. The per-order `pending`-until-cooling-off-expiry hold this risk was raised for is still unbuilt; the cheaper fix is to carry a net-negative month forward as an opening debit instead of discarding it | 4 | Open (2026-06-01, re-scoped 2026-09-17) |
| R-24 | Combined Genos + ADC payout pyramid-scheme surface | Statutory — High | DSR 2021 §2(1)(j); see R-01 | **Re-scoped from the dropped franchise programme 2026-09-17.** An ADC owner is a distributor holding an ADN and occupying a Genos position, so the same sale can pay them GSB, Mentorship, GBB, Rank and Fortune *and* the ADC bonus as its collection point. That overlap is exactly what the franchise version of this risk described; dropping the franchise did not remove it, it made it the default case rather than the edge case. ADC also sits **outside** the ₹50 lakh combined monthly ceiling (it carries its own ₹1,00,000 cap), so nothing bounds the combined figure today. **And one distributor may own unlimited centres:** `arete_centers.assigned_distributor_id` carries a non-unique index and no validation caps the count, while each centre earns its own ₹1,00,000/month — so per-distributor ADC income is bounded only by how many centres they hold. The ₹1L figure bounds a *centre*, never a person. Legal counsel must sign off either on a combined-payout ceiling, or on the position that the ADC bonus is consideration for distinct fulfilment work — which is the position R-47 has to make true before it can be argued. Before the flag is enabled anywhere real: add a uniqueness check or a per-distributor ADC ceiling. **Note 2026-09-18 — a handover record alone does not repair this.** The fulfilment plan writes a centre-acknowledged handover, but `AreteDevelopmentCenterBonusService.php:83-93` sums BV joined on `orders.arete_center_id` and counts orders by `placed_at`; nothing reads a shipment. As designed, a centre would still be paid 3% on an order never consigned to it, never acknowledged and never collected — consideration paid for work the system now records as not done. The requirement is explicit: gate the monthly BV sum and order count on `collected_at IS NOT NULL` for that centre. With that and R-22's missing evidence chain, this moves from “the position cannot be argued” to “the position can be argued” — which is the most fulfilment can achieve here. The combined-payout ceiling and the per-distributor centre cap remain separate and unbuilt **The `collected_at` gate landed 2026-09-18** (095eee97): `AreteDevelopmentCenterBonusService` now counts only orders with a recorded handover at that centre, so the consideration argument is at least available. It is not yet won — R-22's evidence chain is still missing, and the combined-payout ceiling and per-distributor centre cap remain unbuilt | Compliance | Open (2026-06-01, re-scoped 2026-09-17, requirement sharpened 2026-09-18, gate landed 2026-09-18) |
| R-25 | Franchise code identity collision with ADN | Statutory — High | Hard Rule 6; T&C §3.4 | **Withdrawn 2026-09-17 (client decision): the franchise programme is dropped and the Arete Development Centre replaces it.** The risk was a franchise code with its own login, BV attribution and payout becoming a functional second identity. An ADC is not one: it has no login, it is never placed in the Genos and earns no binary-tree bonus, and it is owned by an ADN — transferred by changing the owner ADN on the centre's Edit form, audit-logged. One PAN still maps to one ADN; the centre is a premises record hanging off it | 2 | Closed (2026-09-17) |
| R-26 | Binary spillover enabled in production without PO + Compliance compensation sign-off | Operational — Medium | ADR-0007; DSR 2021 Rule 5; Hard Rules 2 & 3 | Spillover (`placement.spillover.enabled`, and the `placement.spillover.strategy` fill-algorithm selector — `breadth_balanced`/`depth_outer`/`weaker_leg`) changes downline composition and therefore future (Phase 4+) commission distribution. Controls: setting defaults **OFF** (ADR-0007) — the engine and `RegistrationWizardController::start()` both gate on it, so OFF = ADR-0003 single-level behaviour byte-for-byte; the admin Settings description mandates PO + Compliance sign-off before prod enablement; every toggle flip writes a settings-update `audit_log` entry (detective control); placement is structural/pre-revenue so no BV/commission is created by the move itself. Do NOT switch the toggle on in production until the compensation-distribution effect is signed off; revisit at Phase 4 (compensation engine) kickoff | 1 | Open (2026-06-06) |
| R-27 | "Find My ID" (name+PAN → ADN) targeted disclosure / ADN harvesting | Data protection — Medium | DPDP 2023 §5-6, §8(3); Hard Rule 8 | Public `/find-my-id` returns a distributor's ADN on an exact registered-name **AND** PAN match (`FindMyIdController`). Controls: PAN matched as one-way `sha256` (same scheme as registration), never stored/displayed/logged; dual exact match (neither field alone reveals anything); two-tier per-IP rate limit (5/hour + 15/day) counting every attempt, plus route `throttle:8,10`; generic "no match" message (no field-level leak); terminated/frozen/rejected accounts excluded (`users.status IN active,pending`); DPDP consent checkbox + Privacy Policy link required; every attempt (success/miss/throttled) audit-logged with IP + distributor id only (no PAN). Residual: a **targeted** attacker who already holds a specific person's exact name + PAN can confirm arovolife membership and obtain that ADN in one attempt (rate limit cannot stop a single correct guess). Accepted for the ADN-recovery use case; if abuse appears in the audit trail, harden to email/SMS-OTP delivery of the ADN (deliver to the on-file channel instead of on-screen) — noted as the fast-follow mitigation | 1 | Accepted (2026-06-08) |
| R-28 | Customer order document is now a GST tax invoice | Statutory — Medium | CGST Rule 46; CGST Act §15(3)(a); Hard Rule 7 | **Mitigated (2026-08-17).** The printable order document now renders the canonical Tax `Invoice` record rather than recomputing from `Order`, so the document and the tax record can never disagree. Four defects fixed: **(i) the supplier GSTIN was never written** — an invoice without it is not a tax invoice and no buyer can claim input credit (Rule 46(b)); it now comes from `tax.seller_gstin`, and **while that setting is blank the document is honestly labelled an order receipt** rather than presenting itself as a tax invoice. **(ii) The recipient GSTIN was never captured** (Rule 46(e)); an optional, format-validated GSTIN + registered-name pair is collected at checkout and carried onto the invoice. **(iii) The invoice number was `timestamp % 1000000`** — neither consecutive nor collision-free, so two invoices raised 1,000,000 seconds apart carried one identity; replaced by `InvoiceNumberSequence`, a per-financial-year counter incremented under a row lock inside the invoice transaction, formatted `AL/2026-27/000001`, with the Indian April–March year boundary. **(iv) Tax was computed on the pre-discount subtotal**, so an order with a coupon or redeemed points reported more GST than the buyer was charged and the invoice did not foot; under §15(3)(a) a discount shown on the invoice is excluded from the transaction value, so the reduction is now apportioned across the lines by gross (last line absorbing the rounding remainder, so it sums exactly) and the tax recomputed from what was actually charged. **(v) The CGST/SGST versus IGST split was wrong on every intra-state supply** — this entry previously recorded that split as "already correct and unchanged", which is a fair part of why nobody looked at it again. `InvoiceGenerator` compared `tax.seller_state` (a two-letter code by design — the setting caps at two characters) against `orders.ship_state` / `arete_centers.state` (a display name), so `'TG'` never equalled `'Telangana'`; every supply the company made from its own state was classified inter-state and billed IGST where CGST+SGST was due. Remitting to the wrong head is recoverable for the supplier (CGST §77(1) / IGST §19(1) refund it, and §77(2)/§19(2) charge no interest) but not cheaply for the buyer — a B2B recipient must reverse the IGST input credit with §50 interest, and the split is frozen onto the invoice row where no later render corrects it. Both sides now normalise through `IndianStates::canonical()` (which resolves a code, a display name or a mis-cased variant) before the comparison; a state that cannot be resolved is refused outright rather than billed to a guessed head (see the 2026-09-18 update below). Found 2026-09-18 on staging, where 13 issued documents were all IGST with supplier and recipient both in Telangana. The fixtures hid it: the tax test built its order with `ship_state => 'TG'` and the collection tests set `tax.seller_state` to `'TELANGANA'` — a value the settings registry forbids at `max: 2` — so the whole tax-head suite described a configuration the application cannot produce. **Both follow-ups closed 2026-09-18, and the closure opened one narrower item.** (a) **Every state input is now a picked vocabulary, not free text.** `orders.ship_state`, `orders.bill_state`, `customer_addresses.state`, and the inventory warehouse and supplier states are `<select>` elements over `IndianStates::all()` with a matching `Rule::in()` server-side, so markup and validator cannot drift; the distributor edit form — the one remaining ungated input, and an oversight against its own create path — is gated against the same code map the create path uses. (b) **An unreadable state is now refused, not guessed** (`UnresolvablePlaceOfSupplyException`): there is no "unknown" head of tax under IGST §10(1)(a), so a guess is wrong roughly half the time and writes its error onto a serial nothing re-renders. The sale is unaffected — generation runs after the payment commits and the existing `Log::critical` → `InvoiceGapWorklist` → non-silenceable CRITICAL action-centre chain raises it. The same change closed two exposures the refusal itself created: **`tax.seller_state` was an unvalidated 2-character string**, so typing "Telangana" into a developer-owned field stored `Te` and would have halted invoicing for *every* order in the system — it and `tax.seller_state_code` are now enums over `IndianStates::codes()` / `gstCodes()`, which cannot be typo'd or cleared; and **a collection order whose centre carried a null state silently billed CGST+SGST**, which the old `?? sellerState` fallback presented as "the collection case" and was not — `arete_centers.state` is now `required` rather than `nullable`, and a centre with no state is refused by name. **Risk accepted, dated 2026-09-18:** there is still no admin screen to correct `orders.ship_state` on an existing order, so an order whose delivery state cannot be resolved is un-invoiceable through the platform until engineering intervenes. Accepted because the population is empty and bounded: staging carries zero un-invoiced orders and every stored state on staging and dev resolves; the column is written once at checkout, where it is now `required` and picked from a list, so a new unreadable value cannot be created through the UI, and only a legacy row could hold one. The exception message no longer instructs an operator to perform a correction the UI cannot do — it names the editor where one exists (settings, centre) and says plainly that the delivery state has none. **An admin "correct place of supply" action, audit-logged with before/after, remains the open follow-up.** Supplier identity is settings-driven (`tax.seller_gstin`, `tax.seller_legal_name`, `tax.seller_state`, `tax.seller_state_code`, `tax.seller_address`), developer-owned. The invoice shows the HSN-wise taxable value, rate and tax under each head, place of supply, the reverse-charge line and the truth declaration. 15 invoice tests, 10 on the state normaliser, and one pinning the supply-from setting as a vocabulary that free text cannot enter. **Pre-launch: the GSTIN setting is blank and must be filled with the real registration — until it is, every order document is a receipt.** Reviewed by the compliance officer 2026-09-18 twice: the (v) split fix — PASS with the two follow-ups above — and the follow-up closure, which returned three High findings (the unvalidated `tax.seller_state`, the null centre state, and the missing correction screen). The first two are fixed above; the third is the dated risk acceptance above. Items (i)-(iv) are still unreviewed | 2 | Mitigated (2026-09-18) |
| R-29 | Customer email_enc/phone_enc still APP_KEY-bound (not on dedicated PII key) | Data protection — Medium | DPDP 2023 §8(7); Hard Rule 8; ADR-0008 Follow-up 2 | Distributor PAN/Aadhaar/bank moved onto the dedicated `PII_ENCRYPTION_KEY` (ADR-0008, `PiiEncrypted` cast + `PiiCrypter`), but `Customer.email_enc`/`phone_enc` remain bound to the routinely-rotated APP_KEY and have no `PiiEncrypted` cast. No regression (unchanged by this change), but APP_KEY rotation could 500 customer reads as it did for distributors. Adopt `PiiEncrypted` on Customer + extend `pii:reencrypt` to cover those columns before Phase 2 commerce depends on them | 2 | Open (2026-06-08) |
| R-30 | Sharing distributor's name + ADN disclosed to a shared-cart ("Easy Purchase") guest | Data protection — Medium | DPDP 2023 (minimisation); T&C §3; Hard Rule 8 | A guest who opens a distributor's shared cart link is shown the sharing distributor's **ADN + registered full name** (read-only) at checkout so they know who they're buying through (`CheckoutController::resolveBuyerDistributor`, `referral` mode). Controls: only ADN + name are shown — distributor email/phone are deliberately withheld (`SCT-06` asserts `assertDontSee(email)`); no BV/earnings shown (hard rule #3); the disclosure is self-initiated (the distributor created the share link); the guest-checkout exception is scoped (a session pass set on link-open, **cleared on order placement** — `SCT-09`) so it cannot leak across unrelated sessions/carts. Residual: `full_name` is the registered legal name (not a chosen display name), and a share link is forwardable. Accepted for Easy Purchase; revisit a distributor "display name" field if abuse appears | 2 | Accepted (2026-06-14) |
| R-31 | Full PAN + full Aadhaar held encrypted at rest pre-KYC; KYC scans (PAN, Aadhaar front/back, cheque, address proof, photo) retained encrypted after approval (deviates from Hard Rule 8) | Data protection — High | DSR 2021; DPDP 2023 §8; Hard Rule 8; T&C §15 | The `distributors.pan_encrypted` / `aadhaar_encrypted` columns (migration `2026_05_12_000001`) hold the **full** PAN and **full** Aadhaar number at rest, on Product-Owner request, until KYC approval — `ApproveKycSubmission` then **nulls both columns**, leaving only `pan_last4` / `aadhaar_last4`. **Amended 2026-09-11 (client decision, QA fix run Q10/Q22):** the uploaded scans — PAN, Aadhaar front and back, cancelled cheque, address proof, photo — are **kept after approval** (the earlier wording "purges the uploaded images" was also inaccurate: only `pan` + `aadhaar` were ever deleted, F107). Controls on the retained scans: every object is written as `PiiCrypter` ciphertext by `KycDocumentVault` (bucket holds no readable image; legacy objects converted by `kyc:encrypt-documents`); the bytes are served only through the admin route `admin.kyc.document`, which writes an `admin.kyc.document_viewed` audit row per view and never issues a presigned URL (F106); retention is the admin-owned setting `kyc.document_retention_days` (default 2,920 days = the eight years published in `terms.md` §15 / `privacy.md`, R-54), enforced nightly by `kyc:purge-expired-documents` with a `kyc.documents.retention_purged` audit row per distributor. Keeping an Aadhaar image is holding the Aadhaar number in image form — a deliberate, client-directed deviation from Hard Rule 8 that needs the same RA-01 sign-off as the number columns. Hard Rule 8 says raw Aadhaar is **never** stored (only an AUA/KUA reference + last-4), so holding the full number — even transiently and encrypted — is a deliberate deviation. Controls in place: `PiiEncrypted` cast → `PiiCrypter` (AES-256-CBC) on the dedicated `PII_ENCRYPTION_KEY` (ADR-0008); columns `$hidden`; only masked last-4 accessors exposed; values nulled + images purged on approval; surfaced by the T-6.1 security audit (finding F3). **GAP:** the migration comment claims this is "logged in the risk register" but until this entry it was **not**, and there is **no formal risk-acceptance record** (no `docs/security/risk-acceptances.md`). **Action before Phase-1 exit:** Compliance Officer + Product Owner must either (a) formally sign off this deviation with a documented legal basis and retention bound, or (b) re-architect to store only the AUA/KUA reference + last-4 from the outset. Do **not** treat as accepted until signed — formal sign-off record drafted at `docs/security/risk-acceptances.md` (RA-01) awaiting PO + Compliance Officer signatures | 1 | **Open (2026-06-17)** |
| R-32 | Paid GSB retained on post-settlement order cancellation/refund (no clawback) | Statutory — Medium | DSR 2021 Rule 5(1)(c); Hard Rule 2; KP Q8 (kp-clarifications 2026-06-27); ADR-0010 | When an order is cancelled/refunded AFTER its group BV fed a settled GSB cut-off, the credited/paid GSB is deliberately **not** clawed back — Product-Owner direction (KP Q8: "no clawback of already-paid bonus; reverse the BV from future accumulation on the same leg"). Controls: the full BV is reversed via `GroupBvReversalService` (ADR-0010) — netted off the unsettled day where possible, otherwise carried as a same-side negative (`group_bv_debts`) that suppresses future GSB until recovered, so the economics self-correct over time; every reversal audit-logged (`bv.group_reversed`) with per-ancestor `group_bv_reversals` rows; titles/ranks earned are sticky per the same KP answer. Residual (three faces of the same KP directive, all self-correcting via the same-side debt but exposed until enough same-side volume arrives): (a) a commission remains paid on a sale that was ultimately refunded (incl. statutory cooling-off returns); (b) cancelled BV that a settled-but-unpaid day folded into the slab-1 weaker / power **carry-forward** stays in CF (the CF store is never mutated outside a cut-off — rewind-machinery invariant) and could contribute to a later slab match before the debt nets out; (c) `RankQualificationService` sums raw monthly `group_bv_daily`, so a month's rank gate can count BV cancelled after that day settled (KP round-3 #4 says ranks are sticky and only future BV is reduced — consistent, but the same DSR-spirit question). Literal hard-rule-2 linkage to a `product_sale_id` holds in all three; the DSR-spirit exposure needs **legal opinion at the batched launch sign-off** (defer-all-sign-offs gate) and KP's re-confirmation that CF/rank retention is intended. If counsel rejects, Phase-4's wallet-clawback mechanism (ADR-0009 forward note) plus a CF-adjustment pass are the fallback | 4 | Open (2026-07-05) |

| R-33 | GSB slabs 3–7 pro-rated from a daily 45% pool — variable payouts, KP-accepted deviations, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice), §6.5 (pool cap pre-authorised); DSR 2021 Rule 5(1)(d)/(g); Hard Rules 2 & 3; KP worked example 2026-07-29 | Slabs 1–2 stay fixed (₹250/score); slabs 3–7 are priced daily at `min(₹250, floor((pool − slab-1/2 fixed) ÷ Σ slab-3–7 scores))` where pool = `comp.gsb.pool_rate_bp` (45%) of the day's order-backed company BV (`bv_ledger_entries` signed sum — Rule-2 linkage verified: non-nullable `order_id` FK, no non-sale pathway). Controls: economics frozen per day in `gsb_daily_pools` **before any credit**; `score_value_paise` snapshotted per result row (history immune to plan edits and re-runs); single-distributor retries reuse the frozen value; admin per-day report (GSB Input & Output) exposes pool/value/variance/leftover; all behind `GsbDailyPoolPricingFeature` (default OFF = legacy byte-identical). KP-accepted deviations: (a) on a starved day fixed slab 1–2 payouts are still paid in full and may exceed the 45% pool (negative `leftover_paise` recorded); (b) a slab 3–7 match can price at **₹0** while the weaker-leg BV is still consumed (distributor copy discloses the ₹0 floor); (c) zero-variable-achiever days freeze the ₹250 cap so a later admin retry pays full value; (d) frozen/repurchase-held/suspended matched rows are counted in the funded denominator though they may never credit — residue stays with the company. **Activation gate: do NOT enable `GsbDailyPoolPricingFeature` in any environment paying real distributors until the DSA §6.2 30-day written notice to all active Distributors has run and the pool formula is published at `/p/compensation`** (page created and seeded 2026-07-30 — `database/seeders/content/compensation.md`, §5.4 publishes the slab 3–7 pooled score-value formula and the ₹250 ceiling; **remaining pre-activation task: replace the `[EFFECTIVE DATE — TO BE INSERTED]` placeholder with the date stated in the §6.2 notice, and obtain counsel sign-off on the page**) | 3 | Open (2026-07-29) |
| R-34 | Stealth `developer` super-role: platform-configuration actions are concealed from the admin audit-log view | Governance / audit — Medium | DSR 2021 Rule 5 (record-keeping); CLAUDE.md audit-log convention; R-17 (separation of duties) | A `developer` role supersets `admin` and exclusively owns platform configuration (feature flags, compensation-plan edits, and the developer-owned settings keys — plan rates/caps, payout thresholds, cooling-off, placement, attribution, self-purchase, payments, notifications). Product-Owner requirement: the role must be invisible to every other role. Consequence: in the admin console's audit-log view (`AuditLogPresenter::maskPrivilegedActors`), rows whose actor holds `developer` render the actor as `system` — including for **`admin-compliance`**, the role designated to review audit trails. That weakens R-17's stated compensating control ("every action is audit-logged **with the actor** — traceable after the fact") for exactly the actions with the largest economic reach. Controls that remain: `audit_log.actor_id` is **never** altered — the full actor is retained in the database and is recoverable by direct query, by a developer-role viewer, and by any external/forensic export; masking is presentation-only and applies to no other role; every developer action still writes its audit row with full before/after details; plan-edit and settings-write paths are unchanged in what they record. Mitigations added after the 2026-07-29 compliance review: (i) the **registration killswitch** is owned by `incident` and remains toggleable by the whole console incl. `admin-compliance` (`AdminFeatureFlagController::canUse`) — an incident control must not depend on one person's availability; (ii) the statutory/deduction keys (`commerce.cooling_off.days`, `comp.tds.rate_bp`, `comp.admin_charge.rate_bp`, `payout.*` thresholds) are **visible read-only** to admin via `ADMIN_READABLE_KEYS`, so compliance can still verify the 30-day term and the deduction rates; (iii) audit rows *about* a privileged account are removed at query level (`AuditLogPresenter::hidePrivilegedSubjects`) because actor-masking alone leaked the role through the `staff.*` details payload and the resolved subject name. Residual (accepted, PO-directed): an admin-compliance reviewer working solely from the console cannot attribute the config/plan rows to a person, and the compensation-engine flags — including the R-33 gate — are toggleable only by the role that owns them. **Action: Product Owner to confirm the concealment requirement outranks console-level audit attribution for admin-compliance, and to record who holds the developer credential; revisit if an external auditor requires console-only attribution.** | 3 | Open (2026-07-29) |
| R-35 | Mentorship Bonus pro-rated daily from a 3% pool — variable payouts, no configured point value, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; KP worked examples 2026-07-30 | MSB no longer pays `points × a configured ₹250 value` (that column is dropped from `gsb_slabs`). Each day the pool = `comp.msb.pool_rate_bp` (300 bp = 3%) of the day's order-backed company BV (the same signed `bv_ledger_entries` sum the GSB pool uses — Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway), and the point value = `floor-to-whole-rupee(pool ÷ the day's total MSB score points)`, clamped at ₹0 (company BV is a signed sum and can be negative on a refund-heavy day). Every earner is paid `their points × that one value`, so the day's MSB spend is the 3% envelope less the flooring remainder. Controls: economics frozen in `msb_daily_pools` **before any credit** (immutable row, `msb.pool.frozen` audit entry); `msb_point_value_paise` snapshotted per `mentorship_bonus_results` row (history immune to plan edits and re-runs); single-distributor retries reuse the frozen value; the admin **MSB Input & Output Per Day** report exposes BV/pool/points/value/income/leftover per day with CSV. Accepted deviations: (a) an admin retry credits points that were not in the frozen denominator, so `leftover_paise` can go negative and the company absorbs it (mirrors R-33); (b) a **zero-point day** freezes a ₹0 value, so the pool goes unspent and a later retry on that day writes a ₹0 row with no wallet entry — **this is the opposite of the GSB pool's zero-achiever rule** (R-33, where the cap is frozen so a retry pays full value), chosen so a single retried sponsor cannot take a whole day's pool. **Ruled and confirmed 2026-07-30: a zero-point day freezes a ₹0 point value and nothing is paid to anyone for that day, ever, including on a later retry.** The asymmetry with R-33 is deliberate and follows from GSB having a ceiling that MSB does not: every GSB slab's score value is capped at ₹250 (slabs 1–2 fixed at that value, slabs 3–7 variable but never exceeding it), so freezing the cap on a zero-achiever day is inherently bounded — the worst case is one retried distributor paid at the normal maximum. The MSB point value is pure `pool ÷ points` with no cap at all, so there is no ceiling to freeze, and any non-zero standby value would hand a single retried sponsor a disproportionate share of an otherwise unspent day. The report flags such days and every ₹0 credit is logged. **No §6.2 breach has occurred for this revision**: `MentorshipBonusFeature::resolve()` returns `false` and the flag has never been enabled in any environment paying real distributors, so **no MSB has ever been credited to a live distributor** and the plan was never published — the §6.2 obligation attaches to first activation, not to this change. This matters because the revision is otherwise squarely a reduction: a slab-1 sponsor went from a guaranteed ₹5,250 (21 × ₹250) to a variable amount — ₹1,050 in KP's own example, and possibly ₹0. **No independent killswitch (accepted):** unlike GSB (whose pool sits behind its own `GsbDailyPoolPricingFeature` with a byte-identical fixed-price OFF path), MSB's pool is gated only by `MentorshipBonusFeature` — the same flag that decides whether MSB exists at all — and the migration drops `msb_score_value_paise`, so there is no fixed-price fallback. Rollback procedure if the pool arithmetic misbehaves: switch `MentorshipBonusFeature` off (itself a §6.2 event once live), then `php artisan migrate:rollback` the `2026_07_30_100001` migration to restore `gsb_slabs.msb_score_value_paise` (default ₹250) and re-seed `GsbSlabsSeeder`, then restore the fixed-price pricing in `MentorshipBonusService::creditAccrual()`. **Reset vs evidence:** `platform:reset-purchases` truncates `msb_daily_pools`/`gsb_daily_pools`, bypassing the models' append-only guard — after a reset, a day's economics are reconstructible only from the retained `msb.pool.frozen` / `gsb.pool.frozen` `audit_log` rows (the audit log is deliberately preserved by the reset). **Activation gates — both must be cleared before any real MSB payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula is published at `/p/compensation` (page created and seeded 2026-07-30 — §6.2 publishes the point-value formula, §6.3 states the value is variable and may be ₹0, §6.4 publishes the zero-point-day rule, §6.5 marks MSB not yet operative; **remaining pre-activation task, shared with R-33: replace the `[EFFECTIVE DATE — TO BE INSERTED]` placeholder with the date stated in the §6.2 notice, and obtain counsel sign-off on the page**). Deviation (b) was signed off on 2026-07-30 and is no longer a gate.** | 3 | Open (2026-07-30) |
| R-36 | Growth Booster Bonus pro-rated monthly from a 5% pool — variable point value, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; PO confirmations 2026-08-05 | GBB has **no configured rupee value per point**. Each month the pool = `comp.gbb.pool_rate_bp` (500 bp = 5%) of the month's order-backed company **BV** — the same signed `bv_ledger_entries` sum the GSB and MSB pools use, **not** order sales value (Product Owner confirmed 2026-08-05; Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway) — and the point value = `floor-to-whole-rupee(pool ÷ the total AGP of all eligible distributors that month)`. Every earner is paid `their AGP × that one value`, so the month's GBB spend is the 5% envelope less the flooring remainder. **A distributor's per-point rupee value is therefore not fixed in advance**: it falls as the month's total AGP rises and rises as company BV rises, neither of which any distributor can know or control, and it may be ₹0. AGP accrue only from GSB slab matches (12 / 5 / 2 for slabs 1–3, none for 4–7), capped at 120 AGP per distributor per month; eligibility is limited to distributors who held **no rank in the previous month** (first-time rank achievers in the current month remain eligible). Controls: economics frozen in a `gbb_monthly_pools` row **before any credit** (immutable, `gbb.pool.frozen` audit-log entry) and never recomputed, so re-runs and single-distributor retries price against the same snapshot — an auditable, tamper-evident trail matching the GSB (R-33) and MSB (R-35) daily pools; the point value is snapshotted per result row so history is immune to plan edits and later re-runs; the admin **GBB calculation report** exposes BV, pool, eligible distributors, total AGP, point value, income, leftover and held/suspended rows per month. **Repurchase interaction — superseded 2026-09-07 (forfeit model, `fix/compensation-frozen-roster-and-monthly-close`, commits `329c5d8..056e830`):** the hold-and-release wording above is retired. GBB — like Fortune and Rank Bonus — is **never held** by the repurchase cycle: there is no `repurchase_held`/`repurchase_suspended` GBB state and the four `ReleaseHeld*OnReactivation` listeners that used to release it were deleted. The only repurchase-linked exclusion left is the month-end **repurchase wallet = ₹0** gate (`RepurchaseWalletGateService::clearedAtMonthEnd()`, also used by rank requalification and AO-GO): a distributor whose repurchase wallet is not ₹0 on the last day of the month is `repurchase_wallet_blocked` for that month's GBB — gross 0, excluded from the AGP denominator so they cannot dilute the value of a bonus they cannot receive that month, forfeited rather than released later. Upstream, a distributor's GSB cycle failure forfeits the failed days' income outright (`repurchase_forfeited`, gross 0, both carry-forward stores preserved untouched) rather than holding it — those days simply generate less AGP, they do not put GBB itself into a held state. Compliance sign-off for the offers work is commit `b902958`; the trailer on `200dd64` was written before any review of that commit existed and the correction stands in `b902958`'s message. **No §6.2 breach has occurred**: the GBB feature flag resolves `false` and has never been enabled in any environment paying real distributors, so no GBB has ever been credited to a live distributor and the component is published at `/p/compensation` §10 as *not yet operative*. **Activation gates — both must be cleared before any real GBB payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula, the AGP schedule and cap, the prior-month-rank eligibility gate and the variable/possibly-₹0 point value are published at `/p/compensation` with the effective date stated in that notice (shares the outstanding `[EFFECTIVE DATE — TO BE INSERTED]` placeholder and counsel sign-off task with R-33 and R-35).** Related: the Rank Bonus pool derivation was corrected in the same change — each rank's `pool %` is a share of the **20% Rank-Bonus envelope of company BV**, not of turnover directly (10,00,000 BV → ₹2,00,000 envelope → Rank 1's 7% = ₹14,000) | 4 | Open (2026-08-05) |
| R-37 | Fortune Bonus pro-rated monthly from a 5% pool — variable point value, "luck or chance" framing must never reach public copy, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; KP Fortune rework 2026-08-07 | Fortune no longer pays a **fixed rupee amount per matrix level** (the ₹3.39 … ₹51.00 ladder; `fortune_bonus_levels.bonus_paise` is dropped, so no fixed-price fallback survives — same one-way design as MSB in R-35). Each month the pool = `comp.fortune.pool_rate_bp` (500 bp = 5%, superseding the June envelope's 6% Fortune share) of the month's order-backed company **BV** — the same signed `bv_ledger_entries` sum the GSB, MSB and GBB pools use (Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway) — and the point value = `floor-to-whole-rupee(pool ÷ the total Fortune points of everyone enrolled that month)` *[SUPERSEDED 2026-08-09 — see the amendment at the end of this row: values are now computed per matrix level through the cascade, not month-wide]*. Points are earned **only from the distributors placed below the earner** in that month's 3-wide, 9-level matrix (*[superseded ladder — now 9 / 8 / 7 / 6 / 5 / 4 / 3 / 2 / 1, see amendment]* per member at relative levels 1–9, admin-editable; deeper than 9 earns nothing), so a distributor's rupee value is **not fixed in advance and cannot be quoted**: it falls as the month's total points rise, rises as company BV rises, and **may be ₹0** (a sub-rupee division floors to zero and the whole pool goes unspent). **There is deliberately no manual override of the computed point value** (KP 2026-08-07) — an admin can edit the per-level point schedule but can never set a rupee figure for a month or for an individual, which is the control that keeps the payout arithmetic uniform and auditable. Enrolment gates (all monthly; **the repurchase condition is no longer an enrolment gate and nothing is ever "held"** — under the forfeit model confirmed by the client 2026-09-07 a day inside a failed repurchase cycle simply produces no GSB slab achievement, so it never counts toward enrolment in the first place, while a participant still holding repurchase-wallet money at the **last instant of the month** forfeits that month outright at the payout pass: a `repurchase_wallet_blocked` row at gross ₹0, no wallet credit, the matrix position kept, and the share the cascade had allowed for stays with the company as leftover, never redistributed and never released. **Month-1 joiners are exempt from that month-end wallet gate** — the published plan page states that the calendar month in which a distributor registers carries no wallet condition, and the exemption is read from the `new_joiner` tier frozen on the participant row at enrolment, so a re-run cannot widen it; the exemption is Fortune-only, Growth Booster and rank requalification have no such carve-out): month-1 joiners need 3,000 BV lifetime personal purchases **and GSB slab 1 specifically**; month-2+ non-ranked need a personal-purchase title (≥3,000 BV lifetime) + 600 BV in the month + ≥1 slab; ranks 1–5 need 1,000 / 1,100 / 1,200 / 1,300 / 1,400 BV with **8 / 11 / 14 / 17 / 20** credited slab achievements (repeats counted — GSB has only 7 distinct slabs); ranks 6–9 are excluded. Controls: economics frozen in a `fortune_monthly_pools` row **before any credit** (immutable, `fortune.pool.frozen` audit-log entry) and never recomputed, so re-runs and single-distributor retries price against the same snapshot — the same auditable, tamper-evident trail as R-33 / R-35 / R-36; **enrolment into a month whose pool row already exists is REFUSED by the engine (`fortune.enroll.refused_pool_frozen`), so no one can be added to the matrix after the point value is fixed — a late entrant would otherwise be paid at the frozen value on points that were never in the frozen denominator, overspending `pool_paise`**; `points` + `point_value_paise` snapshotted per `fortune_bonus_results` row so history is immune to plan edits and later re-runs; the admin **FB Monthly Calculation** report exposes company BV, pool, total points, point value, payout and leftover per month plus each participant's level, points, value and income. **Mis-selling hazard — explicit: KP's own written spec describes the Fortune Bonus as income "based on luck or chance". That framing must NEVER appear in public copy, distributor-facing UI, training material or any presentation** — a bonus presented as a game of chance invites both a DSR 2021 Rule 5(1)(d) mis-selling reading and a lottery/gaming characterisation. The published description at `/p/compensation` §7 and every internal help page describe it factually as a **participation-based monthly pool** whose value is variable and may be ₹0, with no example figures and no forward-looking amounts. Any copy review touching Fortune must re-check this. **No §6.2 breach has occurred**: `FortuneBonusFeature` resolves `false` and has never been enabled in any environment paying real distributors, so no Fortune Bonus has ever been credited to a live distributor. **Activation gates — both must be cleared before any real Fortune payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula, the per-level point schedule, the participation gates, the variable/possibly-₹0 point value **and the FCFS position-dependence — that a month's matrix is filled first-come, first-served on first-GSB-credit date, so what a distributor earns depends on where they land relative to distributors who qualify later in the same month, a structural advantage of earlier qualifiers that must be disclosed rather than left implicit and must never be framed as luck or chance (counsel to confirm the disclosure wording alongside the §6.2 notice)** — are published at `/p/compensation` §7 with the effective date stated in that notice (shares the outstanding `[EFFECTIVE DATE — TO BE INSERTED]` placeholder and counsel sign-off task with R-33, R-35 and R-36).** Open with KP (do not block, restated at sign-off): whether the matrix should mirror the Genos/sponsorship structure rather than the monthly FCFS fill, and whether the non-ranked "one of 7 titles" gate means Retailer or a higher specific title. **2026-08-09 amendment (KP's level cascade):** depth points changed to 9/8/7/6/5/4/3/2/1; every qualifier now has a guaranteed **₹30 minimum** (`comp.fortune.min_commission_paise`, pro-rated `floor(pool ÷ N)` when the pool cannot cover it — the pool is never overspent); capped levels 0–6 pay `min(₹30 + points × per-level whole-rupee value, cap)` with caps ₹30k/₹30k/₹30k/₹30k/₹20k/₹10k/₹5k **including** the ₹30; levels 7–8 share one residual value; level 9 gets the ₹30 only *[SUPERSEDED 2026-09-03 — levels 7–9 are capped ₹2,500 / ₹1,500 / ₹30, see the amendment below]*; sparse months keep the absolute-level treatment. The frozen snapshot now includes per-level economics (`fortune_monthly_pool_levels`), still immutable, still no manual override; incomes remain variable and quotable by no one (a capped level's value depends on the whole month's points, and the residual value may be ₹0). **Rule 5(1)(c) rationale for the per-head minimum** (write-down demanded by the 2026-08-09 compliance review): the ₹30 is not a recruitment or participation fee in reverse — every recipient must have cleared the month's sales-conditioned gates (≥1 credited GSB slab that month, i.e. matched order-backed group BV on both Genos sides, plus the tier's own 600–3,000 BV personal-purchase requirement), it is funded exclusively from the 5% order-backed BV pool, and it pro-rates to ₹0 in a month with no sales — so no rupee of it can exist without product sales. All prior controls, the "never frame as luck or chance" rule, the FCFS disclosure item and both activation gates carry forward unchanged — the §6.2 notice must describe the cascade (minimum, caps, ~~residual sharing~~ *[SUPERSEDED 2026-09-03 — ten per-level caps]*), **activation gate (ii) now additionally requires the published plan page (`app/database/seeders/content/compensation.md` §7.1–§7.5, reconciled to the cascade 2026-08-09 and to the capacity cut-off 2026-09-03) to be re-verified against the engine at notice time**, and the two 2026-08-09 interpretation decisions (absolute levels in sparse months; pro-rated minimum on shortfall) were made by the product owner's proxy and need KP's written confirmation before the notice is drafted. **2026-09-03 amendment (client's consolidated notes, via the product owner):** levels 7, 8 and 9 are now ordinary capped levels — ₹2,500 / ₹1,500 / ₹30 per member (the ₹30 cap equals the guaranteed minimum) — so every level 0–9 recomputes its own whole-rupee value from the pool and points carried forward; the shared residual value and the flat level are retired (`FortuneBonusLevelsSeeder` + data migration `2026_09_03_100000`; months frozen earlier keep their own `fortune_monthly_pool_levels` snapshot and reconstruct unchanged). Confirmed the same day: a qualifier with nobody below receives the ₹30 only and the unspent pool stays with the company (never redistributed — a handwritten per-head split circulated earlier that day was withdrawn); the ₹30 stays inside the single wallet credit with one Income column and an explanatory note; income below the cap is ₹30 + points × value; the ₹36cr example's shift (level 8 ₹624, leftover ₹24,576) is intended; per-level caps stay on Plan Settings — admin-visible, developer-editable (`role:developer` on the write route). **"Repurchase Wallet zero" gate built 2026-09-03 (reading pending the client's written confirmation):** from month 2 a qualifier's repurchase-wallet balance (10% payout-deduction credits minus checkout debits, `WalletService::repurchaseWalletBalancesAsOfPaise`) must be ₹0 as of the last day of the month being enrolled — entries after month end are ignored although enrolment runs on the 9th; month-1 joiners are exempt (rule 1 has no wallet condition); a distributor who never received a deduction passes. Developer toggle `comp.fortune.require_repurchase_wallet_zero` (default ON, Settings → Compensation plan, `FortuneBonusFeature`-gated). Hard Rule 2 is untouched (a read-only enrolment filter; credits stay product-sale-linked), but the gate is **not** self-evidently Rule 5(1)(c)/(d)-neutral: the wallet balance is cumulative, non-withdrawable (R-60) and scales with earnings (10% of the prior period's bonuses, ₹10,000/period), and it is applied in full at checkout — so clearing a ₹X balance requires ≥ ₹X of product orders, with no ceiling and no "reasonable period to sell" bound. That is a compelled, unbounded purchase obligation enforced by withholding bonus eligibility (the DSR Rule 3 inventory-loading dimension), materially different from the fixed 600 BV monthly repurchase gate, and it is **referred to counsel at the batched launch sign-off** alongside R-32/R-33. It is described factually in the help page and at `/p/compensation` §7.2 and §8 (page version bumped to v2026-09-03). Exclusions are irreversible once the month freezes, so each is logged (`fortune.enroll.skipped_wallet_nonzero`, with the balance) and counted. If the client rules that only the repurchase BV matters the toggle goes OFF; if they mean the BV must be bought *with* the wallet, that is a further change and a fresh review. **Client answers 2026-09-03 (via the product owner):** placement stays FCFS (re-confirmed); the matrix is levels 0–9 only (29,524 positions) and nothing deeper exists — once a month's matrix is full, later qualifiers (FCFS order) are **not entered for that month** and receive nothing, not even the ₹30 (`FortuneBonusParticipant::MAX_POSITIONS`, logged `fortune.enroll.matrix_full`, counted by the enrol command). Disclosed at `/p/compensation` §7.1 (capacity + same-day tiebreak: earlier registration first), §7.2 (meeting the conditions does not guarantee entry) and §7.3; each exclusion is written to `audit_log` (`fortune.enroll.matrix_full`). This is materially better for compliance than a placed-but-unpaid tenth level would have been: a shut-out qualifier hands no points to anyone. Two follow-ups: (a) the capacity cut-off joins the repurchase-wallet gate in the counsel referral at the batched launch sign-off — a qualifier can complete 600–3,000 BV of purchases and still be shut out by a limit they cannot observe; (b) a shut-out qualifier has no distributor-visible record of the month yet (the audit row is admin-side) — product owner to decide whether the Fortune page should show it. NB the Fortune page's explanatory note is `@developer`-gated (pre-existing), so no distributor sees it; the published plan page is the sole distributor-facing disclosure. Still open with the client: rank-of-month vs highest rank. **This is a reduction for levels 7–8, and a severe one in a sparse month**: under the retired residual rule a level-7 member with a single downline in an otherwise empty deep matrix could take the whole remaining pool (the regression test locks ₹99,966 on a ₹1,00,000 pool); the same member now takes at most ₹2,500 — the §6.2 notice must present it as a reduction, as R-35 does for MSB. Pre-flag-on checklist addition: verify all ten `fortune_bonus_levels.cap_paise` on the target environment against §7.4 (the data migration deliberately leaves a hand-edited non-null cap alone). The §6.2 notice must describe the ten per-level caps. **Activation gate (iii), added 2026-09-03:** before the flag goes on anywhere, either the client has confirmed in writing that "Repurchase Wallet zero" means a ₹0 month-end balance **or** `comp.fortune.require_repurchase_wallet_zero` is OFF; and the distributor Wallet page must stop styling a positive repurchase balance as good news and must say that a balance left on the last day of the month excludes them from that month's Fortune matrix (today it cannot mention Fortune without breaking zero-trace flag gating). Gates (i) and (ii) unchanged | 6 | Open (2026-09-03) |
| R-38 | Admin-triggered manual engine runs — money-moving engines can now be invoked from the admin console with dependency chains | Operational — Medium | DSR 2021 Rule 5 (record-keeping); Hard Rule 2; CLAUDE.md separation-of-duties principle; compliance review 2026-08-13 | The Engine Runs page (`/admin/compensation/engine-runs`, shipped 2026-08-13) lets a `finance.record` admin manually queue any bonus engine for a period; triggering an engine first runs its missing prerequisites (prerequisites-first, skip-if-done, never downstream). Controls: (i) every trigger writes an `audit_log` row (`compensation.engine.manual_run`) with actor, mandatory reason, chain id and the resolved step plan **before** dispatch; (ii) every execution — cron, CLI or manual — lands in the append-only `engine_runs` table via a console-event listener, so scheduled and manual history share one log; (iii) all engines are idempotent re-runners against frozen pool economics (R-33/R-35/R-36/R-37), so a redundant run cannot double-credit; (iv) a cache lock plus a live-`running`-row check makes a manual run stand down when the scheduler already holds the engine; (v) a **feature-flag-off no-op is recorded as `skipped` (`feature_flag_off`), never `succeeded`** — the 2026-08-13 compliance review's Critical finding — so the dependency resolver can never treat a flag-off period as computed and later freeze immutable pool rows against missing data; (vi) **maker-checker preserved: the two payout-batch engines (`gsb:weekly-payout`, `payout:monthly-run`) are registry-marked `manuallyTriggerable: false`** — the same `finance.record` permission approves batches, so a manual trigger would let one admin both create and approve a payout; they remain scheduler-only and the controller rejects them — **amended 2026-09-15:** they ARE reachable from the console via the nightly chain, so the admin “Retry this night” control passes `--without-payouts`, which drops both steps whatever the date; no admin-initiated path builds a payout batch, and the maker stays the scheduler (see R-81 addendum) — **further amended 2026-09-17:** the retry now passes `--weekly-payouts-only`, which admits `gsb:weekly-payout` and still excludes `compensation:monthly-payout-close`; the weekly batch is stamped with the acting admin as maker, the monthly close needs no admin hand because every night from the 8th rebuilds it; (vii) a killed/timed-out chain job marks its `running` rows failed via the job's `failed()` hook so no half-applied chain hides behind a stale row; (viii) **added 2026-09-17:** with the GSB feature flag OFF the chain no longer claims a missed Tuesday. The sweep records `skipped` and builds nothing, so no batch ever exists, so the backfill's frontier search can never find one and the null-frontier branch named the same Tuesday as "never built" on every non-Tuesday night for as long as the flag stayed off — noise in `engine_runs.summary` that would have trained operators to ignore the one warning that matters. The Tuesday itself is still stepped, so the `skipped` row that records the engine was off on a night it was due survives; only the backfill and its warning go, and both return the moment the flag does. Tests: `NightlyRunCommandTest` (3 cases). Residual: `engine_runs.summary` stores up to 4,000 chars of raw console output (currently aggregates only — guarded by a regression test asserting no ADN/PAN/phone patterns); any future per-distributor console output would need the PII scrubber before this log. **2026-09-06 (compliance review):** with the developer testing gate open the period ceiling was removed entirely — `periodInputMax()` returned `''` and `parsePeriodOrFail()` skipped the future/in-flight guard — so any future period was accepted. Rank Bonus, GBB, ADC and Fortune have no premature-freeze self-heal (GSB has one, and GBB gained one the same day), so a far-future manual run could freeze a pool only a full recompute could reopen. Both now cap at end-of-next-month, the same horizon the replay uses. | 4 | Open (2026-08-13) |
| R-39 | Grievance email channel is manually worked, not ingested | Statutory — Low | T&C §11; policy §3.2 | `grievance@arovolife.com` has no inbound parser — an officer reads the mailbox and records the complaint with **Record a complaint**, dating it to receipt. The published §3.2 previously promised an "automated acknowledgement… within 48 hours"; that text was **amended on 2026-08-16** to state the mailbox is monitored on business days and to point complainants needing an instant complaint number at the web form. The promise and the mechanism now agree. Residual: a weekend or an unstaffed day can still breach the 48-hour clock, and the breach is only visible once the complaint is recorded. Build inbound ingestion (Postmark inbound webhook — Postmark is already configured in `config/mail.php`) if email volume makes manual working unreliable | 1 | Open (2026-08-16) |
| R-40 | Anti-retaliation protection is published but unbuilt | Contractual — Medium | Policy §6.4 | §6.4 promises a good-faith complainant protection from retaliation by their upline, downline or any administrator, and calls retaliation a terminable breach of the Code of Ethics. **No mechanism implements this.** Partial controls exist — nobody can handle a grievance filed by their own distributor account, and Ethics complaints are restricted to the compliance side — but there is no protected-reporter flag, no warning when an accused upline or admin acts against the complainant, and no automatic exclusion of a named party from handling. Minimum viable mitigation: a `protected_reporter` flag on the ticket that surfaces a warning banner on the complainant's admin screens and blocks the named party from assignment | 1 | Open (2026-08-16) |
| R-41 | Grievance retention purge is written but not scheduled | Data protection — Low | DPDP 2023 §8(7); policy §6.2; DSR 2021 Rule 12 | `grievance:purge-expired` deletes tickets, timeline rows and attachment objects past the seven-year retention window, but is **deliberately not wired into the scheduler** and reports only unless `--force` is passed. Rationale: nothing can expire before 2033, and whether an anonymised summary row should survive the purge for the compliance report's historical counts is a decision for counsel, not one to hard-code seven years early. The live storage-limitation gap the reviewer identified — `retention_until` being set only at closure, so a resolved-but-never-closed ticket retained PII forever and was invisible to the purge — **was fixed on 2026-08-16**: `resolve()` now sets it and `close()` recomputes it. Review date: 2032, before the first cohort can expire | 1 | Open (2026-08-16) |
| R-42 | Published SLA rows 4–6 are not modelled in the grievance clock | Statutory — Low | Policy §2 rows 4–6 | The §2 table publishes six clocks; the grievance module models three. A cooling-off refund (7 business days), a buyback refund (15 business days) and a payout-failure investigation (5 working days) are **order-level and payout-level clocks owned by Commerce and Wallet**, not grievance clocks — a grievance *about* one of them correctly carries the generic 30-day resolution window, since resolving the complaint is a different act from performing the refund. Residual: §2 does not say so, and a reader could take all six as grievance SLAs. Either add per-category overrides to the grievance settings registry, or amend §2 to state which rows are order-level and where they are tracked | 2 | Open (2026-08-16) |
| R-43 | §21 re-registration after termination is recorded but not permitted | Contractual — Medium | T&C §21; Hard rule 6 (one PAN = one ADN) | §21 lets a terminated Direct Seller register again after a wait — one year, or two from the Diamond tiers. The platform now **computes and stores** `distributors.reregistration_allowed_from` at termination and shows it on the dormancy page, but **nothing consumes it**: the unique index on `distributors.pan_hash` rejects any PAN that has ever registered, so in practice the wait is infinite and the platform is stricter than the agreement promises. Making re-registration work is a decision about hard rule 6 — either the terminated row is reused (the distributor keeps their old ADN, and "re-registration" means reactivation) or the index must permit one terminated row alongside one live one (a new ADN, and one PAN then maps to two). **Deliberately not auto-chosen**: CLAUDE.md forbids silently picking a path that changes compliance-adjacent behaviour. Until the Product Owner rules, a returning distributor is handled by hand | 1 | Open (2026-08-16) |
| R-44 | §21 re-registration wait is mapped to the current rank ladder by interpretation | Contractual — Low | T&C §21 | §21 sets the wait by rank, naming **"Sales Master"** (1 year) and **"Diamond Master and above"** (2 years). Neither rank exists: the current ladder is Silver / Pearl / Emerald / Gold / Diamond / Blue Diamond / Royal Diamond / Crown Diamond / Elite Diamond Partner. The implementation reads this as ranks 1–4 → one year, rank 5 (Diamond Partner) and above → two years, unranked → no wait, with the threshold configurable at `termination.diamond_rank_threshold`. That is the closest faithful reading but it **is** an interpretation, and it decides how long a real person is locked out. Product Owner to confirm before the sweep is switched on; if the agreement text is what changes, it needs a §16.2 amendment notice | 1 | Open (2026-08-16) |
| R-45 | Franchise programme built — company fulfilment infrastructure, 3% commission, DSA §6.2 + R-24 gates | Statutory — Medium | DSR 2021 Rule 5(1)(c)/(d); DSA §6.2; Hard Rules 2 & 7; compliance design review 2026-06-01 (DC-01..DC-05); compliance review 2026-08-17; Product Owner decisions 2026-08-16 | **Withdrawn 2026-09-17 (client decision): the franchise programme is dropped and the Arete Development Centre replaces it.** The programme shipped 2026-08-16 behind `FranchiseFeature` (default OFF, zero-trace) and **the flag was never enabled in any environment paying real distributors and no franchise was ever appointed**, so no DSA §6.2 breach occurred and none of the five activation gates was ever exercised. The service, model, feature flag and admin surfaces were removed on 2026-09-01 when `orders.franchise_id` became `orders.arete_center_id`; the same migration dropped all three franchise tables and narrowed the `wallet_ledger_entries.type` enum, so no franchise schema remains — see the note under this table. **Two of its design decisions are the unbuilt answers to live ADC risks and must not be lost with it: (i)** an order counted in the month its **30-day return window closes**, not the month of delivery — paying on delivery credits and sweeps to bank about halfway through the cooling-off window, which is exactly the hole R-23 still carries on the ADC engine; **(ii)** `franchise_commission_result_orders`, which recorded every contributing order and its net value at the moment of payment, in the same transaction and `restrictOnDelete` because it is evidence — the table R-22 now needs an ADC equivalent of | 4 | Closed (2026-09-17) |
| R-46 | Franchise consignment stock — record exists but franchise not fulfilling | Operational — Medium | Compliance design constraint DC-03 (consignment stock remains Company-owned) | **Withdrawn 2026-09-17 (client decision): the franchise programme is dropped.** The stock ledger this row was waiting on shipped 2026-09-12 (S5) and stands on its own — `stock_movements` (append-only), `inventory_levels`, `stock_batches`, `stock_transfers`, `stock_adjustments` and the `inventory:verify` reconciliation command — so DC-03's "demonstrably Company-owned" requirement is met by an audit trail that no longer depends on a franchise existing. The `franchise_stock` ledger contemplated here is not needed and will not be built. An Arete Development Centre is not a stockist: it holds no consignment, is not a warehouse, and cannot receive a transfer. If that ever changes, the route is `warehouses.type` plus the existing ledger — but that enum is `('hub','warehouse','franchise')` with no `arete_center` value, so it needs a widen (and per the standing rule an enum widen needs a non-MySQL branch for the SQLite test DB) and a fresh risk row rather than this one | 4 | Closed (2026-09-17) |
| R-47 | ADC fulfilment is not wired — the buyer picks a collection centre the pipeline then ignores | Operational — High | Compliance review 2026-08-17; DSR 2021 Rule 5(1)(c); CPA 2019 (unfair trade practice) | **Re-scoped from the dropped franchise programme 2026-09-17: `orders.franchise_id` became `orders.arete_center_id` on 2026-09-01 and the defect came across with the column — and got worse, because the franchise picker was pulled before merge while the ADC picker shipped.** Checkout requires a centre when the buyer chooses collection (`CheckoutController::store()`, `shop/checkout.blade.php`), and `orders.arete_center_id` then has **no buyer-facing and no fulfilment consumer** — only the ADC commission engine and the admin centres registry read it. **No shipment routes to a centre, and the centre's name never appears again anywhere** — outside `shop/checkout.blade.php` not one view renders it, and the Fulfilment module does not reference the column at all. It is worse than showing the buyer nothing: the centre's postal address is copied into the order's `ship_*` fields, so the confirmation page and the invoice both render that address under the heading **"Shipping to"** beside the buyer's own name — an address the buyer never gave, labelled as a delivery that will not happen. **The picker is not feature-flag gated:** it renders whenever `AreteCenter::collectionChoicesFor()` returns a centre, independent of the ADC bonus flag, so this is live on every environment today whatever the flag says. The franchise picker was removed on 2026-08-17 for precisely this reason — offering a buyer a choice the pipeline ignores is a representation the system does not honour — so the live ADC picker is the same unfair-trade-practice exposure, now shipped. It also hollows out the Rule 5(1)(c) rationale that R-24 depends on: the bonus is defended as consideration for fulfilment work on identified sales, and the system records no work. Until fulfilment is wired end to end — shipment routing, the admin order and shipment views, the confirmation page, the invoice, the despatch email, and a **centre-acknowledged handover record as the evidence the commission is paid against** — the collection option must not be offered at checkout. **Client decision 2026-09-18: wire the fulfilment** (Shiprocket + manual dispatch + centre collection), not withdraw the picker. Plan and slicing: `docs/plans/2026-09-18-order-fulfilment.md`. **Two requirements were added by the 2026-09-18 compliance review and both block the merge.** (i) The declaration must reach v3 with per-centre re-acceptance before any centre receives a consignment — see R-21 and R-95; wiring the fulfilment without it makes the company induce breach of its own undertaking, which is worse for the Rule 4 defence than having no declaration at all. (ii) **A tax defect the obvious fix would introduce:** `InvoiceGenerator.php:58` is `$placeOfSupply = strtoupper($order->ship_state ?? $sellerState);`, so the correct remedy of no longer forging the centre's address into `orders.ship_*` silently invoices every inter-state collection order as intra-state — CGST/SGST where IGST is due, frozen on the invoice at checkout. Place of supply must be derived from the centre's state when the order is a collection **Substantially remediated 2026-09-18** (commits 7d610f42…095eee97). Shipped: an explicit `orders.delivery_type`; a collection order storing no delivery address at all, so the centre's postal address is never again presented as the buyer's own; place of supply derived from the centre for GST; `DispatchService` consigning the parcel to the centre with a courier leg and an audit row; the centre named on the admin order page and on the buyer's own order page under “Collect from” rather than “Shipping to”; an `awaiting_collection` state so the buyer can be told it has arrived without the cooling-off clock starting before they have the goods; and a buyer-authenticated handover record that the ADC bonus now reads. **Not yet done, and this row stays open until they are:** the v3 declaration and per-centre re-acceptance (R-21, R-95), the DPDP notice for disclosing buyer contact details to a centre operator, the centre-operator self-service surface (today an operator tells staff, who record it), and the despatch / ready-for-collection notifications. **Both blocking review requirements are now cleared (2026-09-18).** (i) The declaration reached v3 with per-centre re-acceptance and a hard dispatch gate — R-21 and R-95 are closed. (ii) The tax defect was fixed when the remedy shipped: `InvoiceGenerator::placeOfSupplyState()` derives place of supply from the centre's state for a collection order, so an inter-state collection is invoiced IGST rather than silently falling back to the seller's state. **The DPDP amendment is written but not yet live.** `database/seeders/content/privacy.md` now carries purpose §4 4a (collection at a centre: name, mobile and order number disclosed to the operator, for that handover only, with home delivery named as the alternative) and a matching §7 recipients row. Editing the seeder changes nothing that a Data Principal can read — the page is held and needs `content:publish privacy` **per environment**, which is destructive and has not been run anywhere. Until it is published and the §13 **30-day notice** has run, the amendment gives no cover: §13 classes a new recipient and a new purpose as material, and 4a says so in terms. **Still open, and the first item is not housekeeping.** Publishing the privacy page and running the §13 30-day notice is currently the only thing standing between a live checkout option and an **unnotified disclosure of a buyer's name and mobile number to a distributor**. `Consignee::forCollectionAt()` puts both on a consignment addressed to a distributor-operated address, `CheckoutService` deliberately retains `ship_name` / `ship_phone_e164` on collection orders so it can, and `AreteCenter::collectionChoicesFor()` is a plain active-centre query with no flag and no date check — so nothing in code ties the collection option to the notice having run. This company's own pattern is the opposite (`genealogy.downline_stats_visible` and the GSB pool flag both stay OFF until their notice periods run); the pattern is missing exactly where the data leaves the company. Tracked as **R-97**. Also still open: the centre-operator self-service surface; and the despatch / ready-for-collection notifications **Update 2026-09-24 — the centre-operator surface and the notifications now exist** (branch `feat/fulfilment-slices-5-9`, slices 5–7). The centre owner has `/my/arete-centre/consignments`: confirm arrival (which emails the buyer the collection code) and hand over against the code the buyer presents. The page shows order number, buyer first name, item count and dates only; ownership is read from `orders.arete_center_id`; it is refused while impersonating; and it 404s while `commerce.collection_at_centre_enabled` is off, so **it is gated by R-97 and is not live anywhere**. The admin *Record collection* now also requires the buyer's code, so every delivered collection order has a `collected_at` handover record — before this fix staff could deliver a collection order with no handover record, and the ADC bonus would then silently skip it. The shipped and ready-for-collection emails carry the courier, the AWB and (Shiprocket) a tracking link. A parcel past `fulfilment.max_dwell_days` is flagged in the Action Center (no automatic return). **What keeps this row open:** only R-97 — `content:publish privacy` per environment and the §13 30-day notice before the switch goes on. Shiprocket PII: the outbound booking necessarily carries the consignee name, phone and address to Shiprocket as processor; the stored `shipment_events` payloads are scrubbed to an allow-list (no buyer phone, no scan locations) in both directions. | 4 | Open (2026-08-17, re-scoped 2026-09-17, substantially remediated 2026-09-18; blocking review items cleared 2026-09-18) |
| R-48 | Redeem-points offer built; half-price offer recorded but not redeemable | Statutory — Medium | DSR 2021 Rule 5(1)(c)/(d); DSA §6.2; Hard Rules 1, 2 & 3; T&C §8; KP spec 2026-06-26; PO decision 2026-08-16; compliance FAIL 2026-08-17 | Shipped 2026-08-17 behind `PurchaseOffersFeature` (default OFF, zero-trace). **The "joining" trigger was DROPPED** (PO 2026-08-16) and is genuinely unexpressible, not merely absent: every grant path requires `bv_ledger_entries` rows, which carry a non-nullable `order_id` FK to `orders`; registration creates no order and reintroducing the trigger would need a migration. Hard rule 1 holds. **The first implementation FAILED compliance review on three Criticals, all now fixed and all worth recording because the prose was right and the arithmetic was not:** (i) `RefundOrder` did not subtract `redeem_points_paise`, so a ₹1,180 order settled with ₹180 cash plus 1,000 points refunded ₹1,180 **in cash** — points were convertible to money by buying and cancelling inside the cooling-off window, defeating the entire "points are not money" characterisation and moving company cash against consideration never received; (ii) `BvLedgerService::reverse()` dates a reversal to the refund date, so a purchase made in August and returned in September left August still qualifying on a sale that came back while breaking September's live streak — the qualification month is now defined by which ORDERS accrued in it, with every entry for those orders netted into that month; (iii) qualification summed **all** attributed BV including retail sales to third parties, so the published "earned entirely from a Distributor's own product purchases" was false — now filtered to `orders.self_consumption`. Also fixed: `RedeemPointsService::refundForOrder()` had no production caller so points were destroyed rather than restored (now called from `RefundOrder` in the same transaction, idempotent); the revenue journal had no contra-revenue line for points, so it was out of balance by exactly that amount and a points-paid order **could never be marked shipped**; an order settled ENTIRELY in points left the refund payable at exactly zero and the ledger rejects a zero-amount line, so the whole refund rolled back and **a fully points-settled order could not be bought back at all** — the statutory T&C §8 buy-back (R-05) failed outright on every `refund_gst = false` path, and it is the state of anyone who redeems the maximum the checkout screen offers (closed 2026-08-17, OFR-018); the `announce` route had no permission gate and its audit row recorded only the new value. **Redeem points remain a discount entitlement, not money**: earned not purchased, non-transferable, own ledger outside the wallet, no TDS, no admin charge, capped at the net-of-GST product value so they can never settle the GST the company remits in cash or the delivery charge. **The half-price half of the offer is NOT redeemable — see R-49** — so its paragraph was removed from published §11.1 and its card from My Offers rather than promising a mechanism that does not exist. **⚠ Still open before flag-on: (a)** KP's written reading of "do not hold any rank" (implemented as *never qualified*, the conservative reading); **(b)** KP's written reading of "20% of total BV" (implemented as 20% of accumulated BV at one point per rupee) — note KP cannot answer (b) meaningfully until (iii) above was fixed, which it now is; **(c)** a documented **GST position** for points — the code now takes one rather than leaving it implicit, and takes the *conservative* one: `TaxSettings::discountsReduceTaxableValue()` ships `false`, so a coupon and redeemed points reduce the amount payable and **not** the taxable value. That keeps `orders.gst_paise`, the `liability.gst_output` credit and the tax invoice on the same figure — the invoice is the source for GSTR-1, and an earlier build declared ₹27 on a ₹1,180 order settled with 1,000 points while the ledger carried ₹180. It is a constant, not a setting, precisely because flipping it without changing `CheckoutService` and `OrderStateMachine` in the same commit would reopen that divergence. **What remains open is whether the §15(3)(a) reduction should be claimed at all** — that is a question for counsel, it costs the company GST on discounted value while unanswered, and it applies to coupons exactly as much as to points (R-28), which pre-date the offers. The artefact counsel will be asked about: on a fully points-settled order GSTR-1 carries an invoice value of ₹180 against a taxable value plus tax of ₹1,180, because the company remits tax on the full sale value while collecting almost none of it in cash; **(d)** an **expiry policy** and the sweeper that writes `expiry` entries — `TYPE_EXPIRY` exists and is never written, so the obligation is currently indefinite; **(e)** an accounting position recognising the outstanding balance as a liability (Ind AS 115); **(f)** the DSA §6.2 notice and the effective date in §11.1. **No §6.2 breach has occurred** — the flag has never been enabled and no points have been credited; note §11.1 ships with the content seeder regardless of the flag, so its accuracy is not protected by the flag being off | 4 | Open (2026-08-17) |
| R-49 | Half-price monthly product is granted but cannot be redeemed | Operational — High | Compliance review 2026-08-17; DSR 2021 Rule 5(1)(c); CPA 2019 (unfair trade practice) | The engine grants a half-price entitlement and names the announced variant, but **no path exercises it**: `PurchaseOfferSettings::offerPricePaise()` has no callers, nothing in cart, catalogue or checkout applies `half_price_rate_bp`, `consumed_order_id` / `STATUS_CONSUMED` are never written for a half-price grant, and no sweeper ever sets `STATUS_EXPIRED`. The published §11.2 paragraph and the My Offers "available — use it by" card were therefore **removed on 2026-08-17** rather than shipped: promising a purchase mechanism the platform ignores is the same defect the franchise collection-point picker was removed for (R-47). The grant row is retained as the record of who qualified. Before this half of the offer is published or enabled: a price override at cart, grant consumption written inside the order transaction, and an expiry job. The **admin** Offers screen carries the same warning as of 2026-08-17 — staff announce the product from there, so an announcement that reads as live is how grants nobody can honour get created. Blocks the half-price half of flag-on; the redeem-points half is independent | 4 | Open (2026-08-17) |
| R-50 | Hard rule 4 is not met: there is no orientation video, and the record used to say there was | **Statutory — Critical** | Hard rule 4; T&C §3.I Step 2; DSR 2021 Rule 5; compliance sign-off C-03 2026-08-17 | The registration wizard renders a placeholder where the orientation video should be, and the only gate is a checkbox the applicant ticks themselves — so the "≥ 95% watched" requirement is never measured because there is nothing to measure. Worse, `RegistrationService` wrote `watch_percent = 100`, `completed_at = started_at` and `playback_fingerprint = 'stub'` for **every** distributor, and stamped `quiz_passed_at` on the spouse of a couple who never sat the quiz. `orientation_views` is precisely the table produced at a DSR audit, so that was an affirmatively false compliance certificate — worse to be caught holding than an honest gap. **Corrected 2026-08-17:** the row now records what actually happened (`watch_percent = 0`, `completed_at = null`, `playback_fingerprint = 'self-declared:no-playback-measurement'`, spouse `quiz_passed_at = null`), and the wizard says plainly that no video exists and that the tick is the applicant's own declaration. The micro-quiz is real and server-validated, and remains the only part of hard rule 4 currently satisfied. **Still required before launch: the video, playback measurement gated at ≥ 95%, a genuine playback fingerprint, the spouse's own orientation and quiz, and an `OrientationGateTest`.** No amount of copy fixes this — it needs the media | 5 | Open (2026-08-17) |
| R-51 | The consent record cannot evidence what was agreed | **Statutory — High** | IT Act 2000 §10A; T&C §3.I Step 9; DPDP 2023 §6; compliance sign-off C-06/C-08 2026-08-17 | `consents.doc_hash_sha256` exists to prove which text a person accepted. It is populated from `RegistrationService::DOCUMENT_HASHES` — four hardcoded hashes of Phase-1 stub strings, not of any published document — and the recorded version is `1.0.0` for all four while the live documents are `terms.md` v2026-05-21, `privacy.md` v2026-05-21 and `compensation.md` v2026-08-07. The applicant never sees the actual documents; they read a short summary in a scroll box. So the electronic record proves neither the text nor the version. This is also why C-08 cannot be rescued by "they accepted the T&C" — the recital about insolvency and moral turpitude is in a document the consent row cannot tie them to. **Closed 2026-08-17.** `ConsentDocuments` now hashes the **published body of the content page the public URL serves** and derives the version from that same page's date, so one source answers "which text, which version" instead of two that disagreed. The wizard links each consent box to the document it names rather than paraphrasing it in a scroll box, and the false `v1.0.0` labels are gone. The service **refuses** when a page is not published rather than falling back — a fallback would let registration proceed while writing a consent that points at nothing, which is this defect reintroduced as a silent default. DEC-05..08 | 4 | Closed (2026-08-17) |
| R-52 | Consent is published as revocable and cannot be revoked | **Statutory — High** | DPDP 2023 §6(4)–(6); privacy.md §10.5; compliance sign-off C-06 2026-08-17 | `privacy.md` states consent is "revocable" and that "you may withdraw it at any time". There is no `withdrawn_at` column on `consents`, no route, no service and no UI — zero code matches for consent withdrawal anywhere. DPDP requires withdrawal to be as easy as giving. Same defect class as R-47 and R-49: the published document promises a mechanism the platform does not have. Either build it — column, route, service, audit row, and a decision about what withdrawal means for an active distributorship — or amend the Privacy Policy to stop promising it. **Closed 2026-08-17 — built, not amended.** `consents.withdrawn_at` + `withdrawal_reason`, a `WithdrawConsent` service, and a page on the distributor's own profile reachable in one click with no approval queue: §6(5) requires withdrawal to be as easy as giving, and a queue would make it harder than the checkbox that granted it. Confirmed by typing WITHDRAW rather than ticking a box, because the consequence is closure of the ADN and a box beside a paragraph gets ticked without the paragraph being read. Closure is recorded as `consent_withdrawn`, not `admin_termination` — the company did not choose it and its name should not be on it. Acceptance rows are **marked, never deleted**: withdrawal does not invalidate prior processing, so the evidence that the earlier processing was lawful must survive. Deliberately not conflated with erasure, which has statutory limits the platform cannot override. CWD-01..08 | 4 | Closed (2026-08-17) |
| R-53 | The three statutory declarations are captured nowhere | **Statutory — High** | T&C §2.3; Indian Contract Act 1872; compliance sign-off C-08 2026-08-17 | Sound mind, not an undischarged insolvent, and no conviction for moral turpitude. The only occurrence anywhere in the codebase is a recital inside `terms.md`; "sound mind" appears in no view, no column, no migration and no test. There is no way to answer "which distributors declared this" from the database, which is the whole point of a declaration. **Closed 2026-08-17.** Three nullable boolean columns on `distributors` plus `declarations_made_at`, captured as three separate checkboxes at the consent step and validated with `accepted` (an unchecked box posts nothing, so a rule that only validates a present value would let a joiner through having declared none of them). Separate facts rather than one "I meet the eligibility criteria" flag, because each limb voids the agreement ab initio on its own and which one was false has to be answerable later. Nullable, not defaulted true: everyone who registered before the question existed was never asked, and the platform must not declare on their behalf. DEC-01..04 | 3 | Closed (2026-08-17) |
| R-54 | The eight-year retention policy is published and unenforced | Statutory — Medium | DPDP 2023 §8(7); terms.md §15; privacy.md; compliance sign-off C-05 2026-08-17 | Eight-year retention is stated in three published places and implemented in none. The platform has two purge commands — `contact-inquiries:purge` and `grievance:purge-expired` (the latter deliberately unscheduled, R-41) — and neither touches distributors, KYC documents, consents, orientation views or cooling-off events. DPDP requires erasure once the purpose is served; holding PII indefinitely while telling data principals it is held for eight years is a discrepancy in the wrong direction. Unlike coupons (R-18) and addresses (R-19) this had no risk row at all until now. **Measured and partly enforced 2026-08-17:** `RetentionPolicy` is now the single source for the periods — three prose documents that nothing read were always going to drift — and `compliance:retention-report` prints what is past its window, dry-run by default. It purges only the unambiguous categories (orientation views, cooling-off events). **Three categories are reported and deliberately not purged**, each needing a decision the command has no business making: deleting audit rows breaks the hash chain and would need it re-anchored; consents are the proof that earlier processing was lawful; and a distributor cascades into orders, the ledger, BV and payouts, which the Companies Act and Income-tax Act require to be kept. **Still open: what happens to a terminated distributor's record after eight years — for counsel, not a DELETE somebody writes on a Friday.** RET-01..06 | 3 | Open (2026-08-17) |
| R-55 | Every published legal document is an unreviewed draft | **Statutory — Critical** | DSR 2021; DPDP 2023 §5; compliance sign-off 2026-08-17 | Line 1 of `terms.md`, `privacy.md`, `ethics.md`, `grievance.md` and `compensation.md` carries `<!-- DRAFT — LEGAL REVIEW REQUIRED -->`. This is broader than the franchise opinion (R-24) and the §6.2 notices: the Direct Seller Agreement itself, the Privacy Policy — which **is** the DPDP §5 notice — the Code of Ethics and the Grievance Policy have never been to counsel. Also verify the content renderer strips HTML comments, or the draft admission ships in the public page source | 5 | Open (2026-08-17) |
| R-56 | The stub payment gateway marked every online order paid without collecting money | **Critical** | Hard rule 2; DSR 2021 Rule 5(1)(c); security audit T-6.1 finding C-1 2026-08-17 | `StubGateway::capture()` unconditionally set the intent to captured and called `markPaid()`, which sets `paid_at`, accrues BV, fires every compensation engine and consumes a consecutive GST invoice number. There is no real gateway, no webhook route and no signature verification anywhere in `app/Modules/Payments`. Anyone with a browser could manufacture arbitrary BV and downstream commission liability at zero cost, and a commission behind a sale where no consideration passed breaks hard rule 2 whatever the ledger shows. **Mitigated 2026-08-17:** the stub now throws unless the environment is local or testing, so it cannot run in production (PAY-G01…G03). **Still required before launch: a real gateway with server-side webhook signature verification, with `markPaid()` reachable only from a verified capture.** The mitigation stops the bleeding; it does not make the platform able to take money. **2026-08-30:** the guard now reads an allow-list (`PAYMENT_STUB_ENVIRONMENTS`, default `local,testing`) so staging can be named for client UAT; production is refused even if listed (PAY-G04…G06), and checkout hides "Pay online" and rejects the POST when no permitted gateway exists instead of throwing 500. **2026-09-04 — Razorpay integration built, flag OFF; partially mitigated, online path only.** Standard Checkout on `/shop/pay/{orderNo}`; `PaymentConfirmationService` is the single caller of `markPaid()` (architecture test `MarkPaidChokePointTest`) and confirms only from a payment fetched from Razorpay's API — never from the browser post or the webhook body — after asserting the intent's gateway order, captured, INR, exact payable, nothing refunded, order still placed, under row locks. The gateway resolver **fails closed**: flag ON with absent/malformed/wrong-mode credentials closes checkout (placement only; My Orders, returns and the cooling-off cancellation stay up) and never falls back to the stub, which additionally refuses whenever the flag is on or a live key is present. Mode is derived from the key prefix and stored on the intent (real money is distinguishable from test money). Webhooks: signature over the raw body, deduplicated on the event id, applied by a queued job that re-fetches. Backstops: `payments:reconcile` (5 min), `orders:expire-unpaid` (checks the gateway before cancelling; a late capture is auto-refunded and alerted). Zero-cash orders (points + repurchase credit cover the price) confirm server-side with the consideration re-derived from the ledgers and evidenced in the `order.paid` audit row; a 1–99 paise payable is taken to ₹1 at checkout. Payloads are allow-list scrubbed on write (no contact/email/VPA/cardholder name; acquirer refund references kept), no raw copy exists, and `payments:redact-events` drops payloads after 180 days (Privacy Policy retention row: non-material, same purpose and recipient already disclosed — no 30-day notice). Refunds: see R-60 (closed), R-68, R-69. **Stays Open until the live smoke test with `rzp_live_` credentials passes on production and the fail-closed behaviour is evidenced there** — `docs/runbooks/razorpay.md` go-live checklist. Also unmet on the ops side: named Grievance Officer (the 21-day refund escalation routes to the seeded placeholder until the launch gate closes it) | 5 | Open — partially mitigated (2026-09-04) |
| R-68 | Cash-on-delivery refunds are discharged by nothing automatic | Statutory — Medium | Hard rule 5; T&C §4, §8 | A refund on an order that was never paid through the gateway (COD, or a payment recorded outside the platform) creates the `liability.refund_payable` obligation and a warning in the payments log, but no RefundIntent: there is no gateway payment to refund against. The only discharge is finance recording a manual NEFT on the unsettled-refunds worklist — and today a COD refund does not appear on that worklist at all, because the worklist is built from refund intents. The buyer's statutory clock runs regardless. **Mitigated 2026-09-04 (same day):** the unsettled-refunds worklist now lists every `refund_approved` order with no RefundIntent under "Refunds owed outside the gateway", with the owed amount read from the order's `order.refund_approved` ledger entry and the 7-working-day clock; finance settles it against the order by NEFT (`RazorpayRefundService::settleOrderManually`, `finance.record`, audit `refund.manual_settlement` on the order), which discharges the payable against the settlement bank account and marks the order refunded (RRF-14, APT-11). A not-returned COD cooling-off return is forfeited through the same order-keyed path as a gateway one (RRS-06). COD is still not offered at checkout, so exposure remains nil; the residual is that the NEFT itself is a manual act with no automation, which is acceptable for a channel that is not switched on *Update 2026-09-23:* paid orders **cancelled** with no gateway payment (payable posted under `order.cancelled:{id}`) were on no worklist and could not be settled; they are now listed on the same worklist / badge / Action Center from that credit, discharged once by NEFT (`refund.manual.order:{id}` key + row lock), and the order stays cancelled. Covered by RRF-15/16, APT-13, BRS-09. | 5 | Mitigated (2026-09-04) |
| R-69 | Coupon value on a non-cooling-off refund is returned in no form | Contractual — Low | T&C §8 (buyback); `BuybackMatrix` voucher instrument; R-60 pattern | `RefundOrder` now subtracts the coupon discount for every refund reason (it was unwound only inside cooling-off, which refunded `discount − shipping` more cash than the buyer paid on a damage / dissatisfaction / buyback return — a discount converted to cash, the R-60 shape on a different lever). Refunding only what was paid is correct; but §8 arguably promises the buyer the *value* back in some form, and `BuybackMatrix` already has the voucher instrument for it. Policy question for the client: return the coupon's value as a voucher on non-cooling-off refunds, or not at all. Not built either way; tracked here so it does not resurface at launch sign-off | 5 | Open — policy decision (2026-09-04) |
| R-70 | Monthly bonus engines depend on a prerequisite the scheduler did not run | Statutory — High | Hard rule 2; DSR 2021 Rule 5(1)(c); DSA §6 (pay the plan as published); compliance review 2026-09-06 | `rank:check-qualifications` is the only writer of `rank_qualifications`; Rank Bonus, Growth Booster, Fortune and Lifetime Awards only READ those rows. It was `EngineCadence::unscheduled()` and appeared nowhere in `routes/console.php`, so the rows were written by a human. That was survivable while Rank Bonus ran on the 8th (about a week of slack); moving every monthly engine to the 1st (2026-09-05) cut the window to 30 overnight minutes. The scheduler does NOT resolve the dependency chain — only the admin trigger and the recompute replay do — so a missed check means Rank Bonus prices the pool from turnover, pays no RAP achiever, and still issues AO-GO grants against the whole Rank 1 pool while consuming a lifetime use: product sales that happened, commissions not paid, and grants that `alreadyGrantedThisMonth` will not re-issue. **Fixed 2026-09-06:** (i) `RankCheckCommand` scheduled `monthlyOn(1, '00:15')` with an explicit previous-month `--month`, its own default month and the registry `defaultPeriod` moved to `prev-month` so the command, the registry and the `engine_runs` row it writes all agree; (ii) all THREE readers of the table refuse with a non-zero exit unless a SUCCEEDED `rank.check` `EngineRun` exists for the month they read, via the shared `Support\RankQualificationsGate` (`--force` overrides for a deliberate replay). The months differ and the gate is one place so they cannot drift: Rank Bonus and Fortune enrolment for month M read M, Growth Booster for M reads **M-1** (`rejectRankedLastMonth`). Guarding Rank Bonus alone — the first cut of this fix, caught on re-audit — still left GBB at 00:45 crediting every distributor the plan bars (an empty exclusion list rejects nobody, and the inflated denominator dilutes the eligible) and Fortune enrolment at 01:00 seating rank 6–9 seniors in the capacity-capped 29,524-position FCFS matrix, permanently displacing eligible distributors for that month. The GBB premature-freeze self-heal does NOT cover this: a 00:45-on-the-1st freeze is after month close and is treated as the normal final row. Residual: the guard trusts `engine_runs`, so a check that succeeded against an empty ladder still opens the gate — correct, but it means the register, not the code, is the record that a zero-qualifier month was expected. **Superseded in part 2026-09-06 (same day):** the standalone `monthlyOn(1, '00:15')` entry for `rank:check-qualifications` has been REMOVED from `routes/console.php` and folded into `compensation:monthly-close` as step 2 of an eight-step ordered chain that aborts on the first non-zero exit. The dependency is now sequenced rather than merely gated — the check cannot be skipped by an overrun, because step 3 does not start until step 2 returns 0. `RankQualificationsGate` is retained as the backstop for a manually triggered engine. **Narrow waiver added 2026-09-08:** `gbb:monthly-run`'s M-1 prerequisite is also satisfied when M-1 has zero `group_bv_daily` rows AND zero `rank_qualifications` rows — nobody could have ranked with no Genos BV posted, so the exclusion set is provably empty; this exists solely because a full recompute replay's first monthly close (Aug for Jul) demands a check for June, which precedes any BV and is never scheduled, and cannot fire in production where every month has a scheduled check. | 4 | Closed — fixed (2026-09-06) |
| R-71 | A queue worker running pre-deploy code credited a bonus with no repurchase deduction | Statutory — High | Hard rule 2; DSR 2021 Rule 5(1)(c); DSA §6 (pay the plan as published); incident 2026-09-05 | A `queue:work` process loads its classes once and keeps them for its whole life, so a worker that booted before a deploy executes the OLD engine — silently, with no error anywhere. **Incident (local, 2026-09-05):** the `compensation` worker booted 2026-09-04 13:17 UTC; the deduction-at-credit-time change (e315301) landed 2026-09-05 13:04 IST. Admin-triggered engine runs execute in that worker, so an August Rank Bonus run credited **₹6,30,644.28 with zero repurchase deduction**. Nothing in `engine_runs`, the logs or the report pages showed a problem; it was caught only by re-reading the wallet ledger. The same worker also runs the full recompute, which would rebuild every bonus in history through the old engines. **Control (2026-09-06):** `Support\WorkerFreshness` compares the worker's boot time (`LARAVEL_START`) against the newest mtime under `app/`; a stale worker makes `RunEngineChainJob` record a `skipped` run with reason `stale_worker` and makes `RecomputeAllJob` throw before it takes its lock and wipes anything. It fails safe (refuses rather than proceeds) and is scoped to `runningInConsole()`, so web requests are unaffected. `app:deploy` already runs `queue:restart`; the guard is the backstop for a deploy that skips it, and for local Docker workers which nothing restarts automatically. Residual: only `app/` is watched — a change confined to `config/`, `routes/` or `vendor/` is not seen, so `queue:restart` on deploy remains the actual discipline. | 4 | Closed — mitigated (2026-09-06) |
| R-72 | A distributor who qualifies for a rank after the pool is frozen is refused that month's bonus | Statutory — High | Hard rule 2; DSR 2021 Rule 5(1)(c)/(d); DSA §6.1/§6.2 (pay the plan as published); compliance review 2026-09-06 | Rank Bonus previously recomputed its pool and its qualifier set live on every run, so a re-run could pay more than the pool — ₹8,000 out of a ₹6,000 pool in the worked case. The fix (2026-09-06) freezes both: `rank_monthly_pools` holds the month's economics and a `rank_bonus_results` roster row is written for every member at freeze, so pass 2 credits only members at the frozen point value. The **consequence** is the risk: a distributor whose `rank_qualifications` row lands after `freezeMonth()` is permanently refused that month and merely listed on an admin banner as "qualified after the pool was frozen". The trigger is not misuse — a payment captured and reconciled after 00:20 on the 1st, or any corrective re-run of `rank:check-qualifications`, produces a genuine post-freeze qualifier. Nothing at `/p/compensation` publishes a crystallisation rule for Rank Bonus, so the published plan promises payment and the engine withholds it; and the distributor cannot see the refusal at all. **Open decision (client + legal):** when does a month's Rank Bonus entitlement vest — on achievement, or on the freeze? Options put forward: (a) publish the crystallisation rule under a DSA §6.2 30-day notice; (b) carry late qualifiers into the following month's pool as a named carry-in; (c) keep the refusal and build a real remedy. The admin banner originally named "a manual credit" as the remedy — that was removed on review, because `manualCredit()` is GSB-only and `manual_credit` is swept by no payout batch (see R-74). Blocks flag-on for Rank Bonus. **Note (2026-09-07):** the repurchase forfeit-model spec (`docs/compensation/repurchase-client-examples-2026-09-07.md`) confirms Rank Bonus qualification now counts only compliant days' Genos BV and is never held by the repurchase cycle, but it does not touch pool-freeze timing — this risk is explicitly out of scope of that spec and remains open. | 4 | Open (2026-09-06) |
| R-73 | The monthly payout moved to the 8th while the published plan still says the 1st | Statutory — High | DSA §6.2 (names payout cadence as a plan-driving setting); DSR 2021 Rule 5(1)(d); same defect class as R-47 and R-49; compliance review 2026-09-06 | Crediting and payment were split on 2026-09-06 to create a correction buffer: the eight crediting engines run on the 1st via `compensation:monthly-close`, and the payout batch runs on the **8th at 04:00** via `compensation:monthly-payout-close`, which refuses unless every crediting engine for the month recorded a SUCCEEDED run. The buffer is the control — it is the week in which a bad month can be found and re-run before money leaves the company, and every defect in the 2026-09-06 audit would have been catchable inside it. 04:00 rather than 03:30 because the weekly GSB batch runs Tuesdays at 03:00 and would otherwise race on the income cap when the 8th falls on a Tuesday. **The risk:** the change swept every admin surface, help doc, feature-flag description and distributor help-tip, and left `/p/compensation` §9 publishing "a single monthly transfer the same morning at 03:30 IST" on the 1st — the published document and the mechanism disagree, which is exactly what R-47 and R-49 already condemn. The page is being amended in the same change. **Open decision (client + legal):** whether a payout-date move is a §6.2 material amendment requiring the 30-day notice. Reading is that it is, since §6.2 names payout cadence; if no monthly batch has ever paid a live distributor (R-56: the gateway is not live), no breach has occurred and this is an activation gate rather than a remediation, on the same reasoning as R-35 for MSB. Confirm before treating it as such. | 4 | Open (2026-09-06) |
| R-74 | `manual_credit` shows in the distributor's wallet but is swept by no payout batch | Statutory — High | Hard rule 2; DSR 2021 Rule 5(1)(c); DSA §6; compliance review 2026-09-06 | `WalletService::balancePaise()` counts `manual_credit`, so money credited that way appears to the distributor as owed. Neither batch sweeps it: the weekly batch takes `GROUP_A_TYPES` and the monthly batch takes groups B, C and D, and `manual_credit` is in none of them. The balance is therefore displayed as payable and never transferred to a bank — a commission earned against a product sale, acknowledged on screen, and not paid. Pre-existing, but the 2026-09-06 change made it load-bearing by proposing a manual credit as the remedy for a refused Rank qualifier (R-72); that sentence was removed on review, and `AdminManualControlsController::manualCredit()` is in any case GSB-only, requiring a `gsb_cutoff_result_id`. **Required:** add `manual_credit` to the monthly sweep set, or stop counting it in the displayed balance. Deliberately NOT done in the 2026-09-06 change — it alters how much money leaves the company and belongs in its own reviewed commit rather than riding along with an engine refactor. **Note (2026-09-12):** the Action Center's `payouts.bank_details_missing` provider now queries `wallet_ledger_entries` directly and deliberately excludes `manual_credit` from its payable-type whitelist, because no batch currently sweeps it. If this risk is ever closed by adding `manual_credit` to the monthly sweep set, that same commit must add it to this provider's whitelist too, or distributors holding only manual credits with no bank record will silently stop being surfaced. **Mitigated (2026-09-17) — the writer was removed, not the symptom.** `AdminManualControlsController::manualCredit()`, its route and its form partial are deleted, so no application code writes `manual_credit` today. The two choices this entry offered were both rejected on the same ground: the control had no maker-checker, no upper bound (`'amount' => ['required','numeric','min:1']`) and called `credit()` rather than `creditWithRepurchaseDeduction()`, so making it pay would have shipped a single-admin, unbounded, undeducted cash path — and leaving it displayed-but-unpaid was the defect itself. **This is mitigation, not elimination, on two counts.** (i) The type is still *writable*: `WalletService::credit()` accepts it (`WalletService.php:51`) and the guarantee rests on no caller existing — enforced from 2026-09-17 by a source scan in `CommissionHasProductSaleTest` that fails if a new writer appears under `app/Modules/**`. (ii) `balancePaise()` still counts the type, so any row already on disk keeps the original defect — displayed as owed, swept by no batch. Evidence at mitigation: **dev = 0 rows**; production does not exist yet; **staging is unverified** — it still ran the control, so `SELECT COUNT(*) FROM wallet_ledger_entries WHERE type='manual_credit'` must be run there before the next deploy. If the count is above zero, "reconciled" means one of three things, chosen and written down per row, not left implicit: sweep it (which requires closing this entry properly, sweep set and Action Center whitelist together), reverse it explicitly as a written-off correction, or stop counting the type in the displayed balance. Note for whoever runs it: staging has no live payout gateway (R-56), so rows found there are a display defect, not a withheld payment — do not escalate it as money owed. The ledger type stays in the enum for historical rows and stays exempt from hard rule 2 (`CommissionHasProductSaleTest`). **Any proposal to write this type again reopens this entry and must settle the sweep set AND the Action Center `payouts.bank_details_missing` whitelist first.** **Follow-up 2026-09-17 (same day): the one-off query is now a standing detector.** A one-off count somebody has to remember to run against each environment before a deploy is a reconciliation that eventually is not run, so `UnpayableManualCreditProvider` (`compensation.unpayable_manual_credit`, Action Center → Money, acting permission `finance.record`) lists every `manual_credit` row that exists, on every environment, with the distributor, the amount and the age. On a clean environment it shows nothing and is invisible — which is what dev looks like. It does not decide anything: each row still needs one of the three written choices above, and the provider's docblock names them so whoever opens it is not left guessing. **Evidence 2026-09-17:** dev re-confirmed at **0 rows**; production does not exist yet; **staging still unverified** — both the SSH database read and a remote `php artisan tinker` were refused by the engineering session's permission sandbox, and were not worked around. The count is now obtainable two ways, either of which closes this: deploy this commit to staging and read the Action Center, or run `SELECT COUNT(*), COALESCE(SUM(amount_paise),0) FROM wallet_ledger_entries WHERE type='manual_credit';` there by hand. Staging ran the deleted control for longer than any other environment, so it is the one place rows could exist. **Owner: the platform owner; due with the next staging deploy, and the result written into this entry — not "before the next deploy" in the abstract.** **Known gap in the detector:** `baseQuery()` filters on the type alone, so a row that has been decided and written off reappears once its snooze expires, and a permanent queue is a queue that gets ignored. Clearing it needs an observable "decided" marker — most likely an offsetting debit keyed `reference_type='wallet_ledger_entry'`, `reference_id=<credit id>`, which `scopeNotReversed()` cannot express because these rows share the sibling `gsb_credit`'s reference tuple. **Deliberately not invented here:** the marker defines what "reconciled" means on the ledger, and it should be chosen against real rows, not against a set that is empty on every environment checked so far. If the staging count comes back zero, the gap is moot and this provider stays silent forever. **Closed 2026-09-18 — the staging count came back zero.** `wallet_ledger_entries` holds **0 `manual_credit` rows, 0 paise** on staging, which is the only deployed environment; local dev is test data and production does not exist yet. No distributor anywhere is therefore shown a balance no batch will pay, the writer is deleted, and `CommissionHasProductSaleTest`'s source scan fails the build if a new one appears. **This closure carries a condition:** if a `manual_credit` writer is ever reintroduced, that same commit must add the type to the monthly sweep set AND to the Action Center's `payouts.bank_details_missing` payable-type whitelist — or distributors holding only manual credits with no bank record silently stop being surfaced — and the reconciliation marker described above must be chosen then, against real rows. | 4 | Closed (2026-09-18) |
| R-57 | Separation of duties was enforced on four routes and assumed everywhere else | **High** | R-17; DSR 2021; security audit T-6.1 finding H-1 2026-08-17 | The permission set was `placement.decide`, `finance.record`, `compliance.discipline` and `grievance.handle`. Every other admin route admitted the whole admin family, so `admin-finance` — the role that by design cannot freeze an account — could approve KYC submissions, view raw Aadhaar and PAN scans, cancel orders, change platform settings, and **set an arbitrary password on any distributor**, which is account takeover against a staff login that has no MFA. Nothing tested any of it. **Closed 2026-08-17:** added `kyc.review`, `commerce.order.manage`, `distributor.credentials`, `settings.write` and `audit.read`, gated the routes, hid the nav entries, and added `SeparationOfDutiesTest` (SOD-01…08) which asserts both the refusals and that each role can still do its own job. Staff MFA remains deferred to Phase 12 and is the compensating control this depends on | 4 | Mitigated (2026-08-17) |
| R-58 | Uploads are stored without virus scanning | High | Security audit T-6.1 finding H-4 2026-08-17 | **Closed 2026-09-17 — superseded by R-83, which is the accepted position.** This row's outstanding action was "run clamd and set `CLAMAV_HOST` in staging and production". The client has decided there is no malware-scanning feature: no scanner will be stood up in any environment. That instruction is therefore not pending, it is withdrawn, and leaving it on the register described a deployment task nobody was ever going to do. What this row did close stays closed: grievance upload paths now apply the same magic-byte validation as the KYC paths via `ValidUploadedDocumentBytes` (GRV-020/021), and the compensating controls hold — the bucket is private, files are never web-served, stream routes are RBAC-gated and audited, and signed URLs expire in 15 minutes. The residual — every uploaded file reaches storage unchecked — is carried by **R-83** and is accepted there | 3 | Closed (2026-09-17, superseded by R-83) |
| R-59 | Testing-only recompute + purchase-data reset can destroy statutory records outside production | Statutory — High | DSR 2021 Rule 5 (record-keeping, 8-year retention); T&C §4 + Hard Rule 5 (30-day cooling-off); T&C §8 (buyback); CGST Rule 46; Income Tax Act (TDS trail) | `compensation:recompute-all` and the admin "Reset purchase data" button (`POST admin/compensation/engine-runs/reset-purchase-data`) truncate `orders`, `order_cooling_off`, returns/buyback/refund intents, `ledger_tx`/`ledger_entries` and `payout_batches`. Four gates, all of which must open: never in production; `COMP_RECOMPUTE_ENABLED`; the CONNECTED database named in `COMP_RECOMPUTE_ALLOWED_DATABASES` (an APP_ENV label cannot prove a database is empty of real people, and staging holds real distributor PII); and the typed database name in the confirm modal. Every request writes `platform.purchase_reset.requested` to `audit_log` BEFORE the truncation, carrying the authenticated actor, the target database and the per-table row counts standing at the time — `audit_log` is not in the wipe list, so the record outlives the data. A partial engine selection additionally requires the operator to acknowledge that unselected engines' results are deleted and not rebuilt. Deletion gate: the entire scaffold — routes, controller actions, both Blade danger cards, `Services/Recompute/`, `RecomputeAllJob`, the config block and every `COMP_RECOMPUTE_ENABLED` line — is removed at client plan sign-off per `docs/runbooks/artisan-commands.md`; `GsbIdleCutoffBatch`, `DerivedTables::dateFilter()` and `engine_runs.duration_ms` are deliberately retained as production improvements. **2026-09-06:** the replay horizon now extends into the future (end of next month) for arrears catch-up and simulated runs, so a replay can stamp future-dated derived rows; the resolved window and its warnings are computed in `resolveWindow()` and are NOT yet written back to the `audit_log` row, which records what was requested rather than what ran. | 3 | Open (2026-08-25) |
| R-60 | Cooling-off refund returns the repurchase-wallet portion as cash | Statutory — High | Hard Rule 5 + T&C §4 (30-day cooling-off, full refund); T&C §11.2; double-entry integrity | **Closed 2026-09-04** (`6038ed7`, `0814916`, `03500e8`). Raised as an open live defect and parked by the business owner on 2026-08-29; fixed on 2026-09-04. The original defect: `RefundOrder::netRefundPaise` (`app/Modules/Returns/Services/RefundOrder.php`) subtracts `discount_paise` and `redeem_points_paise` but not the repurchase-wallet credit applied at checkout. `CheckoutService` writes `orders.subtotal_paise` as gross and `orders.total_paise` as gross − repurchaseCredit, and the refund derives from `subtotal`, so the buyer receives cash exceeding cash paid by exactly the credit. The `repurchase_wallet_used` debit is never reversed, so the ledger is also left unbalanced against a `liability.customer_prepayment` credited only with the net. This is the same abuse the adjacent comment forbids for redeem points: a non-withdrawable entitlement converted to cash via a cooling-off cancellation. Not flag-gated — the credit auto-applies for every logged-in distributor holding a balance. **Fixed.** `RefundOrder` now subtracts the credit actually applied to the order (`WalletService::repurchaseCreditAppliedToOrder`, clamped to the refundable gross) from `netRefundPaise`, so the buyer is never paid cash they did not hand over — the same treatment the adjacent comment already demanded for redeem points. The entitlement is **returned to the repurchase wallet, never as cash**: `WalletService::restoreRepurchaseCreditForOrder()` writes a fresh `repurchase_deduction` tied to the order, capped at what was actually spent and idempotent, so a retried refund cannot mint credit; the original usage row is left standing because the balance is deductions minus usages and deleting the usage would erase what was spent when. The ledger balances again: a `revenue.discounts` credit line matches the unreversed portion, and the same restoration runs on the **cancellation** path in `OrderStateMachine` (a cancelled order unwinds points and repurchase credit alike, and the prepayment reversal was corrected with it). `PayoutService` no longer counts a restored credit as collected, so a refund cannot make the month's repurchase deduction look satisfied. Regression tests in `RefundOrderTest`, `OrderCancelTest`, `CheckoutRepurchaseWalletTest` and `PayoutServiceTest` (65 green, 2026-09-04) lock the refund payable, the restored balance, idempotency on a retried refund, and the ledger balance. Related fix in the same batch: `repurchaseWalletBalancePaise()` takes a row lock, used by checkout, which previously let two concurrent checkouts by the same distributor both apply the full balance. **Residual, unchanged:** whether the repurchase wallet should be withdrawable at all is a plan question, not a defect — it stays non-withdrawable by design and is referred to counsel with R-37's wallet-zero gate at the batched launch sign-off. | 3 | Closed (2026-09-04) |
| R-61 | Group-BV propagation jobs have no overlap guard | Operational — Medium | DSR 2021 Rule 5(1)(c); Hard Rule 2 (commissions traceable to product sales) | `PropagateGroupBvJob` and `ReverseGroupBvJob` carry no `WithoutOverlapping`, `ShouldBeUnique` or cache lock. `RecomputeAllJob` and the engine chain are protected (`Cache::lock`, and `EngineRunService`'s per-engine lock at 7200s ≥ the 3600s timeout); these two are not. If the `compensation` queue is ever run with more than one worker, a job still executing past the connection's 90s `retry_after` is re-reserved and the same group BV is credited twice to the upline, inflating every downstream bonus. Currently mitigated only by Supervisord Job 3 being pinned to a single process and a do-not-raise note in `docs/runbooks/cloudways-deployment.md` §1.9 — documentation, not a code guard. Fix: add `WithoutOverlapping` keyed on `distributorId`. | 4 | Open (2026-08-29) |
| R-62 | ADC application documents are a new PII store with a bounded retention path only for rejections | DPDP §8(7) storage limitation — Medium | DPDP Act 2023 §8(7); T&C §15; compliance review 2026-08-30 | The Arete Development Centre application flow (2026-08-30) stores premises deeds / rent agreements, address-proof bills and premises photos on the private `adc` disk (S3 root `adc-applications`), streamed only through an audited, `adc.application.review`-gated route with 15-minute signed URLs. Rolled-back submissions delete what they uploaded. `adc:purge-rejected-documents` (daily 03:15 IST) erases the files of applications rejected more than 90 days earlier and audit-logs each purge; the application row is kept as the record of the decision. **Open:** approved applications keep their documents for the life of the centre with no defined erasure point after a centre is closed — needs a retention decision (suggest: 1 year after deactivation) and a matching purge branch. | 3 | Open (2026-08-30) |
| R-63 | Centre deactivation is an income-stopping action | DSR 2021 Rule 5 grievance duty; DSA §6.2 — Medium | Compliance review 2026-08-30 | Deactivating a distributor's Arete Development Centre ends its ADC bonus. Controls: the route carries `can:compliance.discipline` (finance cannot do it), a reason is mandatory and audit-logged, and the owner is emailed the reason with the grievance route (`AreteCenterDeactivatedNotification`). The applicant declaration (v2) states a minimum 30-day reasoned written notice before deactivation or transfer for non-development and the right to object via grievance. **Open:** the 30-day notice is a stated commitment, not a code gate — the admin action is immediate. Add a notice step (notice → 30 days → deactivate) before the flag is switched on in production. | 3 | Open (2026-08-30) |
| R-64 | No territorial de-duplication of centres | Operational — Low | Compliance review 2026-08-30 | `distance_to_nearest_adc_km` on an application is self-declared and editable by the applicant; nothing prevents two centres being approved on the same street. Reviewers see city, pincode and the registry filters, but the check is manual. Consequence is commercial (diluted member base per centre), not statutory. Consider a pincode-overlap warning on the review page. | 2 | Open (2026-08-30) |
| R-65 | Downline rank and personal BV visible to the sponsor, upline and admins | Data protection — Medium | DPDP Act 2023 §4–§6 (notice, purpose, minimisation), §8; Privacy Policy §4 5a / §7 / §13; DSA §8.5 / §16.2; Code of Ethics §2.11; Hard Rule 3 (as amended 2026-08-30) | **Decision:** on the client's instruction (2026-08-30, option 2 of three offered) the three earnings-adjacent rows on every Genos / Direct-referral card and in the Details popup — Highest Rank, Current Rank, Personal BV — may be shown to the distributor's sponsor, their placement upline and admins. Until then they were own-data-only. Not an income projection (each figure is a historical fact about that distributor; r.5(1)(d) untouched) but a disclosure of one Data Principal's purchase volume and title to others, so it needs notice, a stated purpose and a limit. **Controls in code:** (1) the switch `genealogy.downline_stats_visible` (developer-owned, default OFF) — merge is NOT go-live; (2) the guard lives in the single assembler `DistributorIdCardStats::compactMany()` and is enforced per node for every caller (tree canvas, Details popup, dashboard): own card always; sponsored + closure-descendant nodes only while ON; admins all nodes only while ON; anyone else masked — the earlier commit had deleted this guard, which also exposed the Details popup, both now closed; (3) negative tests pin sibling/cross-branch masking at service level and the Details popup while OFF (`TreeCardStatsTest`); (4) an on-canvas notice on the distributor tree while ON ("visible to you as their upline … do not share"); (5) Code of Ethics §2.11 prohibits republishing another distributor's rank/BV and using a downline's distance from a BV gate as a purchasing lever (¶4 pressure-selling); (6) personal-purchase title, withdrawal income, wallet and payouts remain own-only; (7) hard rule 3 amended by the user in CLAUDE.md to record the carve-out. **Paperwork:** Privacy Policy §4 5a + §7 recipient row + version v2026-08-30; DSA §8.5 + footer version bump. **Publication path:** `ProductionSeeder` never updates an existing content page — the amended Privacy Policy, DSA and Code of Ethics must be published through the admin Content Pages editor (or `db:seed --class=ContentPageSeeder` on staging, which `updateOrCreate`s) as a release step, and `ConsentDocuments` re-versions the DSA for all new joiners on republication — deliberate. **Accepted:** genealogy reads (tree, Details) are not audit-logged; viewing is read-only and takes no financial or disciplinary action, so no separation-of-duties impact; `/admin/tree` is open to all back-office roles. **Open before the switch goes ON:** issue the 30-day DPDP §5 / DSA §16.2 notice by email + in-app banner; publish the three pages; verify `/p/privacy`, `/p/terms`, `/p/ethics` on staging. Owner: Compliance Officer. Review at launch sign-off. | 3 | Open (2026-08-30) |
| R-66 | Reserved company accounts are compensation-plan participants (sponsorship edges seeded) | Statutory / governance — High | DSR 2021 r.5(1)(c)/(d); DSA §6.2; Hard rule 2 governance; Hard rule 6 (substance); IT Act §194H/§206AA; compliance review 2026-08-31 | **Decision (client, 2026-08-31):** the 31 reserved company accounts are permanent company accounts wanted in production, and each seeded account's sponsor is its direct parent node. `platform:reset` now writes the 30 `sponsorship` edges (root excluded — a self-edge would make the company root its own direct referral); staging + local dev were backfilled the same day with a guarded additive INSERT. **Consequence:** company-owned nodes now accrue Mentorship Bonus on their reserved children's GSB; since MSB point value = pool ÷ total points, company-held points lower the ₹/point paid to genuine distributors that day, and reserved accounts already accrue GSB/MB/GBB/RB (₹6,90,209.75 on the dev dataset). **Controls today:** payout lines stall only on `no_bank_account` (no bank on file — one admin edit away from release); the synthetic `ARVO…` PANs mean any TDS line would file against a non-PAN. One legal entity holding 31 sponsoring ADNs (hard-rule-6 substance) is noted here rather than as a separate entry. **Client decision (2026-08-31):** company accounts DO accrue bonuses by design — the company will assign each reserved account to a proven leader when one is found, so accrued balances are intended to travel with the account; the earlier proposed `PayoutService` accrual exclusion is therefore off the table. Payouts stay held while an account is company-held (the `no_bank_account` hold, since reserved accounts carry no bank), releasing only once the account is assigned to a real person with KYC, a real PAN and a bank on file. **Remaining actions:** (1) an account-assignment/transfer flow does not exist yet — when built it must replace the synthetic `ARVO…` PAN with the assignee's real PAN (TDS cannot file against a non-PAN) and enforce hard rule 6: one PAN = one ADN, so each reserved account must go to a *distinct* leader who holds no other ADN; (2) decide whether the MSB-denominator effect (company-held points lowering the ₹/point for genuine distributors) needs DSA §6.2 notice; (3) ~~ship the environment backfill idempotently~~ DONE 2026-08-31: `ProductionSeeder` now seeds the full 31-account block via the shared `SeedReservedTreeAction` (fresh install → build with edges; block present → heal missing `sponsorship` edges only; foreign tree → warn and skip), retiring the single `PROD_ROOT_*` ad-hoc root (ADN 111222333) in favour of the canonical reserved root 444555666. | Launch | Open (2026-08-31) |
| R-67 | A `bank_decrypt_failed` payout hold has no discovery path | Statutory / operational — High | DSR 2021 r.5(1)(c) grievance duty; DSA §6.2 payout cycle; compliance review 2026-09-01 | **Introduced 2026-09-01 (LOG-2).** `PayoutService::bankLast4ForDistributor()` previously swallowed a decryption failure and returned `null`, so a distributor whose `bank_account_enc` ciphertext was corrupt went into the NEFT file with a **blank account number**. It now raises `Log::critical` and throws `BankDecryptionException`; both `runWeeklyBatch` and `runMonthlyBatch` catch it per distributor and write a held `PayoutLineItem` (`STATUS_BANK_DECRYPT_FAILED`, `net_transferred_paise` 0). **Hard rule 2 unaffected** — no credit is created, the wallet is never debited or swept, and the balance pays out on the first batch after the bank details are re-captured; the fix is strictly better than the blank-account behaviour it replaces. **The gap:** unlike `no_bank_account`, which the distributor can self-fix, this is a *company-side* data-integrity failure the distributor can neither diagnose nor repair. The only alert is a `critical` line in the log file — there is no distributor notification and no admin dashboard surface (`app/Modules/Compensation/Notifications/` holds only the three Arete classes). Earned money can therefore sit withheld across many cycles with nobody aware. The wallet shows only "On hold — contact support". **Remaining actions:** (1) notify the distributor by email + in-app with the grievance route, mirroring `AreteCenterDeactivatedNotification`; (2) surface an open-`bank_decrypt_failed` count on the admin compensation dashboard, not only per batch; (3) define a retention period for `auth.login_failed` audit rows and confirm the privacy notice covers security logging of IP + identifier (raised in the same review; `audit_log` has no prune job). Until (1) and (2) ship, the hold is detectable only by an engineer reading logs. Owner: Compliance Officer. **Note (2026-09-12):** the Action Center's `payouts.bank_details_missing` provider was rewritten to a live payable-balance query, but it still only matches `bank_account_enc` being `NULL`, `''` or the `'stub'` placeholder — a genuine `bank_decrypt_failed` (non-empty, undecryptable ciphertext) is not `'stub'` and remains outside its `WHERE`, so remaining action (2) above is still open; this provider does not close it. **Remaining action (2) closed 2026-09-17:** `PayoutsBankUndecryptableProvider` (`payouts.bank_undecryptable`, Money, CRITICAL, `finance.record`) lists distributors with unswept payable income whose bank column is present but unreadable, so the hold is visible on the Action Center without anyone reading a log file. Its candidate set is disjoint from `payouts.bank_details_missing` by construction — that one wants the column absent, empty or `'stub'`; this one wants it present and none of those — so nobody is listed twice. Two arms, because the fault has two causes and only one is knowable before a payout runs: (a) the stored value is not one of our ciphertexts at all — a plaintext account number written in by a seeder or a raw SQL fix-up, which is the cause actually observed on dev — detected key-blind by the base64 prefix every `Illuminate\Encryption\Encrypter` payload begins with, and therefore caught days BEFORE the Tuesday it would hold; (b) the engine has already written a `bank_decrypt_failed` line on a batch that is not completed, which covers the well-formed-but-unreadable case (a `PII_ENCRYPTION_KEY` that does not match the ciphertext) that no SQL predicate can decide, caught the morning after. **Deliberately not a third arm:** trial-decrypting every candidate on page load would close (b)'s lag and would do it by pulling tens of thousands of bank ciphertexts into a web request; this provider identifies people by ADN and never SELECTs `bank_account_enc`, only tests its shape in `WHERE` (CLAUDE.md PII discipline, hard rule 8). The trade is a few days' notice on a fault whose held income is never swept and is paid in full by the first batch after it is fixed. Tests: `PayoutsBankUndecryptableProviderTest` (7 cases). **Remaining action (1) — notifying the distributor — is still open,** and so is (3). | 4 | Open (2026-09-01) — (2) closed 2026-09-17; (1) and (3) outstanding, review at launch sign-off |
| R-75 | Published weekly payout cadence changes from "every Tuesday" to "the Tuesday after the Wednesday–Tuesday earning week closes" | Statutory — High | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(d); repurchase forfeit-model spec 2026-09-07 | The repurchase forfeit-model spec (`docs/compensation/repurchase-client-examples-2026-09-07.md`, commits `329c5d8..056e830` on `fix/compensation-frozen-roster-and-monthly-close`) changes the weekly Group A payout rule: a batch dated Tuesday `T` now sweeps only entries earned on or before `T − 7` (`PayoutBatch::weeklyEarningWindow()` = `[T−13, T−7]`), i.e. a Wednesday-to-Tuesday earning week paid the following Tuesday, instead of sweeping every unswept entry as of the batch date. Admin and distributor copy already reads "Weekly income for each Wednesday-to-Tuesday earning week is paid on the following Tuesday." **Control:** the content file `app/database/seeders/content/compensation.md` has the new cadence sentence written but **NOT seeded** — a §6.2 material-amendment notice (30 days) must run before the seeder publishes it, per the same reasoning R-73 applies to the monthly payout-date move. **Owner:** client (to confirm the §6.2 notice timing and text). **Blocks:** production deploy of the payout-week copy (`ContentPageSeeder` re-seed of `compensation.md`) and, transitively, turning the repurchase feature flag on in any environment paying real distributors — the code already behaves per the new rule regardless of flag state for `earned_on` stamping, so the risk is disclosure, not mechanism. | 4 | Open (2026-09-07) |
| R-76 | A Rank Qualification Check that did not run can open the gate GBB and Fortune read | Statutory — High | DSR 2021 Rule 5(1)(c) (fair payment per the published plan); DSA §6; compliance review 2026-09-08 | `gbb:monthly-run` and `fortune:enroll-eligible` wait on `RankQualificationsGate::checkedFor()` because their exclusion sets (anyone ranked in M-1 for GBB; ranks 6–9 for Fortune) come from that check. A gate that opened on **any** `skipped` run — `already_running`, `upstream_failed`, `stale_worker` — would read an empty exclusion set and credit GBB to distributors the plan bars and enrol barred seniors into the capacity-capped FCFS Fortune matrix, permanently displacing juniors. **Control (2026-09-08):** the gate opens only on a `succeeded` run, or on a `skipped` run whose recorded reason is `feature_flag_off` **and only while the Rank Bonus flag is still off** (with the engine off nobody can hold a rank, so the empty set is the truth); every other skip keeps it shut (`RankQualificationsGateTest`). **Residual:** turning the Rank Bonus flag ON mid-month makes the month's check mandatory again before GBB/Fortune run — the close resumes at that step. | 4 | Mitigated (2026-09-08) |
| R-77 | A distributor's private message is a mis-selling surface no audit can reach | Statutory — High | DSR 2021 Rule 5(1)(d); Code IV.XII.a; Hard Rule 3; distributor-communications build 2026-09-09 | `PublicCopyAuditTest` scans Blade templates and structurally cannot scan what one distributor types to another. An upline promising earnings in a chat was previously invisible to the company: no report path, no moderation, no record. Mitigated by the report → `messaging.moderate` queue (R-17: not admin-finance), audience restriction to the sender's own Genos line (which removes the enumerate-and-pitch vector), and two rate limits. Residual and accepted: reporting is a member-initiated control, so an unreported claim between two consenting parties still reaches nobody. There is no message-content scanning and none is planned — scanning every private message is a DPDP proportionality problem of its own. | Compliance | **Mitigated** — reporting live; `messaging.reporting_enabled` and `messaging.audience` must both stay at their shipped values (ON / Restricted). Flipping the audience to `anyone` restores enumerate-and-pitch across the whole base, so either change needs a written risk acceptance, not just a settings edit. The DPDP limb of the moderation queue is tracked separately as R-80. |
| R-78 | An administrator's announcement reaches every distributor without passing a code review | Statutory — High | DSR 2021 Rule 5(1)(d); Code IV.XII.a; Hard Rule 3; distributor-communications build 2026-09-09 | Announcement bodies are company copy written in a textarea by staff, not in a template reviewed in a diff, and they reach the whole distributor base the moment they are published. `NoIncomeProjection` — which now owns the phrase list `PublicCopyAuditTest` reads, so the two cannot drift — refuses the copy at save AND again at publish (a draft can be saved clean and edited in another tab). Refusal, not a warning: a warning on a page someone is publishing is a warning they click past. Residual: the phrase list is a substring match and catches the wordings people actually write, not every possible one. The email copy (`announcements.email_copy`) is OFF by default because an emailed announcement cannot be withdrawn — archiving only reaches the in-app copy. | Compliance | **Mitigated** — flag OFF until an accountable author is named |
| R-79 | A public FAQ turns forty operational answers into published company statements | Statutory — Medium | DSR 2021 Rule 5(1)(d); DSA §6.2; distributor-communications build 2026-09-09 | `faq.members_only` ships ON, so the library is visible only after login — and FAQ entries are excluded from the generic `/p/{slug}` reader, which would otherwise have served every answer to the open internet regardless of that setting or of the feature flag (found in review, 2026-09-09). Every FAQ title and body is refused at save if it carries a phrase from `NoIncomeProjection`; the rule is scoped to this type because the Code of Ethics page quotes those same phrases in order to forbid them. Turning it off makes every answer a statement published to prospects, to which the same income-representation rules apply as to the landing page — and `resources/views/faq` is in the copy-audit roots for exactly that reason. Entries are `content_pages`, so they inherit the draft → published workflow and the audit trail. | Compliance | **Open** — do not turn `faq.members_only` OFF without a copy review of every published entry |
| R-80 | Staff can read a private conversation, and the communications tables have no natural end | Statutory — High | DPDP Act 2023 §4, §5, §8(7); Privacy Notice §4.5b, §5; distributor-communications build 2026-09-09 | Reading a member's private message is a real intrusion, so the mechanism is narrow by construction: only a message a recipient has reported, plus three either side for context, only for the two roles that handle grievances (never admin-finance), audit-logged on every open, never edited or deleted by staff, and with no bulk scanning of unreported messages anywhere in the design. The paperwork now matches the mechanism — Privacy Notice §4 carries purpose 5b naming the moderation and the three-message window, §5 carries retention rows for messages (24 months), reports (7 years), blocks (life of the block) and announcement reads (24 months), and `messages:purge` enforces the first and last of those weekly. The notice text lives in `database/seeders/content/privacy.md`, which is **not** what a visitor reads: it has to be republished on each environment for §4.5b and the new §5 rows to reach the published page. Use `php artisan content:publish privacy`, **not** `db:seed --class=ContentPageSeeder` — the seeder rewrites the body of every policy page at once, and `compensation.md` already carries the Wednesday-to-Tuesday payout-week wording that R-75 holds back until the DSA §6.2 30-day notice has run. Publishing all five to ship one would push an un-notified material plan amendment as a side effect. Each `content:publish` run is audit-logged. The sender is told on the compose screen, before typing, that what they send can be reported and read. Residual and open: (a) **the published Privacy Policy page in an environment must carry §4.5b before any report is opened in it** — that is the testable precondition, not the launch date. `MessagingFeature` and `messaging.reporting_enabled` both default ON, so if the republish step is missed, staff moderation runs against a purpose the company has not published. Check the live page, not the repo. Separately, if the notice had already been served to Distributors without §4.5b, the DSA §13 30-day material-change clock has to run first and `messaging.reporting_enabled` stays OFF until it has — a client decision, and a settings edit rather than a deploy; (b) `message_reports.message_id` and `messages.from_user_id` both cascade on delete, so honouring an erasure request by deleting a user row would destroy an open mis-selling report — the legal hold is documented in §5 and §10.3 but is not yet enforced in the schema. | Compliance | **Open** — (a) confirm the §13 position before launch; (b) enforce the legal hold in the schema before the first erasure request is honoured |
| R-81 | The hand that runs a payout batch was the same hand that signed it off | Operational / financial — High | CLAUDE.md “Separation of duties”; R-17; R-57; staging QA 2026-09-10 finding F94 | `approve`, `reconcile` and `retry` all sat behind the single `finance.record` permission, so `admin-finance` could build a payout batch, approve it, download the bank file and record the bank's response — the whole money path in one role, against a staff login with no MFA (Phase 12). `payout_batches` recorded `approved_by` but no `created_by`, so “the maker approved their own batch” was not even representable, let alone blocked. **Closed 2026-09-11:** new `finance.approve` permission held by `admin` and `developer` only — explicitly **not** `admin-finance` (which keeps making and settling) and not `admin-compliance`; `payout_batches.created_by` stamped on every creation path (payout page, Engine Runs manual run, and left NULL for the scheduler, which has no maker); the approve route refuses a batch whose `created_by` is the approver, hides the button, and writes `payout.batch.self_approval_refused`. Residual: a scheduler-built batch can be approved by any single approver — accepted, because the maker there is a cron job, and the amount is still re-derived and confirmed at approval. Tests: `PayoutAdminPagesTest` (4 cases). **Addendum 2026-09-15:** the Engine Runs page gained a “Retry this night” control for a failed nightly chain. A chain run includes the payout-batch engines, and `payoutsAllowed()` admits them whenever the night `isToday()` — which is the window a retry is most used in — so the button would have created a batch attributed to nobody (`created_by` NULL, the scheduler hole above) and then let the same admin approve it. Closed at source instead of relying on the stamp: `compensation:nightly-run --without-payouts` excludes the sweep entirely and the retry job always passes it, so **no admin-CONSOLE-initiated path creates a payout batch**. The maker remains the scheduler. Residual, pre-existing and not widened here: a human running `gsb:weekly-payout` or `compensation:nightly-run --with-payouts` from the CLI still writes `created_by` NULL, because neither `Auth::id()` nor an attributed `EngineRunContext` exists there — and `developer` holds both `finance.record` and `finance.approve`. The operator help names the CLI escape hatch and states that the person who runs it must not approve the batch; stamping a CLI maker is tracked separately. The retry still binds `EngineRunContext` to the acting admin, so the crediting runs it does perform are recorded `manual` + actor rather than as a cron run, and the audit row records `payouts_in_scope: false` plus a `chain_id` joining it to those runs. Tests: `NightlyRunCommandTest` (4 cases), `AdminEngineRunsControllerTest` (14 cases). **Addendum 2026-09-17 (supersedes the payout-scope half of the above):** `--without-payouts` closed the maker-checker hole but opened a payout one. Weekly batches are backfilled from the last batch that EXISTS, so a Tuesday with no predecessor — the platform's FIRST payout Tuesday — is reachable only on its own night; if that night failed, the retry credited wallets, the banner cleared, and no later night could ever build the batch, silently slipping the first payout a full week. The retry now passes `--weekly-payouts-only`: it rebuilds a missed weekly batch and never the monthly payout close (which self-heals on every night from the 8th, so it needs no admin hand and the largest sweep on the platform stays scheduler-only). Maker-checker holds on two independent layers rather than on exclusion: approval requires `finance.approve`, which `admin-finance` — the holder of `finance.record` that reaches the button — does not have; and for `admin`/`developer`, who hold both, the retry job binds `EngineRunContext` so `payout_batches.created_by` records the clicking admin and the self-approval refusal above blocks them. Fail-closed: an unattributed retry (`actorId === null`) gets `--without-payouts` instead, because a batch with a NULL maker is approvable by anyone; `--without-payouts` is also read first, so passing both switches excludes payouts. The audit row now records `payouts_in_scope: 'weekly_only'` (or `'none'`). **New residuals recorded 2026-09-17, all verified by compliance review that day:** (1) *A batch that started and did not finish is rebuilt by nothing.* `payoutBatchExists()` is status-agnostic, and `sweepWeeklyBatch()` creates the batch row BEFORE sweeping, so a first Tuesday killed INSIDE the sweep (OOM, the job's 3600s timeout, SIGKILL) leaves a `processing` row that the retry skips, every later scheduler night skips, and `gsb:weekly-payout` itself refuses because `processing` is in `CLOSED_BATCH_STATUSES` — the same silent slip this change closed for the never-started case. The two half-finished states differ and the operator help now separates them: `GsbWeeklyPayoutCommand`'s catch block flips `processing`→`failed` on any caught Throwable, and `failed` is NOT in `CLOSED_BATCH_STATUSES`, so the ordinary half-finished batch is fully recoverable by `gsb:weekly-payout --date=T` (maker-less, so a second person must approve); only a batch killed outright — SIGKILL, OOM, the job timeout — keeps `processing`, which is closed and needs a status correction before any re-run. Neither is proposed again by the chain, because `payoutBatchExists()` is status-agnostic. Not healed in code: including `failed` Tuesdays in the rebuild list would make a permanently-failing sweep abort the whole chain every night — the chain stops at its first failing step, so a stuck weekly batch would also block the monthly payout close — and re-entering `processing` is only safe if the sweep lock is known free, which is money-path surgery. Containment (the rest of the night still runs) is preferred over automatic repair. Mitigated by disclosure — the operator help and the failure banner both say the button does not cover it, and the help gives the per-state remedy — and by visibility: the batch sits on the Payouts page in `failed` or `processing`. **Residual (1) closed 2026-09-17 for the `processing` half:** `payout:reopen-stuck-batch` (CLI only, registered in `AppServiceProvider`) writes the status the crash could not — `processing` → `failed` — after which the ordinary re-run applies. Two independent guards, because being wrong means two writers on one batch: the sweep lock must be FREE (taken, never waited for, so a live sweep refuses outright) and nothing may have been written for the batch for twice the lock's TTL. **The liveness signal is the per-distributor line item, not `payout_batches.updated_at`** — compliance review 2026-09-17 rejected the first version, which read the batch row: that row is written exactly twice per sweep (`processing` at the start, totals at the end), so its `updated_at` dates the START of the sweep, and a hand-typed `gsb:weekly-payout` (no queue timeout; the lock self-expires at an hour) plausibly runs past two hours at ten lakh — both guards would have passed on a living sweep. **This makes the per-distributor `PayoutLineItem::create()` a load-bearing liveness signal: if that write is ever batched or deferred, this check must be replaced first, or a live sweep becomes invisible to it.** `PayoutBatchStuckProcessingProvider` reads the same expression and the same constant (`PayoutService::STUCK_BATCH_MIN_IDLE_SECONDS`, now public), so the tile cannot fire before the command would act. It moves no money and recomputes nothing; it writes one status plus a `payout.batch.reopened` audit row carrying the actor, both statuses, the idle seconds, the line-item count and whether a maker was stamped — **in one transaction**, so a batch cannot be reopened without the record of it. **The maker is recorded here, not by the re-run:** the re-run is a separate process with no session and no attributed `EngineRunContext`, so `batchCreatorId()` returns NULL there and a reopened scheduler batch would come back approvable by whoever reopened it — the hole this risk exists to close. `--actor` is therefore REQUIRED, it is stamped onto `created_by` when the batch has none, and it never overwrites an existing maker. Deliberately not a button: deciding a payout batch is owed again needs someone who has read the worker logs. Visibility was the other half of the gap and is closed too — `PayoutBatchStuckProcessingProvider` raises the batch in the Action Center (Money, critical) after two hours, because the night that died is cleared by the retry button and nothing else pointed at the residue. Tests: `PayoutServiceTest` (10 cases — the live-sweep-still-writing refusal, the quiet-line-items reopen, maker stamped / maker preserved, the audit-failure rollback, and the sweep re-entering a reopened batch), `PayoutBatchStuckProcessingProviderTest` (6 cases, incl. both sides of the heartbeat). The `failed` half is unchanged and remains a deliberate non-repair for the reason above. (2) *The CLI can still build a maker-less batch*, now by two switches rather than one: `--with-payouts` and the new `--weekly-payouts-only` both return true from `payoutsAllowed()` regardless of date, and a shell has neither `Auth::id()` nor an attributed `EngineRunContext`. The operator help names both and states that whoever runs them must not approve the result; enforcing it in code is tracked separately. (3) *Payout audit rows are now attributed*: `payout.batch.created`, `payout.batch.finalised` and `payout.income_cap_forfeited` stamped `Auth::id()`, which is NULL in a queue worker — so a retry-built batch would have named the admin on `payout_batches.created_by` while the record of the batch's creation, its finalisation and any permanent income-cap forfeit named nobody. All three now use `batchCreatorId()`, the same source as `created_by`. Tests: `NightlyRunCommandTest` (12 payout-scope cases incl. first-Tuesday backfill and both retry-job paths) and `PayoutAdminPagesTest`. The maker claim is pinned as TWO HALVES rather than end to end, deliberately: the acting admin's context surviving into the sweep step's `engine_runs` row (`NightlyRunCommandTest`, which stubs the steps so no batch is created) and context→`payout_batches.created_by` (`PayoutAdminPagesTest`, “stamps the admin who ran the batch as its maker”). Both halves read the same scoped `EngineRunContext`, so the composition is not an independent failure mode. `AdminEngineRunsControllerTest` (14 cases) **Addendum (2026-09-18, ADR-0016 — supersedes the retry-scope halves above):** `--without-payouts`, `--weekly-payouts-only`, `RetryNightlyChainJob` and the `retry-chain` route are deleted; the admin console has no retry or rebuild control of any kind, for any role — the maker-checker questions those switches existed to answer no longer arise there. In their place: a **developer-only** rebuild (`compensation:rebuild-night/-week/-month/-payout`, R-102), which stamps the rebuilding developer as `created_by` on any batch it re-creates (D14) and the existing self-approval refusal then bars their own approval — the same mechanism this row built, reused rather than re-invented. A hand-typed `gsb:weekly-payout` or `compensation:monthly-run` from the CLI still has no maker; that residual is unchanged by this addendum. **S3 holds this row rather than closing anything new**: it creates, for the first time on a console-reachable path, a batch with a human maker who then cannot approve it — exactly the shape R-81 was raised against, now correctly barred rather than newly exposed. **Pre-launch checklist condition (`compliance-officer`, S3 review):** at least two distinct humans must hold `finance.approve`, one of whom is not the developer who runs rebuilds — the same two-humans-or-nothing prerequisite R-92's GSB reversal maker-checker already carries (see R-92, R-91 §14b). On a single shared super-staff login this bar is not maker-checker, it is approval being impossible; track this alongside the other named-officer and two-super-staff-accounts pre-launch items. | Compliance | **Closed (2026-09-11)** |
| R-82 | The payout export now carries full bank account numbers out of the platform | Statutory (DPDP) — Medium | DPDP Act 2023 §8(4)–(5); CLAUDE.md hard rule 8; staging QA 2026-09-10 findings F44 / F95; client decision Q5 (2026-09-11) | The manual-NEFT export used to emit only `Bank Last 4`, which the bank cannot execute — so finance was told to "upload it to the bank" a file that was in fact a reconciliation sheet. On the client's instruction it is now a real bank-upload file: full account number, IFSC and beneficiary name, decrypted at download time. That makes the download itself a disclosure of every payee's bank details. Controls: the route requires `finance.record` (not every admin role) **and** an approved batch; the file is generated on demand and never written to disk or to any log; the account number appears in no audit row, no flash and no application log; each download writes `payout.batch.bank_file_exported` with the actor, the batch, the line count and a SHA-256 (raw bytes in `after_hash`) of the exact file handed over, so a leaked file can be matched to the download that produced it; a ciphertext that will not open exports as a `bank_decrypt_failed` line with the account and IFSC blank rather than a silent gap. Residual (accepted): a finance user with a legitimate reason to download can retain the file outside the platform — inherent to paying by bank upload; the digest makes it attributable after the fact. Tests: `PayoutAdminPagesTest` (export contents + audit digest + decrypt-failure line). **Amended 2026-09-24 (user decision, R-108 branch): the file is now kept.** Every NEFT file downloaded and every bank response file uploaded is stored so finance can later prove what the bank was told and what it answered, and compare imports. This replaces "never written to disk" above. New controls: PiiCrypter ciphertext on the PII key, on the private `payout-bank-files` disk (no URL ever issued), written *before* the file is handed over and refused outright when storage is down (fail-closed); downloaded only through the `finance.record` route, each download audited (`payout.bank_file.downloaded`, digest of the bytes, no contents); the parsed rows kept for comparisons hold ADN, status, UTR, amount and the bank's reason, never the account number, IFSC or name (the bank's free-text reason can still carry an account number — same exposure as `failure_reason`); erased by `payout:purge-expired-bank-files` (daily 03:40 IST) after the admin-owned `payout.bank_file_retention_days`, default 2,920 days — the books-of-account period (Companies Act 2013 §128(5)); the registry floor of 365 days is deliberate, since the parsed rows survive a purge and still support the books. Views of the compare and all-imports pages are not audited (ADN/UTR/reason only). **New residual: one encrypted store of every payee's bank details, under one key**, for up to eight years. **Launch gate:** the Privacy Policy / DPO checklist must state the eight-year retention of payout bank details. Tests: `PayoutBankFileHistoryTest` | Compliance | **Open (2026-09-24)** — stored bank files; Privacy Policy retention line due before launch; review with R-104 |
| R-83 | No malware scanning: an administrator opens distributor uploads that nothing has checked | Security / operational — High | T-6.1 finding H-4; R-58; staging QA 2026-09-10 finding F74; client decisions Q20 (2026-09-11) and 2026-09-17 | **Accepted permanently by the client — there is no malware-scanning feature.** Staging originally refused every upload because no ClamAV daemon exists — fail-closed, and correct, but it blocked Distributor Requests, grievance evidence and KYC re-upload. The client's decision is that no environment will run a scanner and that an upload must never be blocked by a missing one, so `uploads.malware_scan` (env `CLAMAV_ENABLED`) turns the scan off. The scanning code stays in place and defaults to ON — an environment that never heard of the switch, and any value other than an explicit `false`, still refuses — so the switch is the feature's off position, not dead code, and a future decision to scan needs a config change rather than a rebuild. What is accepted is the residual: every file a distributor uploads reaches storage unchecked, and the person who opens it is a compliance officer or a KYC reviewer the platform asked to open it. Compensating controls: uploads are stored on a private disk, served only through authenticated controllers, never executed, and byte-checked against their claimed type by `ValidUploadedDocumentBytes`. Every unscanned file writes an `uploads.scan_skipped` warning carrying the uploader, the field, the size and a SHA-256 of the bytes, so an incident can ask which files were never scanned and match them by digest — and so "we accept unscanned uploads" stays a number somebody can count. Tests: `MalwareScanningTest` AVS-07 (switch off accepts and logs), AVS-08 (absent switch still refuses). | Compliance | **Accepted by the client (2026-09-11, reaffirmed 2026-09-17)** — no Phase 12 revisit; the decision is that the feature does not exist. **Outstanding control, owner needed:** the acceptance's only remaining mitigation is telling staff the files are unscanned, and that is an untracked verbal briefing today. Make it a real control — a banner on the document-stream view plus a line in `resources/help/kyc-review-guide.md` and `grievance-handling.md` — otherwise a permanent High acceptance rests on nothing a reader can verify |
| R-84 | Admin BV-ledger export memory ceiling | Operational — Low | CSV→XLSX export migration S6b decision (2026-09-12); compliance review 2026-09-12 | `AdminBvLedgerController::export()`/`exportShow()` previously called `->get()` on unbounded ledger queries. S6b (client decision: lazy/cursor iteration) replaced this with `cursor()`-backed generators, removing Eloquent model hydration and the in-memory CSV/XLSX document from the export path — a real, large reduction. Residual: the mysql PDO connection still has `MYSQL_ATTR_USE_BUFFERED_QUERY` on, so the driver itself buffers the full result set before `cursor()` can start yielding rows, meaning a multi-million-row export still carries a memory ceiling from buffering alone, not an unbounded-safe stream. Compensating controls: both endpoints are date-range scoped and behind `can:audit.read`; the summary tab is bounded by distinct distributor count, not ledger-entry count. | Compliance | Open — revisit if the ledger passes roughly 1M rows, or before disabling buffered queries on this connection (an ADR-sized decision affecting every query, not scoped to this export) |
| R-85 | Simulated compensation figures standing in a test environment, and two open questions on the month-end repurchase-wallet rule | Operational / statutory (DSR 5(1)(d)) — Medium | Staging bug 14 Sep 2026; F125 (12 Sep 2026); ADR-0014; client decisions 2026-09-05 and 2026-09-07 | **(a) Projected figures.** A dev/staging recompute can replay the scheduler's calendar past today (`--horizon=today|projection`), producing bonuses, cycles, wallet balances and payout batches for instants that have not arrived. Those are ordinary rows: read aloud in a demo they would be an income projection, which hard rule 3 forbids anywhere. Controls: the horizon is recorded on the run's audit row (CLI runs record the shell account and host, since there is no session); `RecomputeState` reads it and **every page** — admin, distributor, storefront, registration wizard — carries an amber banner naming what was simulated and through when; the two standalone printable pages (ID card, profile stats) carry it outside `.no-print` so it survives Save-as-PDF; **every report download** through `ReportExport::respond()` is renamed `PROJECTED-<date>-…` and carries a first row saying so, which covers the two distributor-facing exports (GSB history, wallet ledger); **approving a payout batch and building a bank NEFT file are refused outright** while a projection stands, so a forecast can never become an instruction to move money; the scheduled compensation engines pause while it stands — including `payout:auto-retry-failed`, the only one that reaches a bank — and that pause fails closed if the state cannot be read; a nightly 23:30 IST reset returns the environment to production-faithful; and the whole mechanism is inert in production, where `RecomputeGuard` refuses. Residual (accepted): someone screenshotting a projected page, or quoting a figure read off one, during the window it stands. **(b) Month-end wallet rule, two client questions still open:** (1) a GSB/MSB repurchase deduction credited on the last days of a month counts against that month's wallet gate with ≤ 1 day left to spend it; (2) since the 2026-09-07 change one unspent balance is judged against two deadlines — the repurchase cycle's due date and the calendar month end — which the 09-06 drop migration warned "punishes one unspent balance twice". Neither is changed by ADR-0014; both need a client decision. | Compliance | Open — (a) mitigated by the banner + pause + nightly reset; (b) awaiting client answers |
| R-86 | Supplier cost and company margin readable outside the roles that need it | Operational — High | CLAUDE.md "Separation of duties"; R-17; R-57; profit-on-sales build 2026-09-15 | The Profit on Sales report puts landed cost, COGS and margin per product on one page, and the landing-price history puts the whole supplier cost trail on another. Controls: a dedicated `profit.report.view` permission held by `admin-operations` and `admin-finance` only — deliberately **not** `admin-compliance`, which polices conduct and has no duty needing the company's margin; every one of the four profit routes is asserted role-by-role (not merely the first) by `ProfitReportTest`; the landing-price-history route is gated on `inventory.view` rather than inheriting the catalog group's wider role list; the nav item is rendered only for holders of the permission; every export writes `profit.report.exported` with the actor and the filters. Residual and open: the product edit form still shows `cost_price` and `landing_price` to every admin role, including admin-compliance — pre-existing, not created by this build, and the next thing to close in this area. | Compliance | Open — close the product-form cost exposure |
| R-87 | A scope-aware profit service with scope-blind company figures | Statutory — High (Hard Rule 3) | DSR 2021 Rule 5(1)(d); CLAUDE.md hard rule 3; profit-on-sales build 2026-09-15 | `ProfitReportService::tradingAccount()` accepts a `SalesScope`, but purchases, opening/closing stock valuation, GST input and total commission outflow are company figures that cannot be narrowed to one distributor. A distributor-scoped caller would therefore have been handed the company's supplier spend and whole-book commission under their own name — own-data-only, broken structurally rather than by a missing `where`. Mitigated 2026-09-15: `tradingAccount()` throws unless the scope is `SalesScope::all()`, so the mistake is not reachable; the row-level reads (`byProduct`, `lineRegister`) do honour the scope and are tested against a distributor with nothing attributed. No distributor-facing route reaches this controller today. | Compliance | **Mitigated (2026-09-15)** — a distributor-facing profit view needs its own method, never this one |
| R-88 | A cut-off proven done by result rows alone | Statutory — High (Hard Rule 2) | DSR 2021 Rule 5(1)(c); compliance review of the nightly chain, 2026-09-15 | `EngineStatusService::computedCutoffDatesBetween()` counts a day as cut off if ANY `gsb_cutoff_results` row exists for it, or if a succeeded run is stamped inside its own day. The cut-off commits per distributor, so a run that died half way through leaves exactly that shape, and an admin retry at noon cannot have seen the evening's sales. Anything that decides whether a month may be FROZEN on such a day would price the month short, permanently — a re-run reuses the frozen pool rather than repairing it. Mitigated 2026-09-15: `completedCutoffDatesBetween()` proves a day from the run log alone, with the same period-end rule as `hasSucceededRun()`, and it is what the nightly chain's backfill frontier, its month-close gate and `MonthlyCloseCommand::preflight()` all read. Residual and open: `EngineChainResolver`'s month-wide dependency expansion still reads the looser method — correct for "has this day been started, so a dependency may proceed", and to be re-checked if it ever gates a freeze. | Compensation | Open — the freeze paths are closed; the dependency resolver still reads the looser proof |
| R-89 | A compensation outage longer than the chain may heal | Statutory — High (Hard Rule 2) | DSR 2021 Rule 5(1)(c); compliance review of the nightly chain, 2026-09-15 | `compensation:nightly-run` backfills at most 31 missed nights; a longer gap is deliberately left to a human, because replaying a month of engines unattended at 00:05 is a decision nobody made. Nothing else can see such a gap: `EngineHealthService::missing()` judges each engine on its most recent fire, and once last night succeeds the earlier days produce no signal at all — days of product-sale-driven commission never computed, with no record that it happened. Mitigated 2026-09-15: the chain writes a `compensation.nightly_run_backfill_gap` audit row naming the range and the last day it can prove, and the health digest reports it for seven days alongside a night the scheduler skipped and a month the chain would not close. Residual: the healing itself is still a human action. **Addendum (2026-09-18, ADR-0016):** the backfill and its 31-night cap are now specific to the Nightly Run only — the Weekly Run's self-heal reaches back one Tuesday at a time and the Monthly Run's one-month lookback (D8) is bounded by design, so neither shares this row's 31-night cap or its gap-reporting mechanism; a weekly or monthly gap that outruns its own self-heal is a separate, narrower risk than this row describes and is not yet given its own number. | Compensation | Mitigated (2026-09-15) — reported; the catch-up remains manual |
| R-90 | Engine throughput at ten lakh distributors | Operational — High | Nightly-chain plan, 2026-09-15; ADR-0015; `docs/testing/scale/2026-09-15-engine-benchmark.md` | The whole compensation night is one process by design — the crediting engines share a repurchase cap summed non-atomically, so they cannot safely run concurrently (ADR-0015). **Measured 15 Sep 2026 at ten lakh distributors** on a synthetic population (21.6M closure rows, average depth 21.6, 1.58M paid orders, BV propagated up each placement chain). **The platform did not reach ten lakh before that run: four unbounded `whereIn($ids)` calls built one SQL placeholder per distributor against MySQL's 65,535 limit and threw at roughly 65,000** — in `RepurchaseEvaluateCommand::withPossibleCycle()`, `BvLedgerService::warmPersonalBvCache()`, `IncomeEligibilityService::warmCycleCache()` and `GsbIdleCutoffBatch::partition()`. Each was an exception that would have aborted the night, not a slow query; all four are now chunked at 500 and verified semantics-preserving by compliance review. Memory was the second ceiling: both nightly commands materialised every active distributor as a model, 485 MB at 100k growing linearly (~4.8 GB projected). Both now `chunkById()` at 2,000, warming and forgetting per chunk: **58 MB and 138 MB at ten lakh** against a 512 MB container limit. Residual, and the reason this stays open: the nightly chain finishes in **52 minutes** at ten lakh, of which **2,825s (90%) is `repurchase:evaluate` doing 6,004,508 queries — six per distributor, every night, with no `warmBatch()`** — and it is scaling **superlinearly** (0.42s per thousand at 1k, 1.97s at 100k, 2.83s at 1M; 14.4× wall clock for 10× population). The night has about an hour of headroom today and would not keep it at twenty lakh. Not yet measured: the credit tail (wallet write, repurchase deduction, admin charge, TDS) and the monthly close. **`RepurchaseCycleService::warmBatch()` shipped 2026-09-17**, mirroring `GsbCutoffService::warmBatch()`: `RepurchaseEvaluateCommand` warms each 2,000-distributor chunk, evaluates it and forgets it. Five caches — newest cycle (consumed on read, so a rolled or resolved cycle is re-read rather than replayed), highest qualified rank, the 600-BV anchor (the running-total ledger walk `firstReachedBvPaiseAt()` does per distributor, done once for the chunk), and per-distributor-per-day self-purchase BV and repurchase-wallet movement, grouped by day so row count is bounded by days transacted on rather than window length. Every cache is a strict accelerator with a live fallback keyed on the warmed window, so a distributor the warm missed, a window it does not cover, or a second evaluation of the same distributor all get the same numbers at the old cost — the property that makes a cache in front of a money path safe. Two corrections from compliance review 2026-09-17: `warmBatch()` now calls `forgetBatch()` as its first statement (two warms without a forget kept the first chunk's covered-id set while the second overwrote the window bounds, so a first-chunk distributor could pass the coverage check against a map holding none of their rows — under-counted repurchase BV, and a cycle wrongly suspended for income actually earned), and the cold cycle read gained the same `orderByDesc('id')` tie-break the warm uses, so two cycles opened on one day cannot make warmed and cold pick different rows. The `warmBatch()` call also moved inside the command's `try`, so a throw in the warm still reaches the `finally`. Measured on the test database: a steady-state night (every distributor holding an open cycle, the common case) fell from **120 queries for 60 distributors to 4**, the four being the chunk's own warm; a chunk opening first cycles fell from 6.0 to 3.1 queries per distributor, the remainder being the cycle INSERT and UPDATE that cannot be batched. Verdict-equivalence is pinned by a cold-vs-warm test over a population covering every branch `evaluate()` has — below-anchor, open window, on-time resolution, late fulfilment and ranked obligation — comparing every persisted column of every cycle. Tests: `RepurchaseCycleServiceTest` (5 cases, incl. the cross-chunk window-leak case). Not re-benchmarked at ten lakh: the query reduction is measured, the wall-clock claim is not, and the superlinear term is unprofiled. **Addendum (2026-09-18, ADR-0016):** the throughput measured here is the Nightly Run alone (repurchase evaluation + cut-off); the Weekly and Monthly runs are now separate processes at 03:00/04:00 and have not been benchmarked at scale in their own right — the sweep lock they share (`compensation:payout-sweep`) is a new serialisation point this row's figures do not cover. | Compensation | Open — `warmBatch()` shipped 2026-09-17 and not yet re-benchmarked at scale; the superlinear term is still unprofiled. **Addendum (2026-09-17):** the GSB out-of-order guard added that day runs one `exists()` per distributor on the engine path (`GsbCutoffService::computeForDistributor()`). Small beside the work `warmBatch()` already saved (120→4 queries) and the engine path is a minority of the roster, but it is per-distributor and belongs in the chunk warm-up — fold it in when `warmBatch()` is next revisited rather than as its own change. |
| R-91 | A GSB day that failed is unrecoverable in production once the nightly chain has run past it | Statutory — High | DSR 2021 Rule 5(1)(c)/(d); DSA §6 (pay the plan as published); ADR-0014; ADR-0015; compliance review 2026-09-17 | The carry-forward store is rolling, so a cut-off date can only be replayed while nothing later has advanced it — `GsbCutoffService::computeForDistributor()` refuses otherwise, and from 2026-09-17 that refusal correctly also covers a retry whose failed row was deleted. **The window is about one nightly cycle, not "sometimes".** `GsbIdleCutoffBatch::write()` inserts a `no_match` row for every idle distributor each night and `no_match` advances the store, so the next chain run at 00:05 IST closes the door for essentially the whole roster. After that: Manual Controls → Retry throws, the Engine Runs GSB backfill throws for the same distributors, and `compensation:recompute-all` refuses in production unconditionally by design (ADR-0014, `RecomputeGuard`). A distributor genuinely owed that day's GSB is therefore withheld with **no self-service and no documented production remedy** — `docs/runbooks/engine-failure-triage.md` §14 terminates at the word "escalate" without naming what the platform team actually does, who may do it, or within what time. This is not a regression: before 2026-09-17 the same retry silently produced *wrong* figures instead of refusing, which was worse. The refusal is right; the missing remedy behind it is the risk. **Two mechanisms found on 2026-09-17 while writing the procedure, both of which make this worse than first recorded.** (i) **The stranded BV, not just the bonus.** `GsbCutoffService` reads exactly one day's `group_bv_daily` row (`GsbCutoffService.php:162`) and it is the only engine reader of that table; the next night reads its own date plus the carry-forward store. A night that never settles therefore strands that day's Genos BV outside the store permanently — unmatched, never carried forward, never picked up. The distributor loses the day's match *and* that BV's contribution to every later match. (ii) **The chain never comes back for it.** `EngineStatusService::computedCutoffDatesBetween()` counts a date as computed if *any* `gsb_cutoff_results` row exists for it — a `failed` row counts, and so does a `no_match` row written for an unrelated distributor by `GsbIdleCutoffBatch`. So the backfill that genuinely repairs a night the chain never started does **not** repair a night the chain ran and failed part-way through. The admin help said "a failed night is not a lost night" without that distinction; corrected 2026-09-17. **Delivered 2026-09-17:** (a) **drafted, not signed off** — compliance review the same day found the first draft would have **double-paid** any affected day whose credit had already been swept to a bank (the replay writes a new result-row id, so the new `gsb_credit` carries a reference the old reversal does not mask, is unswept, and falls inside the next Tuesday's window), and that it restated GSB alone while GSB's `repurchase_deduction_paise` feeds the wallet-zero gates on Fortune, Growth Booster, Rank requalification and AO-GO. Both are corrected in the text — an already-paid check is now a gate before the replay rather than a footnote, and step 4 diffs the monthly engines with a crossed-month decision reserved to the authoriser — along with four smaller errors: the reversal scope had to widen to `credited` **or** `reversed` (a previously-reversed row inside the range short-circuits the replay and staleness propagates), the day-before carry-forward columns have to be captured before the delete because `gsb_carryforwards` keeps no history, the rehearsal needs its own restore rather than the rebuilt clone it was meant to verify against, and the verification needed a guard in the other direction because a replayed date recomputes every other distributor's `no_match` row against *today's* personal BV and can credit someone nobody authorised. **"Rehearsed" is still not true of anything** — no one has run it end to end, and until they have, (a) is a draft. The procedure now reads: §14a–d of `docs/runbooks/engine-failure-triage.md` carries a numbered production rebuild — measure the loss first and drop the zero-BV distributors, two-role authorisation (platform owner + a `finance.approve` holder who is not the operator), snapshot, restore into a `RecomputeGuard` allow-listed clone, rebuild there, diff, rehearse, then apply in the one order that works — `reverseBonusCredit()`, **delete** the result rows (reversal alone is not enough: `computeForDistributor()` short-circuits both `credited` and `reversed` as `ALREADY_SETTLED`), restate the carry-forward, and replay in date order through the ordinary Engine Runs trigger so the engine writes its own `gsb_credit` rows through the swept, product-sale-chained path — no credit is ever written by hand. Verification-or-rollback, clone destruction and distributor notification close it. Three cases are named as out of its reach: money already settled to a bank (a gate before the replay, not a footnote — and one with no remedy behind it, see R-92's open gap), a rebuild range that crosses a closed month, because GSB feeds the wallet-zero gates on Fortune, Growth Booster, Rank requalification and AO-GO while the procedure restates GSB alone, and a wrong `group_bv_daily` row. The procedure also records that step 2 copies real PII into a lower environment and is a DPDP processing event. (b) done — the 00:05 IST deadline is now stated on the Retry form itself and in `resources/help/compensation.md`, together with what a missed day actually costs. **Still open, and still gating flag-on:** (a) a signed-off, actually-rehearsed run; (c) the client/legal decision; and the two names. **Open decision (client + legal):** a GSB day earned against product sales, refused by the guard and not recovered, is a day the published plan promised and the platform did not pay — is that a DSA §6 breach, and if so what is owed: the day's computed bonus, the compounded effect of the stranded BV on later matches, or a goodwill figure? The procedure deliberately leaves "rebuild or carry it as a correction" to the client, but only with the measured figure in hand and the distributor told either way; silently leaving it is not one of the options. **Named-authoriser gate:** §14b names two *roles*; the rebuild is not authorised until real names sit behind them, tracked with the other named-officer placeholders (Grievance Officer, DPO, Nodal Officer). One engineer holding both credentials does not satisfy it. Possible narrowing, not yet done: an idle `no_match` advance is numerically a no-op, so the guard could compare the stored carry-forward instead of merely counting later rows — it would not help the common case, where the distributor whose night failed has real BV and a genuine advance the next night. **Delivered 2026-09-18 (ADR-0016):** `compensation:rebuild-night` is now the sanctioned same-day remedy for a failed night — developer only, previewed, audited — and the R-91 window this row describes is a first-class refusal inside it (D11): once any cut-off row exists for a later date, including an idle `no_match` row, the rebuild refuses with the same message the out-of-order guard already gives. This closes the "no documented production remedy" gap for a night still inside its window; it does **not** touch a night a later cut-off has already passed — that case is still exclusively §14's clone procedure below, unrehearsed, and the stranded-BV and never-comes-back mechanisms this row documents are unchanged for it. | 4 | Open (2026-09-17) |
| R-92 | One administrator can permanently reverse earned commission, with no second approval and no way back | Operational / financial — High | CLAUDE.md "Separation of duties"; DSR 2021 Rule 5(1)(c); DSA §6; R-17; R-81; compliance review 2026-09-17 | `AdminManualControlsController::reverseCredit()` writes a debit equal to the full net GSB for a date and flips the result row to `reversed`. A single holder of `compliance.discipline` can do it, and nothing undoes it: `computeForDistributor()` short-circuits a `reversed` row as `OUTCOME_ALREADY_SETTLED`, so Retry cannot restore the credit, and Manual GSB Credit — the only control that previously *looked* like a way back — was deleted on 2026-09-17 because it credited money no payout batch ever swept (R-74). Removing it took away an illusory remedy, not a real one, but it does leave the reversal with no in-app counterpart. Compensating controls today: the `compliance.discipline` gate, a mandatory 10-character reason, and an `audit_log` row with before/after wallet and repurchase-wallet digests. The confirmation copy states the finality plainly (`_form-reverse.blade.php`). **Mitigated (2026-09-17) — maker-checker built, rather than the risk accepted.** `reverseCredit()` no longer reverses anything: it writes a pending `gsb_reversal_requests` row and an audit entry, and moves no money. A second admin approves it at `manual-controls/reversals/{id}/approve`, and only that approval runs the ledger work. **Both halves of the separation are enforced**, the same shape as the payout-batch approval (QA F94): the approve route sits behind a new `compensation.reversal.approve` permission which is SUPER_ONLY — so `admin-compliance`, the role that raises the request, does not hold it and two compliance admins cannot sign off for each other — and on top of that, a holder of both permissions may not approve a request they raised themselves, checked against `gsb_reversal_requests.requested_by` and recorded as `compensation.gsb.reversal_self_approval_refused`. Three further properties are load-bearing rather than decorative. (i) **The request snapshots the figure.** `net_gsb_paise` and `repurchase_deduction_paise` are stored as the requester saw them and compared against the live row at approval; a drift refuses the approval (`ReversalRequestStale`) instead of reversing a different amount than the one authorised. (ii) **The checker is told.** A `GsbReversalAwaitingApprovalProvider` puts pending requests in the Action Center's Money group under the checker permission — maker-checker whose checker is never notified is maker-checker in name only. (iii) **Rejection and withdrawal are first-class**, both requiring a written note, so a request that should not proceed is closed on the record rather than left pending. Seven tests cover the flow and the three pre-existing reversal tests now drive it end to end through two admins. **What this does not change:** the reversal is still terminal — `computeForDistributor()` short-circuits a `reversed` row as `OUTCOME_ALREADY_SETTLED`, so nothing restores the credit, and that is deliberate (see R-91 §14c, where the production rebuild has to *delete* the row rather than reverse it). **Money that has already left is refused, not warned about.** The first cut of this control relied on prose telling the approver to check the payout status; compliance review called that correctly — there was no control, and maker-checker had widened the window. Approval now reads `swept_by_payout_batch_id` on the credit's ledger entry and refuses outright (`ReversalCreditAlreadyPaid`), because a wallet debit does not recall a transfer and would otherwise net an undisclosed clawback against future earnings. **Open, and it is a real gap:** a credit paid out against a fraudulent or cancelled order still has to be recoverable, and there is no authorised path for that today — the refusal names finance as the escalation without one existing. The DSA also does not publish whether a negative wallet netted against future bonuses is a permitted clawback at all, which has to be settled before any such path is built. Approval additionally refuses on projected figures, the same guard the payout approval has (`RefusesProjectedFigures`, lifted into a shared concern rather than copied). **Launch prerequisite, easy to miss:** `Gate::before` answers true for every super staff account (`AppServiceProvider.php:140`), and `compensation.reversal.approve` is SUPER_ONLY — so for the roles that can approve at all, the whole separation reduces to the `requested_by` self-check. On a platform running one shared super-staff login this control is not maker-checker, it is *reversal is impossible*. **Two distinct super-staff accounts, held by two different people**, belongs on the named-officer pre-launch checklist alongside R-91 §14b. **Deploy note:** the new permission is provisioned by `RolesAndPermissionsSeeder`, which `ProductionSeeder` calls and `app:deploy` runs — so a deploy through the documented path needs nothing extra. A staging update done as a bare `git_pull` does not run it, and because `Gate::before` lets super staff through every gate, the missing permission would not visibly break anything until a scoped role tried to approve. Run the seeder after any pull-only deploy. | 4 | Mitigated (2026-09-17) |

| R-93 | The Arete Development Centre bonus can pay cash while the published plan says it is not payable and discloses no terms | **Statutory — Critical** | DSR 2021 Rule 5(1)(d); DSA §6.2, §12; compliance review 2026-09-17 | Published `/p/compensation` §11 lists the ADC Bonus among components that are "not operative, and none is payable, until it is published on this page with an effective date" — yet **§8 (Deductions) of the same page already lists ADC bonuses among those the 3% administrative charge applies to**, so the document contradicts itself, and the ADC Bonus has **no published section at all**: no rate, no base, no cap, no formula, which is precisely what §6.2 requires to be published. Meanwhile the engine is built and one developer-role toggle turns it on with no code change: `AdcBonusRunCommand` gates on the flag, `AreteDevelopmentCenterBonusService` writes an `adc_credit` wallet credit, and `PayoutService` sweeps `adc_credit` as Group D gross in the monthly batch — real cash. `AreteDevelopmentCenterBonusFeature::resolve()` returns `false`, but Pennant's store is `database`, so the resolver default is only what applies until somebody toggles it: **on the dev database the flag is already `true`**. **No active breach — confirmed by the client 2026-09-17: there are no real distributors on any environment, local and staging hold test accounts only, and there is no production environment at all — the server has not been stood up.** So the flag being `true` has misdescribed nothing to a real person, and the staging flag state is no longer the question that decides this row. It stays **Critical as a launch gate**, not as a live incident: the defect is in the published plan, it is exactly the kind of thing that is easy to carry past launch unnoticed, and the moment one real distributor exists with the flag on, this becomes a Rule 5(1)(d) breach with no further code change. The same contradiction covers **Growth Booster and Rank Bonus**, both named in that §8 table while §11 declares them non-operative. It does *not* extend to Lifetime Awards (absent from the table, and §8 states non-cash awards carry no administrative charge) or to Fortune (in the table, but published at §7 with its status at §7.6). Lifetime Awards carries the lesser version of the problem: built, flag `true` on dev, declared non-operative, no published section. **Launch gate — required before the first real distributor is registered on any environment where these flags are on:** publish an ADC section carrying rate, base, cap and formula with an effective date following the §6.2 notice, and fix the §8-versus-§11 contradiction in the same edit. The same gate covers Growth Booster and Rank Bonus | Compliance | Open (2026-09-17) |
| R-94 | A buyer who chooses collection is still charged a delivery fee | Operational — High | CPA 2019 s.2(47) (unfair trade practice); compliance review 2026-09-17 | `CheckoutService::place()` computes `$shippingPaise = $this->shipping->feePaise($subtotalPaise)` with **no collection branch** and adds it to the order total; `ShippingService::feePaise()` returns the configured fee (₹60 by default) for any cart below the free-shipping threshold. The checkout JS `setDelivery('collect')` hides the address block but does not zero the fee. So a buyer who elects to collect from an Arete centre pays for a delivery that never happens — charging for a service not rendered. Live on every environment, not feature-flag gated, and not covered by R-47's wording. Fix: zero the fee when the delivery type is collection, or apply R-47's remedy and withdraw the collection option until fulfilment is wired. **Closed 2026-09-18 — fixed, and by neither of those two routes.** The client chose to wire the fulfilment rather than withdraw the picker, and a collection is now charged a `commerce.collection_fee_rupees` handling fee **instead of** the delivery fee, defaulting to ₹0. Zeroing the delivery fee would have been the smaller change but the wrong shape: it left no way to price a collection later without a deploy, and it would have folded two different revenue lines into `shipping_paise`, which `ProfitReportService` reports as “shipping collected” and `RefundOrder` refunds on cooling-off. The free-shipping threshold deliberately does **not** waive a collection fee: that threshold buys off a delivery cost and there is no delivery. The fee follows the same refund rule as delivery — returned inside cooling-off, kept outside it. Checkout renders one server-costed summary per delivery method and the toggle only changes which is visible, so the figure on screen cannot drift from the figure charged | 2 | **Closed (2026-09-18)** |
| R-95 | Some Arete centres are live with no declaration on file, and there is no way to record a re-acceptance | Statutory — High | DSR 2021 Rule 4; DSA §5.1, §5.2; hard rule 7 evidence; compliance review 2026-09-18 | Found while ruling on R-21. Two separate defects, both of which undermine the same evidence. **(i) No re-acceptance is possible.** Declarations are keyed to an *application*: `arete_center_application_declarations` carries `application_id` with a unique index `uniq_acadecl_app_key` on `(application_id, declaration_key)`, and `AreteCenterApplicationService::recordDeclarations()` stamps `AreteCenterDeclarations::VERSION` once at acceptance. The versioning is right in itself — it exists so a reword never rewrites what was actually agreed — but it means a **new** version cannot be recorded against a centre that already exists. Bumping the constant changes only what future applicants sign. **(ii) Admin-created centres have no declaration in any version.** `AdminAreteCenterController::store()` creates an `AreteCenter` directly at `STATUS_ACTIVE` with no application, so no declaration row is ever written. For those centres the evidence R-21's closure rested on does not exist at all, in v2 or otherwise. **Required:** a per-**centre** declarations table (`center_id`, key, version, `accepted_at`, `ip`); a hard gate that no centre receives a first consignment until the current version is recorded against that centre; and admin-created centres blocked from consignment until their assigned distributor accepts. Not urgent while the ADC flag is off and no parcel has ever been routed to a centre — it becomes blocking the moment collection fulfilment ships. Tracked with R-21 and R-47. **Mechanism built 2026-09-18** (095eee97): `arete_center_declarations` is keyed on `center_id`, backfilled from what applicants actually accepted (their own version, timestamp and IP; nothing invented for centres that never had an application), and `DispatchService` refuses to consign to a centre that has not accepted every declaration at the version in force. An admin-created centre therefore cannot receive a parcel until its assigned distributor accepts. **Closed 2026-09-18.** Both defects are now addressed. **(i) Re-acceptance now exists.** `AreteCenterDeclarationService` records acceptance per centre at the version in force, through two deliberately separate entry points: `acceptByOwner()` for a centre assigned to a distributor, and `acceptForCompanyCentre()` for a company-run centre that has no distributor to sign. Neither can be pointed at the other kind of centre. That split is the substance, not ceremony: the declaration is the signature the dispatch gate reads as evidence, so an admin able to produce it on a distributor's behalf would be manufacturing the evidence rather than collecting it. The owner accepts from **My Arete Development Centre**, where outstanding declarations appear above everything else with the acceptance recorded against date, time and IP; staff accept for a company centre from **Admin → Arete Centres → Edit**, gated on `compliance.discipline` so admin-finance cannot sign. Acceptance is idempotent and writes an `arete_center.declarations_accepted` audit row naming who signed and in which capacity. **(ii) Blocked centres are now visible.** The registry list carries a *Declarations pending* badge, so staff do not have to attempt a dispatch to discover a centre is blocked. Note the asymmetry that remains by design: a blocked centre is still selectable by a buyer at checkout — the block is on consigning the parcel, not on choosing the centre. Verified in the browser on dev: with v3 in force all three centres showed *Declarations pending*; accepting for the company centre cleared its badge and left both distributor centres pending, with no admin path to sign for either. **That last claim was too strong and is corrected here:** it was true of the *accept* route and false of the *edit* route. Reassigning a centre carried the previous operator's signature forward, and impersonation let staff produce an owner-signed row directly. Both are fixed — see R-21 for the detail and R-98 for the standing impersonation risk — and the mechanism now reads only live, non-superseded acceptances | 7 | **Closed (2026-09-18, after remediation — see R-21)** |
| R-96 | Declaration v4 — five defects in the v3 text, batched rather than patched | Statutory — Medium | Compliance review 2026-09-18; DPDP §8(2), §8(5)-(7); DSA §16.1/§16.2 | Five findings against the v3 wording, deliberately **not** shipped as edits to v3, because v3 is already signed and editing signed text is the exact failure mode `AreteCenterDeclarations::SUPERSEDED` exists to prevent. One further version bump, one further re-acceptance, done once. **(i) Retention** — `buyer_data_duty` ties erasure to arovolife asking or the centre closing, both of which arovolife controls; DPDP §8(7) wants erasure when the purpose is served, which here is the moment of handover. **(ii) Processor duties** — the declaration is the §8(2) processor contract and carries no security-safeguards duty and, more seriously, **no duty to report a personal-data breach to arovolife**, which leaves the company unable to perform its own §8(6) notification. **(iii) A duty the platform will not let the operator perform** — "I will record every handover on the platform on the day it happens", when there is no centre-operator surface and `CollectionHandoverService` is reachable only from the admin order screen; a term the signatory cannot perform is weak evidence and an unfair term. Reword to "report to arovolife the same day" until the surface ships. **(iv) `buyer_data_duty` cites no clause** while every sibling declaration is anchored; it should cite DSA §9 / the Privacy Policy and Code of Ethics §2.11. **(v) `contact_consent` is a DPDP consent bundled all-or-nothing with contractual undertakings** (§6(1)); pre-existing from v2 and arguably service-necessary, but worth splitting while the text is open | 7 | Open (2026-09-18) |
| R-97 | Collection is live at checkout with no published DPDP notice for the disclosure it causes | Statutory — High | DPDP §5, §8(2); Privacy Policy §13; compliance review 2026-09-18 | The buyer's **name and mobile number** reach a distributor-operated address the moment a centre can receive a parcel. `Consignee::forCollectionAt()` puts both on the consignment, and `CheckoutService` deliberately retains `ship_name` / `ship_phone_e164` on collection orders so it can. The privacy amendment covering this (purpose §4 4a and the §7 recipients row) is written but **unpublished**, and §13 requires **30 days' advance notice** for a new recipient and a new purpose — 4a is both and says so itself. Nothing in code binds the collection option to that period: `AreteCenter::collectionChoicesFor()` is a plain active-centre query with no flag and no date check. The checkout copy tells the buyer the order will be held at the centre and says nothing about their details going to the distributor who runs it, so there is no §5 notice at the point of choice either. **This company's own pattern is the opposite** — `genealogy.downline_stats_visible` and the GSB pool flag both stay OFF until their notice periods run. **Required:** gate the collection picker behind a setting that stays OFF until `content:publish privacy` has run and 30 days have elapsed, and add one sentence at checkout naming the disclosure. Until then the exposure is a live data disclosure, not a paperwork gap. **Remediated 2026-09-18 (client decision: manual switch, default OFF).** `commerce.collection_at_centre_enabled` is developer-owned and ships OFF, carrying the same `impact` wording as `genealogy.downline_stats_visible`: do not enable until the privacy amendment is published and the 30-day notice has elapsed. The check sits in `AreteCenter::collectionChoicesFor()`, which closes both surfaces at once — the checkout picker reads it and so does the `Rule::in` validating a submitted centre — and it **fails closed**, so an unreadable settings table means no disclosure rather than a silent one. Deliberately **not** applied to dispatch: an order already placed for collection still ships and hands over normally, so turning the switch off never strands a parcel mid-journey; a test asserts exactly that. The §5 notice at the point of choice is now on the checkout picker itself, naming what is shared, with whom, for what, what is not shared, and that home delivery is the alternative. Verified in the browser on dev: with the switch off, Delivery method offers Home delivery only. **Residual, and the reason this row stays open:** the 30 days are enforced by the person flipping the switch, not by the code. The client chose that knowingly over an auto-gate computed from the publish date | 4 | Open (2026-09-18, gated same day; closes when the notice is published and the period has run) |
| R-98 | Impersonation is a full session swap with no read-only mode | Security — High | Compliance review 2026-09-18; separation of duties | `AdminImpersonationController::start()` calls `Auth::login($target)` and regenerates the session, refusing only other super-staff as targets. There is no read-only mode and no middleware blocking state-changing POSTs, so a super-staff admin acting as a distributor can take **any** action that distributor could take, and the resulting rows carry the distributor's own user id. Only a timestamp correlation against `admin.impersonate.start` would ever reveal it. Found because it defeated the declaration-acceptance split (R-21, R-95); **that one route is now blocked** while `impersonator_id` is in session, but the general capability is unchanged and every other state-changing route is still reachable. **Required before launch:** either a read-only impersonation mode, or an explicit allow-list of routes reachable while impersonating, plus stamping the impersonator onto every row written during an impersonated session | 7 | Open (2026-09-18) |
| R-99 | Returned goods the ledger has no record of shipping | Operational — Medium | T&C §8; DSR 2021 Rule 12; compliance review 2026-09-18 | `restockReturn()` restocked a return straight from the order's lines with no check that the units had ever left the warehouse, so an order reaching a terminal state without a pack created stock out of nothing — dev carries exactly that (`ORD-260904-JSCXZO`, refunded, never packed; two variants sat at on-hand 501 against a true 500). **Fixed 2026-09-18, and the first attempt at the fix was wrong in the way that matters most here.** Capping the restock at the ledger's `sale_out` reads "no movement" as "never shipped", and `CogsResolver` names three routine ways a *shipped* order has no movement behind it: the `packForShipment` bail-out, `no_track` variants, and orders older than the ledger. With `InventoryFeature` off in every environment the first of those is a designed path, not an anomaly — so the cap would have refused physically received goods entry to the book, understating on-hand and leaving off-book units with no batch or expiry record. Trading invented stock for destroyed information is the worse trade, because the second kind is unrecoverable. The discriminator is now the order's own `shipped_at`, which `markShipped` stamps whether or not the pack succeeded: the ledger bounds the return wherever it picked anything, a shipped order it has no record of picking is reconciled to the inspector who is holding the goods (the movement's reason names the missing pack), and only a line on an order that never shipped restocks nothing. Every outcome — restocked, capped or skipped — lands on a `return.restocked` audit row carrying requested vs restocked vs basis. **Residual, and why this stays open: (i)** a genuine never-shipped return means a buyer may have been refunded for goods that never left, which is an ops and possible non-delivery signal; it currently reaches a log line and an audit row, not the statutory grievance register the way `RefundWorklist` routes its equivalent (DSR Rule 12). **(ii)** Cooling-off returns never restock at all — `OpenReturn` refunds directly and `InspectReturn` refuses them — so the goods from the statutory 30-day return have never entered the ledger in any version. Neither residual touches money: buyback amounts are computed from `BuybackMatrix` into `BuybackDecision` on a path with no Inventory coupling, so no refund depends on whether stock went back on the shelf | 7 | Open (2026-09-18) |
| R-100 | The Action Center discloses sensitive-category grievance counts to staff who may not see the tickets | Statutory / privacy — Medium | DPDP 2023 §§4-6; Code of Ethics §2.11; R-17 | `GrievanceSlaDueOrBreachedProvider::baseQuery()` and its third-party-overdue sibling apply **no category filter**, unlike `AdminGrievanceController::applyVisibility()` and `AdminGrievanceReportController::hiddenCategoriesFor()`, which both exclude `TicketCategory::sensitiveValues()` from staff without `compliance.discipline`. So the Action Center's breached-SLA row counts ethics and privacy tickets for every holder of `grievance.handle`, which includes `admin-operations` — a count of complaints they are not cleared to open. **Pre-existing since the Action Center shipped 2026-09-12; found 2026-09-18 during the admin-dashboard review**, which surfaced it by writing the control down in `resources/help/dashboard.md` as though it already existed. It is a count only: no ticket number, subject or party is exposed, and the dashboard change *reduced* the landing-page disclosure from the old card's ticket numbers to a bare count. **Closed 2026-09-18** by the option the client chose: filter the count, and give compliance its own row. `GrievanceSlaDueOrBreachedProvider` and `GrievanceThirdPartyOverdueProvider` now exclude `TicketCategory::sensitiveValues()`, and `GrievanceSensitiveSlaDueOrBreachedProvider` / `GrievanceSensitiveThirdPartyOverdueProvider` extend them with the complementary scope and `compliance.discipline` as their `permission()`. The two halves are disjoint and exhaustive, so no breached **statutory** clock is hidden from everyone — it moves to the desk cleared to open the ticket. The split is enforced by `ActionCenterRegistry::for()`'s ordinary permission filter, not by viewer checks inside the providers, and the sensitive rows inherit the clock rather than restating it so the pairs cannot drift. **The same review found the disclosure surviving on a second surface** and it is fixed in the same change: the sidebar Grievances badge counted every unsettled ticket with no category filter, while the queue's own "Open (N)" tab is filtered — so an operations officer could recover the sensitive count by subtracting one number from the other. `resources/views/admin/layouts/admin.blade.php` now applies `applyVisibility()`'s rule to the badge, under **two** cache keys (`admin.grievances.unsettled_count.general` / `.all`); the single global key it used before would have served whichever total was computed first to every viewer, which is worse than not caching. Covered by `AdminRoleSeparationTest::R17-grievance-badge`. **Caveat:** the Action Center half of this is dark in practice until `ActionCenterFeature` is switched on and the sidebar item stops being developer-only (`AdminNavigation.php`) — the general row was equally unreachable before, so this is not a regression, but "it moves to the desk cleared to open the ticket" is true of the code and not yet of any live screen. `resources/help/dashboard.md` and `docs/runbooks/action-center.md` describe the split | 7 | **Closed (2026-09-18)** |
| R-101 | The three relaxations that let a month be closed and paid at all: the launch month, a month with no sales, and what "every owed day was cut off" now means | Statutory / operational — Medium | ADR-0016 (D1, D1b, D2, D13); compliance review 2026-09-18 (A1, A6, A7); DSR 2021 Rule 5(1)(c); hard rule 2 | Three deliberate relaxations ship together with the engine-cadence split, each one narrowing a refusal that would otherwise make a month permanently unclosable — and each one worth stating plainly, because they decide when money is computed. **(1) The launch month (D2).** `MonthCutoffCoverage` no longer demands a daily cut-off for every calendar day of a month: days before the platform's first distributor (`PlatformStart::firstDay()`, `MIN(distributors.effective_date)`) are not owed one, because a day with nobody in the tree has nobody to match, no group BV to fold and no carry-forward to advance. **The bound is exact and is the whole of the mitigation: any day on which even one distributor existed is still owed a completed cut-off**, so a stretch of quiet days inside the platform's life — which is also what a broken order feed looks like — still defers the close and raises the alert. The relaxation cannot shorten a month that had distributors in it. **(2) A month with no product sales (D1).** `MonthlyEngineCompletionGate::owedNoCrediting()` waves such a month past the crediting-completion gate, so its monthly payout batch is still built. That batch is created `pending`, has no `created_by` (nobody made it; which is R-81's known residual for a batch with no maker (a scheduler-built batch has no human maker to bar; a hand-typed one should have, and does not)), and **pays nobody — unless earlier months left unswept monthly credits**, which the sweep window `WalletLedgerEntry::earnedForMonthOrBefore()` deliberately collects: a credit held back for KYC, a missing bank account or the 3,000 BV bar is paid by the first batch built after the hold clears. No credit is invented by this path and hard rule 2 is untouched — every rupee it sweeps is already a wallet credit chained to a `product_sale_id`. **A7 (2026-09-18) closes the one way this could have paid something it should not have:** the monthly run no longer builds month M's batch while month M−1's own batch is deferred on an incomplete crediting engine, because M's sweep would collect M−1's half-computed credits and the rest of M−1 would then be credited into a month nothing sweeps again; M is recorded as its own deferral naming M−1, and the two are built oldest-first once M−1 is fixed. **(3) What counts as a completed cut-off (D13, A1, A6).** A day is proven by the run log alone — a succeeded `gsb.daily-cutoff` run that started after that day had ended — and the LATEST finished attempt decides, so a night that was wiped by a rebuild and then failed reads "not computed" and the month is not closed over rows that no longer exist. Two amendments keep that rule from refusing what it cannot fix: **A1** — the out-of-order refusal (`CutoffReplayedOutOfOrder`, R-91: the carry-forward store is rolling and a passed day cannot be rebuilt) is recorded `skipped`, not `failed`, because it is an ordering decision that wrote nothing and decided nothing about the day; **A6** — a failed latest attempt un-proves a day only when no LATER day in the range is proven (the day is the frontier). Behind the frontier the day's rows are intact and R-91 forbids rebuilding it, so it stays proven. Without A6 anyone holding `finance.record` could block a month's crediting for ever by re-running a past day from the Engine Runs page, since that re-run can never succeed. | Compensation | Open (raised 2026-09-18) — **residual:** a month that is genuinely short (a day inside the platform's life with no completed cut-off that can no longer be rebuilt) can still only be closed by a hand-typed `php artisan compensation:monthly-close --month=<YYYY-MM> --force`, which credits the month knowing it is short and records the override in the run log; there is no self-service path and there is deliberately no automatic one. Review at the first month-end after launch. |
| R-102 | A developer-driven single-period rebuild now exists in production | Statutory / Operational — High | ADR-0016 (D4, D9–D14); compliance review 2026-09-18 (S3 sign-off on DN-1, D10, D11, D12) | The admin "Retry this night" control (R-81, R-89, R-90) is deleted and replaced by four commands — `compensation:rebuild-night`, `-week`, `-month`, `-payout` — reachable only by the `developer` role, in production as well as on dev/staging. Each can **delete** a night's cut-off/pool/mentorship rows, a pending payout batch's own debits and line items, or a month's crediting-engine rows, and can **un-sweep** a payout batch's credits, before re-running the ordinary engine command for that period. **What it can delete and un-sweep:** the wipe-scope tables in the implementation plan are the source of truth. The exact rule, corrected by `compliance-officer` from the plan's looser headline ("a rebuild never deletes a credit") because that headline is looser than both §29 and the code, which do delete unswept, unreversed credit rows on the night and month paths: **a rebuild never deletes a credit that has been paid, swept or reversed, and never deletes a credit except together with the result row it derives from, in the same transaction, in order to re-derive both.** A never-approved batch's own debits are removed under DN-1's separate, explicit exception, because those debit rows are the batch's own projection of a payout that never happened, not earned commission. **The refusals (D9–D12) are the control, not a courtesy:** a frozen month (D9, no override, not even `--force`), a night that is no longer the newest one (D11, R-91's window), a month the next month was already built on (D12), and an approved or gateway-touched batch (DN-2) each stop the rebuild outright before anything is written. **It is a one-role control with no maker-checker of its own** — the developer who decides a rebuild is owed is the same developer who runs it — mitigated by: a mandatory two-step preview compared by fingerprint so a confirm never acts on state that changed underneath it; a mandatory written reason (≥ 10 characters); three audit rows per rebuild (`compensation.rebuild.queued`/`wiped`/`completed`, or `.refused`), each carrying row counts and a before-hash; the rebuilt batch's maker bar (D14 — the developer who rebuilt a payout batch cannot also approve it, R-81); and `RebuildPreflight`, which refuses beside any of the three scheduled runs or another rebuild already in flight. **`compliance-officer`'s S3 sign-off is conditioned on five further items, recorded here as its condition rather than left in a review thread. (1) The DN-1 deletion doctrine holds only while five conditions hold, all verified in the S3 review:** (i) `approved_at IS NULL` and status not in `FrozenPayoutGuard::FROZEN_STATUSES`, re-read under the sweep lock after `refresh()` — never on the caller's stale copy; (ii) no `payout_gateway_events` row exists for the batch or any of its line items, checked inside the transaction; (iii) the deletion is scoped by `reference_type`/`reference_id` to that batch's own line items and to exactly the four debit types (`payout_debit`, `admin_charge_debit`, `tds_debit`, `income_cap_forfeit`) — never by date, actor or amount; (iv) the whole un-build, including its audit row, is one transaction; (v) the `payout.batch.unbuilt` audit row carries `line_item_ids`, `debit_ids`, `forfeit_ids`, both paise sums, `entries_unswept` and the pre-delete `before_hash`. **If any one of these five is relaxed in a later change, this sign-off lapses and DN-1 must be re-argued from scratch** — it is not a standing approval of "delete the debits." **(2) The wiped-then-failed-re-run unrecoverability window:** a month wiped by `compensation:rebuild-month` whose re-run keeps failing can run out of time — once M+1 closes over it, D12 refuses any further rebuild attempt, and the only remedy left is a correction decision by the client (as R-91 §14 already requires for a passed GSB day). Visible through the failed rebuild's own run row, the engine-failures badge and the nightly deferral alerts — nothing hides it — but inherent to the approved wipe-then-re-run design, recorded here rather than fixed. **(3) A night inside a closed-but-not-frozen month is rebuilt on a WARNING, not a refusal:** D11 lets `compensation:rebuild-night` proceed there because the month is not frozen (D9), but that month's Rank, Growth Booster, Fortune and ADC figures were computed on the pre-rebuild GSB numbers and now stand uncorrected until someone acts on the warning — and nothing alerts on the follow-up being skipped. Accepted **on condition** that `docs/runbooks/engine-failure-triage.md` states the follow-up `compensation:rebuild-month` as a mandatory numbered step in this situation, not as advice — done in the same change as this row. **(4) ₹50 lakh income-cap headroom:** rebuilding an older, unfrozen batch beneath a later, already-frozen one can change how the forfeit is split across earned months, because the cap counts swept rows only and the later batch's allocation was fixed against the pre-rebuild figures. Warning only, surfaced in the rebuild preview text — never a refusal, because the later batch's own approval stands (D9). Recorded here as an explicit **operator obligation**, tied to the existing "rebuild these too, oldest first" preview warning: rebuild later unfrozen batches in batch-date order after an older one, so no batch is ever measured against a headroom that changes again after it is checked. **(5) The Redis-lock caveat:** `compensation:payout-sweep` (`PayoutService::withSweepLock()`) is a cache lock on the platform's shared Redis instance, evicted under `allkeys-lfu` alongside eight other applications (ADR-0011) — so a silent eviction is a theoretical way for two writers to meet on the same batch. The guarantees that actually hold are underneath the lock, not the lock itself: the single database transaction, `lockForUpdate()` on the line items, and the frozen-status re-check under `refresh()` (item (1)(i) above). The lock narrows the window; it is not what makes concurrent writes safe. **Domain events are re-emitted on the re-run** (income notifications may be sent twice) — not suppressed, and out of scope for this change; a rebuilt period's distributor may see a duplicate notification for a bonus they were correctly credited once. **Open item: rehearsed on staging — no.** The rollout plan calls for one rebuild rehearsal on staging with the client's knowledge before this reaches production traffic; until that rehearsal is recorded here, treat the mechanism as reviewed but unexercised. **Pre-launch note, found while writing this row:** `FortuneStagingE2ESeedCommand` (`fortune:staging-e2e-seed`) calls `FortuneBonusService::runForMonth()` directly, bypassing `FrozenPayoutGuard` entirely — there is no frozen-month check anywhere in the command, and no environment gate either (it is marked `[DEV]` in its description and docblock only; nothing in code refuses it in production or against a frozen month). It writes synthetic BV and Fortune rows with FK checks disabled and carries its own `--rollback`. Staging-only by convention, not by code. **Delete this command before production launch** — it is exactly the shape of control (a service called under the compensation engines' own frozen-lock guard, from a command that does not check it) this ADR closes everywhere else. **Addendum 2026-09-19 (known risk, accepted, not guarded):** a night rebuild of a day that carried a personal-BV top-up which was REVERSED AFTER that day settled re-derives the day on a leg that is still inflated. `GsbPersonalBvTopupService::reverseForOrder()` takes its settled branch in that case and decrements the CURRENT day's `group_bv_daily` accumulator rather than the original day's — the deliberate no-clawback forward-debt pattern — so the original day keeps the top-up's BV; the night wipe hands back only un-reversed top-ups (a reversed row was already decremented at reversal, and handing it back a second time would double-count), and `pendingAccruals()` excludes the cancelled order from the re-run, so nothing re-applies it either. The rebuilt day's weaker leg, and the Genos BV the Rank check reads from it, therefore stand above what the surviving orders justify, by the reversed top-up's amount. Narrow by construction: it needs a cancellation or refund taken AFTER the day settled, on a day that is still the newest rebuildable night (D11's window), for a distributor whose leg touched a slab that day. Generous to the distributor, never to the company, and bounded by the cancelled order's own BV. The clean repair is the same one `GsbCutoffService` already names for the neighbouring residual — a windowed recompute, which re-derives `group_bv_daily` from source and runs on dev and staging only. **Accepted as a known risk by the client on 2026-09-19; no refusal added and the hand-back deliberately not widened.** **Addendum 2026-09-19 (S4, the developer rebuild surface — three residuals, all disclosure, none a relaxation):** the web control is built on the S3 commands and changes none of their guarantees — **the five DN-1 sign-off conditions above are unchanged and still hold; nothing in S4 relaxes any of them.** What S4 adds is a second reader (the admin family, who can see the run log but not the control) and a second actor record (a named developer instead of a shell), and those produce three recorded residuals. **(i) The Run events page shows the whole admin family what a rebuild is, and who ran it.** Every crediting command passes `FrozenPayoutGuard::creditingRefusal()` to `noteSkipped()` and `RecordEngineRun` writes it verbatim to `engine_runs.error`, so a skipped row carries in full the same `php artisan compensation:rebuild-payout --month=…` sentence that `AdminEngineRunsController::refusalForReader()` strips out of a live refusal on the Engine Runs page; and a rebuild's own run row names the developer who ran it by email (`events.blade.php` renders `$run->actor?->email`). **Accepted as an audit fact and deliberately not weakened:** the Run events page IS the run log, a rebuild that deleted derived money rows belongs in it, and the developer's identity on that row is exactly the accountability the D14/R-81 maker rule depends on. What is withheld is the standing menu entry rather than the fact: the four `developerOnly` engines are excluded from the Engine filter's options AND from its validation for non-developers, so a platform on which no rebuild has ever run does not advertise the control before there is anything to record; every historical row stays visible to every admin role. The standard held to throughout is **"no control, and no command an admin could run"** — never "not the word rebuild", which also appears as an ordinary English noun inside the frozen-month refusal. **(ii) `admin-compliance` sees THAT a period's derived money rows were deleted, with reason, counts and IP — but not WHO deleted them.** `AuditLogPresenter::maskPrivilegedActors()` blanks `actor_email` on every row whose actor holds the hidden `developer` role, for any viewer who does not hold it, and `AdminAuditLogController`'s `actor=` filter excludes privileged ids so a search cannot confirm the account exists either. The STORED record is complete and hash-chained — `audit_log.actor_id` is retained in full and only the admin-facing rendering is masked — but a compliance reviewer reading the Audit Log page alone cannot attribute a rebuild. **Pre-launch obligation, stated here rather than left implicit:** any compliance audit of rebuild activity must take attribution from a developer-level view of the Audit Log or from a direct `audit_log` export, not from the admin Audit Log page. **(iii) A web-rebuilt payout batch needs a second `finance.approve` human, and a single-developer deployment does not have one.** `finance.approve` is held by `admin` and `developer` only (R-81, deliberately not `admin-finance`); a batch rebuilt from the web records the rebuilding developer as its maker (D14), whom the existing self-approval refusal then bars. Where that developer is the only account holding `finance.approve`, the rebuilt batch is not merely awkward to approve — it is **unapprovable**, and the money does not move until a second holder exists. **Add to the pre-launch named-officers / staffing gate: at least one `admin` account held by a human who is not the developer who runs rebuilds** — the same two-humans-or-nothing prerequisite R-81 and R-92 (§14b) already carry, now load-bearing for the rebuild path as well. **Addendum 2026-09-19 (rehearsal finding — a prevent-before control became detect-after):** the rebuild carried a guard, `spentRepurchaseCredit()`, refusing a period whose earners had drawn on their repurchase wallet at any instant from the period's first `repurchase_deduction` onward — no upper time bound, no override. Staging showed it was **unsatisfiable by construction**: the monthly gates (GBB, Fortune, rank requalification, AO-GO) require the repurchase wallet to read zero at month end, and the only way to reach zero after being credited during the month is to spend during it, so every month that paid a gated bonus was permanently un-rebuildable. Evidence: August 2026's rebuild was blocked by 170 spends — 150 from the settlement fixture and **20 from ordinary human product orders placed weeks later in September**, i.e. a distributor buying a product in September permanently blocked an August rebuild. The guard is replaced by a post-replay reconciliation: each rebuilder carries the affected distributors' unfloored repurchase positions on the plan, and after the wipe AND the replay the command re-measures them, warns, and writes `compensation.rebuild.repurchase_shortfall` with per-distributor before/after paise — on the success branch and on the re-run-failed branch, which is where a shortfall is likeliest. **The S3 sign-off does not lapse:** the removed guard is not among D9–D12 nor any of the five named conditions. **Residual, and it is new:** this control detects, it does not repair. `WalletService::repurchaseWalletBalancePaise()` floors the balance at 0, so a negative position reads as zero, the next credit is fully spendable, and the hole stays open until a human posts a ledger correction against the audited row. Direction of harm is company-ward, not distributor-ward — nothing is clawed back and no earned credit is deleted (DN-1 untouched). **Assign an owner for acting on `compensation.rebuild.repurchase_shortfall` rows before launch.** | Compensation | Open (2026-09-19) — reconciliation shipped; rehearse on staging, assign a shortfall-correction owner, then delete the Fortune staging-E2E command before launch |
| R-103 | GST reversals on refunds are read from ledger entries, with no numbered credit-note document behind them | Statutory (GST) — Medium | CGST Act 2017 §34; CGST Rules 2017 Rule 53(1A); compliance review 2026-09-20 (statutory reports) | The GST summary and the outward register both report credit notes, but there is no credit note. What they read is the `liability.gst_output` debit a refund posts to the ledger, identified by the invoice and the order it reverses — there is no `credit_notes` table, no document is issued to the buyer, and **no consecutive serial number is allocated**. Rule 53(1A)(b) requires a credit note to carry "a consecutive serial number not exceeding sixteen characters ... unique for a financial year", and GSTR-1 Table 9B is filed credit-note-by-credit-note against that number. So every reversal the reports show is a real reduction of output tax that cannot yet be filed as one: at the first return the numbers would have to be entered against serials invented outside the platform, which is exactly the break in the audit trail §34 exists to prevent. Nothing is wrong with the figures — the amounts and the head split are read from the ledger the refund actually posted, and `refunds_without_tax_credit` already counts the refunds that reversed no tax — the defect is that they are anonymous. Not reached in any environment yet: no GSTR-1 has been filed, because there is no production environment yet (client, 2026-09-17). Mitigation: a `CreditNote` model with its own financial-year sequence, allocated when a refund is approved and stamped on the ledger entry, so the register reports the document instead of inferring it. Must land **before the first GSTR-1 filing**, not before launch of the reports themselves | Commerce | Open (raised 2026-09-20) — pre-launch: due before the first GSTR-1 filing |
| R-104 | Full bank account numbers are rendered on the payout batch pages, not only in the bank file | Statutory (DPDP) — Medium | DPDP Act 2023 §8(4)–(5); CLAUDE.md hard rule 8; R-82; client decision 2026-09-20 | R-82 put full account numbers into the manual-NEFT export because the bank cannot execute a file of last-four digits. This row records the screen that goes with it: the payout batch pages now decrypt and render the whole account number for holders of `finance.record`, so a number can be checked against the bank's response without downloading the file. Audited per page render — `HandlesPayoutBatchActions::bankAccountColumn()` writes `payout.batch.bank_accounts_viewed` with the batch, the page and the count revealed, and deliberately no account numbers and no names; anyone without `finance.record` gets the masked column and no audit row is written. **Residual: a rendered number is outside the audit trail the moment it is on screen** — screen capture, a shoulder, and the browser's own page cache and back-button history, none of which the audit row can see or bound. The disclosure is also all-at-once: the whole page's numbers are decrypted on load, so a reveal is never narrower than a page. Mitigation: a per-row reveal toggle and a session-scoped reveal, so the audit row records the accounts a human actually asked for rather than the accounts a page happened to hold; until then the count on the audit row is the page size, not an interest. Include in the R-82 review cadence rather than reviewing it on its own clock — the export and the screen are one decision and should be re-argued together | Compensation | Open (raised 2026-09-20) — reviewed with R-82 |
| R-105 | Published policy text conditions the cooling-off refund on product condition | **Statutory — High** | Hard rule 5; T&C §4; DSR 2021 Rule 5; compliance review 2026-09-23 | The client's Terms §8 and Returns §4 (published 2026-09-23) make the cooling-off refund depend on "Saleable" condition and pay "Direct Seller Price less GST" without an invoice. The platform still refunds in full inside cooling-off and **must never be aligned to this text**. Listed in `docs/compliance/policy-docs-conflicts-2026-09-23.md` item 19. Needs the client's corrected text. | Client | Accepted (user, 2026-09-23) — published as the client wrote it; the platform still refunds in full inside the 30-day window; client to amend Terms §8 / Returns §4 before launch |
| R-106 | Staff always see the purchase mark on the admin Genos | Statutory / DPDP — Low | Hard rule 3 amendment 2026-09-23 (user decision); R-65 | `DistributorIdCardStats::visibleIds()` gives staff (all five back-office roles) every card's purchase mark, only on `admin.tree.*` routes and regardless of `genealogy.purchase_mark_visible`. Staff already see personal BV on the admin distributor page. Every other surface, and every distributor viewer, still follows the R-65 switch. | 12 | Accepted (user, 2026-09-23) |
| R-107 | Offline orders: one person can record and confirm the same payment; cash, proof-file and retention controls | Statutory / Operational — Medium | Hard rule 2; R-17; Income Tax Act s.269ST / s.271DA; DPDP Act 2023 §8(7); user decision D1-B 2026-09-23; compliance review 2026-09-23 | Offline orders (`docs/plans/offline-orders-2026-09-23.md`) let staff create an order for a distributor who paid outside the gateway; finance confirms the money through `PaymentConfirmationService::confirmOffline()`, which is what creates BV and commission. Creating needs `commerce.order.manage`, confirming needs `finance.record` — but `admin`/`developer` hold both, so **one super-admin can record a payment and confirm it on their own say-so** (decision D1-B: split permissions, no different-person rule). Mitigation: `same_actor` is recorded on both the `order.offline_created` audit row and the `order.paid` evidence; **admin-compliance reviews every same-actor confirmation monthly**. Staff cannot create or confirm for their own distributor account. **Cash:** under ₹2 lakh per person per day (s.269ST), enforced in validation and under a row lock in the service. **Proof files** are PiiCrypter ciphertext (deposit slips carry account numbers; a cash-deposit slip of ₹50,000 or more carries a PAN), served only through an audited route, and erased by `commerce:purge-offline-payment-proofs` after the admin-owned retention periods (default 2,920 days from confirmation; 90 days after rejection or cancellation). **Open, launch gate:** the Privacy Policy does not yet state the proof-file retention period — needs the client's wording and `content:publish`. **Residual (s.269ST):** the cash ceiling is per buyer and per `received_on`, so one third-party payer funding several distributors, or a back-dated `received_on`, is not caught by the code — include both in the monthly review. **Residual:** a pending offline order holds money that is not on the books until confirmation; rejection is allowed only with an explicit "no money received" assertion, and money that was received must be confirmed then cancelled so the refund is owed on the books. | Commerce | Accepted (user, 2026-09-23) — monthly same-actor review; Privacy Policy retention line due before launch |
| R-108 | Single-person manual payout settlement: one finance user can mark a paid transfer returned and send it again | Operational / Financial — High | Separation of duties (CLAUDE.md architecture); R-82; user decision 2026-09-24 (permission option B); compliance review 2026-09-24 | The payout batch page lets `finance.record` alone Mark paid (with a UTR), Mark failed, Mark returned, Send again and Check with Razorpay on the lines of an **approved** batch (`PayoutLineSettlementService`), so a failed or bounced payment can always be finished without touching the wallet. No second person is required (user decision). **A false "returned" followed by Send again produces a second real transfer that nobody else approved**, and Mark paid can close a line with any UTR not already in use. Controls: approved batch required (`approved_at`), row-locked state checks, one audit row per action with before/after hashes and from/to status (the previous UTR on a return, the previous reason and payout id on a send-again); UTR unique across all payout lines; a per-attempt warning before a second bank file is downloaded for lines already in one; in Razorpay mode, no manual marking of a waiting line and the old payout id dropped only after Razorpay confirms that payout failed or was reversed; the wallet ledger is never written. **Compensating control:** admin-compliance reviews every `payout.line_item.marked_returned`, `sent_again` and `marked_paid` row monthly, by someone other than the actor (same shape as R-107). **Residual (accepted):** error or collusion by one finance user between reviews. **Follow-ups:** the distributor is told of a failed payout only on their wallet page — email + in-app notice with the grievance route (R-67 pattern) is open; a return changes a closed period's "net transferred" figure and the 26Q register then shows the line without its old UTR (kept in the audit row) — note on the TDS page open | Compensation | Accepted (user, 2026-09-24) — monthly review of manual settlement actions; review at launch sign-off |


### Franchise programme — withdrawn 2026-09-17

The franchise programme is dropped in full. The **Arete Development Centre**
takes its place: a training and support venue owned by an ADN, holding no stock
and billing nothing. R-21, R-25, R-45 and R-46 close with it. R-22, R-23, R-24
and R-47 do **not** — those four describe defects that moved to the ADC along
with `orders.franchise_id` → `orders.arete_center_id` (2026-09-01) and are still
live; each is re-scoped rather than withdrawn, and R-47 is worse than it was,
because the franchise collection-point picker was pulled before merge while the
ADC one shipped.

The code and the schema are already gone. Migration
`2026_09_01_200000_replace_franchise_with_arete_center_on_orders.php` dropped
`franchise_commission_result_orders`, `franchise_commission_results` and
`franchises`, swapped `orders.franchise_id` for `orders.arete_center_id`, and
narrowed the `wallet_ledger_entries.type` enum to remove `franchise_credit`; the
service, model, `FranchiseFeature` flag and admin screens went with it. Verified
on the dev database — no `franchise%` table exists. The only survivor is the
now-unreachable `'franchise_credit'` string in `WalletService`'s constant list,
which the narrowed enum can no longer produce. **Nothing needs dropping and no
migration is pending.**

**Removing §11.1 from the plan source does not change what a distributor reads
— it needs a separate publish step, run per environment.** `compensation` is in
`ContentPageSeeder::HELD_SLUGS`, and `seedAsDraft()` skips any slug whose row
already exists — deliberately, so a seeder run can never overwrite published
legal copy. It takes **`php artisan content:publish compensation`** on each environment
(audit-logged as `content_page.republished`). **Run on local dev 2026-09-17 with
the client's approval** — verified: the 3% commission text is gone, the page now
ends at §11.1 Redeem points, disclosure version v2026-09-17. **Staging republished
2026-09-17** with the client's approval and verified the same way. **There is no
production environment yet**, so what remains is a launch step rather than an
outstanding gap: `content:publish` must run for every held slug when production
is first stood up, because a `git pull` alone will not move it. Characterisation for the record: removing an expressly non-operative
component with a placeholder effective date, never appointed and never paid, is
a **non-material amendment under DSA §16.2**, so no §6.2 thirty-day notice is
required — but that is a legal position read into §6.2, not something §6.2 says
on its face.

## Compliance items C-01 … C-09 (Phase 1)

Aligned to the PRD §12. Each must be signed before Phase 1 exits.

| ID | Item | Signed by | Signed date | Artefact |
|---|---|---|---|---|
| C-01 | Joining free of cost enforced in code | | | Test + PR link |
| C-02 | No income projection in registration UI | | | Content review memo |
| C-03 | Orientation gate enforced | | | Test + PR link |
| C-04 | Cooling-off one-click cancellation | | | Test + PR link |
| C-05 | Register of DS export working, retention policy set | | | Admin export screenshot + retention ADR |
| C-06 | DPDP: consent is purpose-limited, revocable, DF contact published | | | Privacy notice + consent table migration |
| C-07 | State-aware age rule honoured (MH=21) | | | Test + PR link |
| C-08 | Declarations (jail/bankrupt/sound-mind) captured | | | Test + PR link |
| C-09 | Placement transparency — snapshot + resolved side visible to DS | | | Test + screenshot |

## Accepted risks (dated)

| Date | Risk ID | Acceptor(s) | Reason | Review date |
|---|---|---|---|---|
| 2026-05-25 | R-16 | compliance-officer | Residual TOCTOU: concurrent placement under the requester is not fully serialized by the target-parent advisory lock during approval. Low placement volume in Phase 1 makes the race practically unreachable | Phase 2 exit gate |
| 2026-05-25 | R-17 | compliance-officer | Any `admin` may approve/reject a binary-placement move; a dedicated `admin-operations` role is deferred. Small, trusted admin team in Phase 1 | Phase 2 exit gate |
| 2026-07-30 | R-35 (b) | Business owner | Zero-point MSB day freezes a ₹0 point value: the day's pool goes unspent and no MSB is paid for that day even on a later admin retry. Accepted knowing R-33 rules the GSB twin the other way — GSB's score value is capped at ₹250 for every slab (1–2 fixed, 3–7 variable but never above it) so freezing the cap is bounded, whereas the uncapped MSB point value has no ceiling to freeze | First real MSB payout |
| 2026-09-17 | R-25, R-45, R-46 | Client | Franchise programme dropped in full; the Arete Development Centre replaces it. These rows describe risks that exist only if a franchise exists. The flag was never enabled in any environment paying real distributors and no franchise was ever appointed. | Closed, no review |
| 2026-09-17 | R-21 | Client (close) / compliance-officer (condition) | Franchise dropped, so the offline-retail risk goes with it — **but the closure rests on the centre owner's declaration that the centre is not an e-commerce fulfilment point, and the shipped checkout flow falsifies it** by writing the centre's postal address into the order's `ship_*` fields. Closed conditionally, not absolutely. | **On R-47 closure — reopens if R-47 is not remedied** |
| 2026-09-17 | R-58 | Client | Superseded by R-58's only outstanding action being withdrawn: no clamd will be stood up in any environment. Residual carried by R-83. | Closed, no review |
| 2026-09-17 | R-83 | Client | There is no malware-scanning feature. Reaffirms the 2026-09-11 acceptance and removes the Phase 12 revisit: files reach storage unchecked by decision, not by omission. Every skipped scan logs `uploads.scan_skipped` with a SHA-256 so the exposure stays countable. | None — permanent |
