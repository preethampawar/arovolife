# Compliance Risk Register

Standing risks re-assessed at every phase exit gate. Critical / High
risks must be mitigated or formally accepted (dated, signed) before a
phase can close.

| ID | Risk | Nature | Source | Mitigation | Phase owner | Status |
|---|---|---|---|---|---|---|
| R-01 | Pyramid / money-circulation interpretation | Statutory — Critical | DSR 2021 §2(1)(j); T&C Definition "Pyramid Scheme" | Commissions only on product sales; every commission row has `product_sale_id NOT NULL` | 4 | Pending |
| R-02 | Mis-selling & income guarantees | Statutory — High | DSR 5(1)(d); T&C §6 | No earnings calculator on public site; orientation required; all marketing copy reviewed | All | In-design |
| R-03 | Forced purchase at joining | Statutory — High | T&C §4 | Registration cannot add SKUs; unit test asserts | 1 | In-progress |
| R-04 | Failure of cooling-off (30 days) | Statutory — High | T&C §4, Code V | `cooling_off_end_at` stamp; one-click cancel; reminders at D-20 / D-7 / D-1 | 1 + 8 | In-progress |
| R-05 | Buyback / return not honoured | Statutory — Medium | T&C §8 | Workflow enforces saleable/non-saleable split and GST deduction rules | 8 | Scheduled |
| R-06 | Duplicate PAN / duplicate ADN | Statutory — High | T&C §1.4, §7 | Unique index on PAN hash; couple rule engine; duplicate detection cron | 1 | In-progress |
| R-07 | KYC data breach (PAN, Aadhaar) | Data protection — Critical | DPDP 2023; T&C §15 | AES-256 at rest; last-4 display; audit log; purpose-limited access | 1 | In-progress |
| R-08 | Third-party resale / e-commerce listing | Contractual — High | T&C §9 | Terms acceptance; marketplace crawler; takedown workflow | 2 + 8 | Scheduled |
| R-09 | Minor as distributor | Statutory — Critical | T&C §1.1 | DOB validation with state threshold; PAN cross-verification | 1 | In-progress |
| R-10 | Auto-termination of inactive DS (>1 y) | Contractual — Medium | T&C §21 | Scheduled cron on `last_sale_at`; 7-day notice; freeze then terminate | 8 | Scheduled |
| R-11 | Grievance redressal SLA breach | Statutory — Medium | T&C §11 | Ticket ID + SLA clock; status updates; public status page | 8 | Scheduled |
| R-12 | Placement Strategy flipped mid-flow | Operational — High | ADR-0002 (superseded) | n/a — ADR-0003 removes the strategy concept entirely; no `placement_strategy_snapshot` column, no resolver; placement is a single-level invariant rule | 1 | Mitigated (2026-05-01) |
| R-13 | Placement_id outside sponsor's downline | Operational — High | ADR-0003 | `PlacementEngine::isSelfOrDescendant()` (`app/Modules/Genealogy/Services/PlacementEngine.php:147`); cross-line attempts raise `CrossLinePlacementError`, dispatch `ForbiddenPlacementAttempted`, write `genealogy.placement.rejected` audit row; `RegistrationWizardController::start()` re-validates at link-open time and surfaces failures as a generic Contact Us redirect | 1 | Mitigated (2026-05-01) |
| R-14 | Contact-form PII on a plaintext queue payload | Data protection — Medium | DPDP 2023 §6, §8(3) | `NewContactInquiryNotification` is constructed with the inquiry id only; PII is re-fetched at delivery time so the `jobs` table never carries name/email/phone/address/message | 1 | Mitigated (2026-05-01) |
| R-15 | Contact-inquiry retention not bounded | Data protection — Medium | DPDP 2023 §8(3) | `contact-inquiries:purge` artisan command (`app/Modules/Public/Console/PurgeStaleContactInquiriesCommand.php`) deletes unhandled inquiries >90d and handled inquiries >365d; scheduled daily at 03:00 IST via `PublicServiceProvider::boot()`; each run records counts (no PII) in `audit_log`; covered by 6 Pest tests (PURGE-01..06) | 1 | Mitigated (2026-05-01) |
| R-16 | Line-change approval residual TOCTOU on requester leaf status | Operational — Low | ADR-0003 | Line-change approval re-checks the requester's leaf status under a row lock, but a concurrent placement landing under the requester is not fully serialized by the target-parent advisory lock (`app/app/Modules/Genealogy/Services/ApproveLineChange.php`). Accepted for Phase 1 (low placement volume); revisit if placement volume rises. **Resolved (2026-06-17):** `ApproveLineChange` now holds the advisory lock on BOTH the requester and the target parent (ascending-id order, deadlock-safe), fully serialising any concurrent placement landing under the requester against the leaf re-check | 1 | Resolved (2026-06-17) |
| R-17 | Any admin may approve/reject a line-change (binary-placement move) | Operational — Low | T&C §3; separation-of-duties principle | Any user with the `admin` role may approve/reject line-change requests; the admin route group in `app/routes/web.php` is not scoped to a dedicated `admin-operations` role. A dedicated `admin-operations` role is deferred to a later phase. Accepted for Phase 1 (small admin team). **Resolved (2026-06-18):** separation-of-duties roles shipped — `admin` super-admin (Gate::before bypass) + scoped `admin-operations` / `admin-finance` / `admin-compliance`; line-change decisions gated `can:placement.decide`, COD/finance `can:finance.record` (closes R-20 too), block/terminate `can:compliance.discipline`. Tests: AdminRoleSeparationTest | 1 | Resolved (2026-06-18) |
| R-18 | `coupon_redemptions.customer_id` retention window not bounded | Data protection — Medium | DPDP 2023 §8(3) | The coupon-redemption ↔ customer FK has no defined retention/purge. It is required during the order's life for per-customer usage caps + tax audit; schedule a `coupon-redemptions:purge` command (or null `customer_id` after the order's return window closes) before Phase 2 closes | 2 | Open (2026-05-31) |
| R-19 | `customer_addresses` retention window not bounded | Data protection — Medium | DPDP 2023 §8(3) | Shipping + billing addresses are saved "on file" for the customer at checkout (Epic 4) with no defined retention/purge. The "My Addresses" self-service book (saved-address feature, 2026-06-07) now lets a data principal view/edit/**delete** their own saved shipping addresses — a partial DPDP correction/erasure mitigation — but the gap remains Open: no scheduled purge, and the `customer_addresses.customer_id` `cascadeOnDelete` FK is never fired because there is no account-closure/Customer-deletion flow. Schedule a purge or anonymise-on-account-closure policy alongside R-18 before Phase 2 closes | 2 | Open (2026-05-31) |
| R-20 | COD cash-collection (`markCodPaid`) postable by any admin | Operational — Low | T&C §3; separation-of-duties (see R-17) | The Epic-4 `AdminOrderController::markCodPaid` posts a cash-in ledger entry but is gated only by the `admin` role. Like R-17, it should move to a dedicated `admin-finance` role before order volume grows. **Resolved (2026-06-18):** `markCodPaid` is now gated `can:finance.record` (held by `admin-finance` + super-admin) — see R-17 | 2 | Resolved (2026-06-18) |
| R-21 | Franchise modelled as an offline retail outlet | Statutory — Critical | DSR 2021 Rule 4; T&C §5.2(2); Code of Ethics §2.9; Hard Rule 7 | The partner's "billing centre" with walk-in product sales is offline retail, which our own T&C forbids. The franchise MUST be restructured as company-operated billing **infrastructure** with mandatory ADN attribution on every sale (no anonymous walk-in sales; no independent franchisee-owned retail). Gating legal/product decision required before any franchise ADR/ERD/migration is written | 7 | Open (2026-06-01) |
| R-22 | Franchise 3% payout without per-sale `product_sale_id` traceability | Statutory — High | DSR 2021 Rule 5(1)(c); Hard Rule 2 | If BV is attributed at the franchise-code level (aggregate) rather than per sale, the 3% becomes indistinguishable from a non-sale-based commission at audit. Every franchise payout row must FK to individual `product_sale_id` rows; no aggregate-balance payouts; recruitment-originated BV must be excluded | 7 | Open (2026-06-01) |
| R-23 | Franchise 3% cooling-off clawback gap | Statutory — High | T&C §4.2(4); ADR-0005; Hard Rule 5 | If the 3% is paid at billing, sales cancelled within the 30-day cooling-off window create a refund-without-clawback gap. The 3% must be held `pending` for each underlying order's cooling-off window and unlocked only on expiry; cancellation reverses the corresponding pending payout. Data model (FK chain franchise_payout → product_sale_id → order_cooling_off) must be designed in now. **Partially mitigated (2026-08-07, `feature/adc-center-address`):** the ADC engine's monthly base is now the signed net member BV — same-month refunds are deducted before the 3% is computed, and a month netting ≤ 0 is skipped (`AreteDevelopmentCenterBonusService::runForMonth()`). Residual gap: BV reversals carry `effective_at` = refund date, so a sale refunded in a later month does not claw back the month it was already paid on, and a net-negative month is discarded rather than carried forward. The per-order `pending`-until-cooling-off-expiry hold this risk requires is still unbuilt — revisit at Phase 7 | 7 | Open (2026-06-01) |
| R-24 | Combined binary + franchise payout pyramid-scheme surface | Statutory — High | DSR 2021 §2(1)(j); see R-01 | A franchise owner who is also a binary-tree distributor could earn overlapping overrides on the same sales. Legal counsel must sign off on a combined-payout ceiling before Phase 7 design; the comp-plan's 3% figure is not pre-approved for a stacked-on-binary scenario | 7 | Open (2026-06-01) |
| R-25 | Franchise code identity collision with ADN | Statutory — High | Hard Rule 6; T&C §3.4 | A franchise code with its own login, BV attribution and payout is functionally a second identity. It must be architecturally distinct from the ADN and must NOT be placed in the Genos (no binary-tree bonuses — franchise earns only the 3%). If a franchise owner also holds an ADN, PAN links to the ADN; the franchise entity records its own GST/TAN/PAN separately | 2 | Open (2026-06-01) |
| R-26 | Binary spillover enabled in production without PO + Compliance compensation sign-off | Operational — Medium | ADR-0007; DSR 2021 Rule 5; Hard Rules 2 & 3 | Spillover (`placement.spillover.enabled`, and the `placement.spillover.strategy` fill-algorithm selector — `breadth_balanced`/`depth_outer`/`weaker_leg`) changes downline composition and therefore future (Phase 4+) commission distribution. Controls: setting defaults **OFF** (ADR-0007) — the engine and `RegistrationWizardController::start()` both gate on it, so OFF = ADR-0003 single-level behaviour byte-for-byte; the admin Settings description mandates PO + Compliance sign-off before prod enablement; every toggle flip writes a settings-update `audit_log` entry (detective control); placement is structural/pre-revenue so no BV/commission is created by the move itself. Do NOT switch the toggle on in production until the compensation-distribution effect is signed off; revisit at Phase 4 (compensation engine) kickoff | 1 | Open (2026-06-06) |
| R-27 | "Find My ID" (name+PAN → ADN) targeted disclosure / ADN harvesting | Data protection — Medium | DPDP 2023 §5-6, §8(3); Hard Rule 8 | Public `/find-my-id` returns a distributor's ADN on an exact registered-name **AND** PAN match (`FindMyIdController`). Controls: PAN matched as one-way `sha256` (same scheme as registration), never stored/displayed/logged; dual exact match (neither field alone reveals anything); two-tier per-IP rate limit (5/hour + 15/day) counting every attempt, plus route `throttle:8,10`; generic "no match" message (no field-level leak); terminated/frozen/rejected accounts excluded (`users.status IN active,pending`); DPDP consent checkbox + Privacy Policy link required; every attempt (success/miss/throttled) audit-logged with IP + distributor id only (no PAN). Residual: a **targeted** attacker who already holds a specific person's exact name + PAN can confirm arovolife membership and obtain that ADN in one attempt (rate limit cannot stop a single correct guess). Accepted for the ADN-recovery use case; if abuse appears in the audit trail, harden to email/SMS-OTP delivery of the ADN (deliver to the on-file channel instead of on-screen) — noted as the fast-follow mitigation | 1 | Accepted (2026-06-08) |
| R-28 | Customer order-summary printout not a GST-compliant tax invoice | Statutory — Medium | CGST Rule 46; Hard Rule 7 | The printable order document (`shop/orders/invoice.blade.php`, route `orders.invoice`) renders directly from `Order`, not the canonical Tax `Invoice` model (`app/Modules/Tax/Models/Invoice.php`, which carries `seller_gstin`, `place_of_supply`, CGST/SGST/IGST split, `invoice_no`, `pdf_hash_sha256`). Mitigation in place: the document is labelled "Order Summary" (not "Tax Invoice") and carries an explicit footnote "This is an order summary / payment receipt, not a GST tax invoice", so it does not misrepresent itself; it shows the customer (buyer), the attributed distributor's name + ADN, line items with HSN + GST%, and totals. BEFORE customer-facing launch: either re-source this view from the Tax `Invoice` model (stored invoice number + GSTIN + CGST/SGST/IGST split + PDF hash) or keep the receipt label until a GSTIN is configured. No earnings/BV shown (hard rule #3); owner-only access (IDOR-guarded) | 2 | Open (2026-06-08) |
| R-29 | Customer email_enc/phone_enc still APP_KEY-bound (not on dedicated PII key) | Data protection — Medium | DPDP 2023 §8(7); Hard Rule 8; ADR-0008 Follow-up 2 | Distributor PAN/Aadhaar/bank moved onto the dedicated `PII_ENCRYPTION_KEY` (ADR-0008, `PiiEncrypted` cast + `PiiCrypter`), but `Customer.email_enc`/`phone_enc` remain bound to the routinely-rotated APP_KEY and have no `PiiEncrypted` cast. No regression (unchanged by this change), but APP_KEY rotation could 500 customer reads as it did for distributors. Adopt `PiiEncrypted` on Customer + extend `pii:reencrypt` to cover those columns before Phase 2 commerce depends on them | 2 | Open (2026-06-08) |
| R-30 | Sharing distributor's name + ADN disclosed to a shared-cart ("Easy Purchase") guest | Data protection — Medium | DPDP 2023 (minimisation); T&C §3; Hard Rule 8 | A guest who opens a distributor's shared cart link is shown the sharing distributor's **ADN + registered full name** (read-only) at checkout so they know who they're buying through (`CheckoutController::resolveBuyerDistributor`, `referral` mode). Controls: only ADN + name are shown — distributor email/phone are deliberately withheld (`SCT-06` asserts `assertDontSee(email)`); no BV/earnings shown (hard rule #3); the disclosure is self-initiated (the distributor created the share link); the guest-checkout exception is scoped (a session pass set on link-open, **cleared on order placement** — `SCT-09`) so it cannot leak across unrelated sessions/carts. Residual: `full_name` is the registered legal name (not a chosen display name), and a share link is forwardable. Accepted for Easy Purchase; revisit a distributor "display name" field if abuse appears | 2 | Accepted (2026-06-14) |
| R-31 | Full PAN + full Aadhaar held encrypted at rest pre-KYC (deviates from Hard Rule 8) | Data protection — High | DSR 2021; DPDP 2023 §8; Hard Rule 8; T&C §15 | The `distributors.pan_encrypted` / `aadhaar_encrypted` columns (migration `2026_05_12_000001`) hold the **full** PAN and **full** Aadhaar number at rest, on Product-Owner request, until KYC approval — `ApproveKycSubmission` then **nulls both columns and purges the uploaded images**, leaving only `pan_last4` / `aadhaar_last4`. Hard Rule 8 says raw Aadhaar is **never** stored (only an AUA/KUA reference + last-4), so holding the full number — even transiently and encrypted — is a deliberate deviation. Controls in place: `PiiEncrypted` cast → `PiiCrypter` (AES-256-CBC) on the dedicated `PII_ENCRYPTION_KEY` (ADR-0008); columns `$hidden`; only masked last-4 accessors exposed; values nulled + images purged on approval; surfaced by the T-6.1 security audit (finding F3). **GAP:** the migration comment claims this is "logged in the risk register" but until this entry it was **not**, and there is **no formal risk-acceptance record** (no `docs/security/risk-acceptances.md`). **Action before Phase-1 exit:** Compliance Officer + Product Owner must either (a) formally sign off this deviation with a documented legal basis and retention bound, or (b) re-architect to store only the AUA/KUA reference + last-4 from the outset. Do **not** treat as accepted until signed — formal sign-off record drafted at `docs/security/risk-acceptances.md` (RA-01) awaiting PO + Compliance Officer signatures | 1 | **Open (2026-06-17)** |
| R-32 | Paid GSB retained on post-settlement order cancellation/refund (no clawback) | Statutory — Medium | DSR 2021 Rule 5(1)(c); Hard Rule 2; KP Q8 (kp-clarifications 2026-06-27); ADR-0010 | When an order is cancelled/refunded AFTER its group BV fed a settled GSB cut-off, the credited/paid GSB is deliberately **not** clawed back — Product-Owner direction (KP Q8: "no clawback of already-paid bonus; reverse the BV from future accumulation on the same leg"). Controls: the full BV is reversed via `GroupBvReversalService` (ADR-0010) — netted off the unsettled day where possible, otherwise carried as a same-side negative (`group_bv_debts`) that suppresses future GSB until recovered, so the economics self-correct over time; every reversal audit-logged (`bv.group_reversed`) with per-ancestor `group_bv_reversals` rows; titles/ranks earned are sticky per the same KP answer. Residual (three faces of the same KP directive, all self-correcting via the same-side debt but exposed until enough same-side volume arrives): (a) a commission remains paid on a sale that was ultimately refunded (incl. statutory cooling-off returns); (b) cancelled BV that a settled-but-unpaid day folded into the slab-1 weaker / power **carry-forward** stays in CF (the CF store is never mutated outside a cut-off — rewind-machinery invariant) and could contribute to a later slab match before the debt nets out; (c) `RankQualificationService` sums raw monthly `group_bv_daily`, so a month's rank gate can count BV cancelled after that day settled (KP round-3 #4 says ranks are sticky and only future BV is reduced — consistent, but the same DSR-spirit question). Literal hard-rule-2 linkage to a `product_sale_id` holds in all three; the DSR-spirit exposure needs **legal opinion at the batched launch sign-off** (defer-all-sign-offs gate) and KP's re-confirmation that CF/rank retention is intended. If counsel rejects, Phase-4's wallet-clawback mechanism (ADR-0009 forward note) plus a CF-adjustment pass are the fallback | 4 | Open (2026-07-05) |

| R-33 | GSB slabs 3–7 pro-rated from a daily 45% pool — variable payouts, KP-accepted deviations, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice), §6.5 (pool cap pre-authorised); DSR 2021 Rule 5(1)(d)/(g); Hard Rules 2 & 3; KP worked example 2026-07-29 | Slabs 1–2 stay fixed (₹250/score); slabs 3–7 are priced daily at `min(₹250, floor((pool − slab-1/2 fixed) ÷ Σ slab-3–7 scores))` where pool = `comp.gsb.pool_rate_bp` (45%) of the day's order-backed company BV (`bv_ledger_entries` signed sum — Rule-2 linkage verified: non-nullable `order_id` FK, no non-sale pathway). Controls: economics frozen per day in `gsb_daily_pools` **before any credit**; `score_value_paise` snapshotted per result row (history immune to plan edits and re-runs); single-distributor retries reuse the frozen value; admin per-day report (GSB Input & Output) exposes pool/value/variance/leftover; all behind `GsbDailyPoolPricingFeature` (default OFF = legacy byte-identical). KP-accepted deviations: (a) on a starved day fixed slab 1–2 payouts are still paid in full and may exceed the 45% pool (negative `leftover_paise` recorded); (b) a slab 3–7 match can price at **₹0** while the weaker-leg BV is still consumed (distributor copy discloses the ₹0 floor); (c) zero-variable-achiever days freeze the ₹250 cap so a later admin retry pays full value; (d) frozen/repurchase-held/suspended matched rows are counted in the funded denominator though they may never credit — residue stays with the company. **Activation gate: do NOT enable `GsbDailyPoolPricingFeature` in any environment paying real distributors until the DSA §6.2 30-day written notice to all active Distributors has run and the pool formula is published at `/p/compensation`** (page created and seeded 2026-07-30 — `database/seeders/content/compensation.md`, §5.4 publishes the slab 3–7 pooled score-value formula and the ₹250 ceiling; **remaining pre-activation task: replace the `[EFFECTIVE DATE — TO BE INSERTED]` placeholder with the date stated in the §6.2 notice, and obtain counsel sign-off on the page**) | 3 | Open (2026-07-29) |
| R-34 | Stealth `developer` super-role: platform-configuration actions are concealed from the admin audit-log view | Governance / audit — Medium | DSR 2021 Rule 5 (record-keeping); CLAUDE.md audit-log convention; R-17 (separation of duties) | A `developer` role supersets `admin` and exclusively owns platform configuration (feature flags, compensation-plan edits, and the developer-owned settings keys — plan rates/caps, payout thresholds, cooling-off, placement, attribution, self-purchase, payments, notifications). Product-Owner requirement: the role must be invisible to every other role. Consequence: in the admin console's audit-log view (`AuditLogPresenter::maskPrivilegedActors`), rows whose actor holds `developer` render the actor as `system` — including for **`admin-compliance`**, the role designated to review audit trails. That weakens R-17's stated compensating control ("every action is audit-logged **with the actor** — traceable after the fact") for exactly the actions with the largest economic reach. Controls that remain: `audit_log.actor_id` is **never** altered — the full actor is retained in the database and is recoverable by direct query, by a developer-role viewer, and by any external/forensic export; masking is presentation-only and applies to no other role; every developer action still writes its audit row with full before/after details; plan-edit and settings-write paths are unchanged in what they record. Mitigations added after the 2026-07-29 compliance review: (i) the **registration killswitch** is owned by `incident` and remains toggleable by the whole console incl. `admin-compliance` (`AdminFeatureFlagController::canUse`) — an incident control must not depend on one person's availability; (ii) the statutory/deduction keys (`commerce.cooling_off.days`, `comp.tds.rate_bp`, `comp.admin_charge.rate_bp`, `payout.*` thresholds) are **visible read-only** to admin via `ADMIN_READABLE_KEYS`, so compliance can still verify the 30-day term and the deduction rates; (iii) audit rows *about* a privileged account are removed at query level (`AuditLogPresenter::hidePrivilegedSubjects`) because actor-masking alone leaked the role through the `staff.*` details payload and the resolved subject name. Residual (accepted, PO-directed): an admin-compliance reviewer working solely from the console cannot attribute the config/plan rows to a person, and the compensation-engine flags — including the R-33 gate — are toggleable only by the role that owns them. **Action: Product Owner to confirm the concealment requirement outranks console-level audit attribution for admin-compliance, and to record who holds the developer credential; revisit if an external auditor requires console-only attribution.** | 3 | Open (2026-07-29) |
| R-35 | Mentorship Bonus pro-rated daily from a 3% pool — variable payouts, no configured point value, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; KP worked examples 2026-07-30 | MSB no longer pays `points × a configured ₹250 value` (that column is dropped from `gsb_slabs`). Each day the pool = `comp.msb.pool_rate_bp` (300 bp = 3%) of the day's order-backed company BV (the same signed `bv_ledger_entries` sum the GSB pool uses — Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway), and the point value = `floor-to-whole-rupee(pool ÷ the day's total MSB score points)`, clamped at ₹0 (company BV is a signed sum and can be negative on a refund-heavy day). Every earner is paid `their points × that one value`, so the day's MSB spend is the 3% envelope less the flooring remainder. Controls: economics frozen in `msb_daily_pools` **before any credit** (immutable row, `msb.pool.frozen` audit entry); `msb_point_value_paise` snapshotted per `mentorship_bonus_results` row (history immune to plan edits and re-runs); single-distributor retries reuse the frozen value; the admin **MSB Input & Output Per Day** report exposes BV/pool/points/value/income/leftover per day with CSV. Accepted deviations: (a) an admin retry credits points that were not in the frozen denominator, so `leftover_paise` can go negative and the company absorbs it (mirrors R-33); (b) a **zero-point day** freezes a ₹0 value, so the pool goes unspent and a later retry on that day writes a ₹0 row with no wallet entry — **this is the opposite of the GSB pool's zero-achiever rule** (R-33, where the cap is frozen so a retry pays full value), chosen so a single retried sponsor cannot take a whole day's pool. **Ruled and confirmed 2026-07-30: a zero-point day freezes a ₹0 point value and nothing is paid to anyone for that day, ever, including on a later retry.** The asymmetry with R-33 is deliberate and follows from GSB having a ceiling that MSB does not: every GSB slab's score value is capped at ₹250 (slabs 1–2 fixed at that value, slabs 3–7 variable but never exceeding it), so freezing the cap on a zero-achiever day is inherently bounded — the worst case is one retried distributor paid at the normal maximum. The MSB point value is pure `pool ÷ points` with no cap at all, so there is no ceiling to freeze, and any non-zero standby value would hand a single retried sponsor a disproportionate share of an otherwise unspent day. The report flags such days and every ₹0 credit is logged. **No §6.2 breach has occurred for this revision**: `MentorshipBonusFeature::resolve()` returns `false` and the flag has never been enabled in any environment paying real distributors, so **no MSB has ever been credited to a live distributor** and the plan was never published — the §6.2 obligation attaches to first activation, not to this change. This matters because the revision is otherwise squarely a reduction: a slab-1 sponsor went from a guaranteed ₹5,250 (21 × ₹250) to a variable amount — ₹1,050 in KP's own example, and possibly ₹0. **No independent killswitch (accepted):** unlike GSB (whose pool sits behind its own `GsbDailyPoolPricingFeature` with a byte-identical fixed-price OFF path), MSB's pool is gated only by `MentorshipBonusFeature` — the same flag that decides whether MSB exists at all — and the migration drops `msb_score_value_paise`, so there is no fixed-price fallback. Rollback procedure if the pool arithmetic misbehaves: switch `MentorshipBonusFeature` off (itself a §6.2 event once live), then `php artisan migrate:rollback` the `2026_07_30_100001` migration to restore `gsb_slabs.msb_score_value_paise` (default ₹250) and re-seed `GsbSlabsSeeder`, then restore the fixed-price pricing in `MentorshipBonusService::creditAccrual()`. **Reset vs evidence:** `platform:reset-purchases` truncates `msb_daily_pools`/`gsb_daily_pools`, bypassing the models' append-only guard — after a reset, a day's economics are reconstructible only from the retained `msb.pool.frozen` / `gsb.pool.frozen` `audit_log` rows (the audit log is deliberately preserved by the reset). **Activation gates — both must be cleared before any real MSB payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula is published at `/p/compensation` (page created and seeded 2026-07-30 — §6.2 publishes the point-value formula, §6.3 states the value is variable and may be ₹0, §6.4 publishes the zero-point-day rule, §6.5 marks MSB not yet operative; **remaining pre-activation task, shared with R-33: replace the `[EFFECTIVE DATE — TO BE INSERTED]` placeholder with the date stated in the §6.2 notice, and obtain counsel sign-off on the page**). Deviation (b) was signed off on 2026-07-30 and is no longer a gate.** | 3 | Open (2026-07-30) |
| R-36 | Growth Booster Bonus pro-rated monthly from a 5% pool — variable point value, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; PO confirmations 2026-08-05 | GBB has **no configured rupee value per point**. Each month the pool = `comp.gbb.pool_rate_bp` (500 bp = 5%) of the month's order-backed company **BV** — the same signed `bv_ledger_entries` sum the GSB and MSB pools use, **not** order sales value (Product Owner confirmed 2026-08-05; Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway) — and the point value = `floor-to-whole-rupee(pool ÷ the total AGP of all eligible distributors that month)`. Every earner is paid `their AGP × that one value`, so the month's GBB spend is the 5% envelope less the flooring remainder. **A distributor's per-point rupee value is therefore not fixed in advance**: it falls as the month's total AGP rises and rises as company BV rises, neither of which any distributor can know or control, and it may be ₹0. AGP accrue only from GSB slab matches (12 / 5 / 2 for slabs 1–3, none for 4–7), capped at 120 AGP per distributor per month; eligibility is limited to distributors who held **no rank in the previous month** (first-time rank achievers in the current month remain eligible). Controls: economics frozen in a `gbb_monthly_pools` row **before any credit** (immutable, `gbb.pool.frozen` audit-log entry) and never recomputed, so re-runs and single-distributor retries price against the same snapshot — an auditable, tamper-evident trail matching the GSB (R-33) and MSB (R-35) daily pools; the point value is snapshotted per result row so history is immune to plan edits and later re-runs; the admin **GBB calculation report** exposes BV, pool, eligible distributors, total AGP, point value, income, leftover and held/suspended rows per month. Repurchase interaction (disclosed in the admin help): GBB calculated during a distributor's repurchase grace window is **held** (`repurchase_held`) and released on completion — held distributors remain in the pool denominator because they may yet be paid; once the grace window lapses it is **forfeited** (`repurchase_suspended`) and those distributors are excluded from the denominator so they cannot dilute the value of a bonus they can never receive. **No §6.2 breach has occurred**: the GBB feature flag resolves `false` and has never been enabled in any environment paying real distributors, so no GBB has ever been credited to a live distributor and the component is published at `/p/compensation` §10 as *not yet operative*. **Activation gates — both must be cleared before any real GBB payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula, the AGP schedule and cap, the prior-month-rank eligibility gate and the variable/possibly-₹0 point value are published at `/p/compensation` with the effective date stated in that notice (shares the outstanding `[EFFECTIVE DATE — TO BE INSERTED]` placeholder and counsel sign-off task with R-33 and R-35).** Related: the Rank Bonus pool derivation was corrected in the same change — each rank's `pool %` is a share of the **20% Rank-Bonus envelope of company BV**, not of turnover directly (10,00,000 BV → ₹2,00,000 envelope → Rank 1's 7% = ₹14,000) | 4 | Open (2026-08-05) |
| R-37 | Fortune Bonus pro-rated monthly from a 5% pool — variable point value, "luck or chance" framing must never reach public copy, DSA §6.2 notice gate | Statutory — Medium | DSA §6.2 (plan disclosure, 30-day notice); DSR 2021 Rule 5(1)(c)/(d); Hard Rules 2 & 3; KP Fortune rework 2026-08-07 | Fortune no longer pays a **fixed rupee amount per matrix level** (the ₹3.39 … ₹51.00 ladder; `fortune_bonus_levels.bonus_paise` is dropped, so no fixed-price fallback survives — same one-way design as MSB in R-35). Each month the pool = `comp.fortune.pool_rate_bp` (500 bp = 5%, superseding the June envelope's 6% Fortune share) of the month's order-backed company **BV** — the same signed `bv_ledger_entries` sum the GSB, MSB and GBB pools use (Rule-2 linkage inherited: non-nullable `order_id` FK, no non-sale pathway) — and the point value = `floor-to-whole-rupee(pool ÷ the total Fortune points of everyone enrolled that month)` *[SUPERSEDED 2026-08-09 — see the amendment at the end of this row: values are now computed per matrix level through the cascade, not month-wide]*. Points are earned **only from the distributors placed below the earner** in that month's 3-wide, 9-level matrix (*[superseded ladder — now 9 / 8 / 7 / 6 / 5 / 4 / 3 / 2 / 1, see amendment]* per member at relative levels 1–9, admin-editable; deeper than 9 earns nothing), so a distributor's rupee value is **not fixed in advance and cannot be quoted**: it falls as the month's total points rise, rises as company BV rises, and **may be ₹0** (a sub-rupee division floors to zero and the whole pool goes unspent). **There is deliberately no manual override of the computed point value** (KP 2026-08-07) — an admin can edit the per-level point schedule but can never set a rupee figure for a month or for an individual, which is the control that keeps the payout arithmetic uniform and auditable. Enrolment gates (all monthly, all requiring the repurchase wallet cleared — `IncomeEligibilityService` holds during the grace window and suspends once it lapses): month-1 joiners need 3,000 BV lifetime personal purchases **and GSB slab 1 specifically**; month-2+ non-ranked need a personal-purchase title (≥3,000 BV lifetime) + 600 BV in the month + ≥1 slab; ranks 1–5 need 1,000 / 1,100 / 1,200 / 1,300 / 1,400 BV with **8 / 11 / 14 / 17 / 20** credited slab achievements (repeats counted — GSB has only 7 distinct slabs); ranks 6–9 are excluded. Controls: economics frozen in a `fortune_monthly_pools` row **before any credit** (immutable, `fortune.pool.frozen` audit-log entry) and never recomputed, so re-runs and single-distributor retries price against the same snapshot — the same auditable, tamper-evident trail as R-33 / R-35 / R-36; **enrolment into a month whose pool row already exists is REFUSED by the engine (`fortune.enroll.refused_pool_frozen`), so no one can be added to the matrix after the point value is fixed — a late entrant would otherwise be paid at the frozen value on points that were never in the frozen denominator, overspending `pool_paise`**; `points` + `point_value_paise` snapshotted per `fortune_bonus_results` row so history is immune to plan edits and later re-runs; the admin **FB Monthly Calculation** report exposes company BV, pool, total points, point value, payout and leftover per month plus each participant's level, points, value and income. **Mis-selling hazard — explicit: KP's own written spec describes the Fortune Bonus as income "based on luck or chance". That framing must NEVER appear in public copy, distributor-facing UI, training material or any presentation** — a bonus presented as a game of chance invites both a DSR 2021 Rule 5(1)(d) mis-selling reading and a lottery/gaming characterisation. The published description at `/p/compensation` §7 and every internal help page describe it factually as a **participation-based monthly pool** whose value is variable and may be ₹0, with no example figures and no forward-looking amounts. Any copy review touching Fortune must re-check this. **No §6.2 breach has occurred**: `FortuneBonusFeature` resolves `false` and has never been enabled in any environment paying real distributors, so no Fortune Bonus has ever been credited to a live distributor. **Activation gates — both must be cleared before any real Fortune payout: (i) the DSA §6.2 thirty-day written notice has run; (ii) the pool formula, the per-level point schedule, the participation gates, the variable/possibly-₹0 point value **and the FCFS position-dependence — that a month's matrix is filled first-come, first-served on first-GSB-credit date, so what a distributor earns depends on where they land relative to distributors who qualify later in the same month, a structural advantage of earlier qualifiers that must be disclosed rather than left implicit and must never be framed as luck or chance (counsel to confirm the disclosure wording alongside the §6.2 notice)** — are published at `/p/compensation` §7 with the effective date stated in that notice (shares the outstanding `[EFFECTIVE DATE — TO BE INSERTED]` placeholder and counsel sign-off task with R-33, R-35 and R-36).** Open with KP (do not block, restated at sign-off): whether the matrix should mirror the Genos/sponsorship structure rather than the monthly FCFS fill, and whether the non-ranked "one of 7 titles" gate means Retailer or a higher specific title. **2026-08-09 amendment (KP's level cascade):** depth points changed to 9/8/7/6/5/4/3/2/1; every qualifier now has a guaranteed **₹30 minimum** (`comp.fortune.min_commission_paise`, pro-rated `floor(pool ÷ N)` when the pool cannot cover it — the pool is never overspent); capped levels 0–6 pay `min(₹30 + points × per-level whole-rupee value, cap)` with caps ₹30k/₹30k/₹30k/₹30k/₹20k/₹10k/₹5k **including** the ₹30; levels 7–8 share one residual value; level 9 gets the ₹30 only; sparse months keep the absolute-level treatment. The frozen snapshot now includes per-level economics (`fortune_monthly_pool_levels`), still immutable, still no manual override; incomes remain variable and quotable by no one (a capped level's value depends on the whole month's points, and the residual value may be ₹0). **Rule 5(1)(c) rationale for the per-head minimum** (write-down demanded by the 2026-08-09 compliance review): the ₹30 is not a recruitment or participation fee in reverse — every recipient must have cleared the month's sales-conditioned gates (≥1 credited GSB slab that month, i.e. matched order-backed group BV on both Genos sides, plus the tier's own 600–3,000 BV personal-purchase requirement), it is funded exclusively from the 5% order-backed BV pool, and it pro-rates to ₹0 in a month with no sales — so no rupee of it can exist without product sales. All prior controls, the "never frame as luck or chance" rule, the FCFS disclosure item and both activation gates carry forward unchanged — the §6.2 notice must describe the cascade (minimum, caps, residual sharing), **activation gate (ii) now additionally requires the published plan page (`app/database/seeders/content/compensation.md` §7.3–§7.5, reconciled to the cascade 2026-08-09) to be re-verified against the engine at notice time**, and the two 2026-08-09 interpretation decisions (absolute levels in sparse months; pro-rated minimum on shortfall) were made by the product owner's proxy and need KP's written confirmation before the notice is drafted | 6 | Open (2026-08-09) |

## Compliance items C-01 … C-09 (Phase 1)

Aligned to the PRD §12. Each must be signed before Phase 1 exits.

| ID | Item | Signed by | Signed date | Artefact |
|---|---|---|---|---|
| C-01 | Joining free of cost enforced in code | | | Test + PR link |
| C-02 | No income projection in registration UI | | | Content review memo |
| C-03 | Orientation gate enforced | | | Test + PR link |
| C-04 | Cooling-off one-click cancellation | | | Test + PR link |
| C-05 | Register of DS export working, retention policy set | | | Admin export screenshot + retention ADR |
| C-06 | DPDP: consent is purpose-limited, revocable, DF contact published | | | Privacy notice + consent table migration |
| C-07 | State-aware age rule honoured (MH=21) | | | Test + PR link |
| C-08 | Declarations (jail/bankrupt/sound-mind) captured | | | Test + PR link |
| C-09 | Placement transparency — snapshot + resolved side visible to DS | | | Test + screenshot |

## Accepted risks (dated)

| Date | Risk ID | Acceptor(s) | Reason | Review date |
|---|---|---|---|---|
| 2026-05-25 | R-16 | compliance-officer | Residual TOCTOU: concurrent placement under the requester is not fully serialized by the target-parent advisory lock during approval. Low placement volume in Phase 1 makes the race practically unreachable | Phase 2 exit gate |
| 2026-05-25 | R-17 | compliance-officer | Any `admin` may approve/reject a binary-placement move; a dedicated `admin-operations` role is deferred. Small, trusted admin team in Phase 1 | Phase 2 exit gate |
| 2026-07-30 | R-35 (b) | Business owner | Zero-point MSB day freezes a ₹0 point value: the day's pool goes unspent and no MSB is paid for that day even on a later admin retry. Accepted knowing R-33 rules the GSB twin the other way — GSB's score value is capped at ₹250 for every slab (1–2 fixed, 3–7 variable but never above it) so freezing the cap is bounded, whereas the uncapped MSB point value has no ceiling to freeze | First real MSB payout |
