@props(['label'])

{{-- Section heading row inside the trading-account table. --}}
<tr class="bg-gray-50">
    <td colspan="2" class="px-4 py-2 text-xs font-semibold uppercase tracking-wider text-gray-700">{{ $label }}</td>
</tr>
